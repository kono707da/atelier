# Atelier 更新日志

本文件记录每个版本的详细变更，最新版本在前。提交代码前追加新版本记录。

## v0.8.5 — 2026-08-02 — 修复 API JSON 导入后画布节点堆叠问题

分支：`dev-260802-GLM5.2-workflow-import-file-upload`（基于 `main`，合并到 `GLM-MAIN`）

### 变更摘要

API JSON 格式的工作流导入后所有节点位置为 [0,0]，导致画布上节点全部堆叠重叠。修复：导入 API JSON 后自动调用后端布局计算，画布加载时检测节点全在原点也自动触发布局。

### 前端变更

- **runtime-api.js submitWorkflowImport**：导入完成后如果 `sourceFormat === "api_json"`，自动调用 `POST /api/workflows/{id}/draft/layout/compute` 计算节点位置。
- **runtime-api.js renderProductionWorkflowCanvas**：加载草稿后检测所有节点是否都在 [0,0]，如果是则自动调用布局计算并重新加载。
- **index.html**：版本标签更新至 `v20260802-ux-v109`。

### 测试

- 创建 API JSON 格式测试工作流（7 节点），导入后所有节点位置确认为 [0,0]。
- 修复后打开画布，7 个节点自动分散到不同位置（14~710px / 68~263px），不再堆叠。

## v0.8.4 — 2026-08-02 — 工作流导入弹窗改版（拖拽上传 + 自动识别）

分支：`dev-260802-GLM5.2-workflow-import-file-upload`（基于 `main`，合并到 `GLM-MAIN`）

### 变更摘要

工作流导入弹窗改版：删除粘贴 textarea 和来源格式下拉框，改为拖拽上传区（dropzone），前端自动识别 UI/API 格式，文件名自动填入工作流名称。

### 前端变更

- **runtime-api.js 导入弹窗**：重写为 dropzone 布局，点击或拖拽 `.json` 文件上传，`FileReader` 读取后用 `detectWorkflowFormat` 自动识别格式（UI JSON 有 `nodes` 数组，API JSON 值含 `class_type`），文件名（去 `.json` 扩展名）自动填入工作流名称。
- **runtime-api.js submitWorkflowImport**：从 dropzone 暂存的 `_parsedJson` 和 `_sourceFormat` 取值，不再依赖 textarea 和 select。
- **styles.css**：新增 `.workflow-import-dropzone` 及子元素样式（虚线边框、hover 高亮、拖拽高亮、图标、文件信息行）。
- **index.html**：版本标签更新至 `v20260802-ux-v108`。

### 测试

- 浏览器验证：选择 test-workflow.json 文件后，dropzone 切换为已选择状态，显示文件名和"UI 格式 · 1 KB"，工作流名称自动填入"test workflow"。

## v0.8.3 — 2026-08-02 — 工作流导入弹窗新增文件上传

分支：`dev-260802-GLM5.2-workflow-import-file-upload`（基于 `main`，合并到 `GLM-MAIN`）

### 变更摘要

工作流导入弹窗新增"选择文件"按钮，用户可选择 `.json` 文件自动读取内容填入 textarea 预览后再提交导入，无需手动粘贴 JSON 文本。

### 前端变更

- **runtime-api.js 导入弹窗**：`ensureWorkflowImportModal` 新增文件选择行（按钮 + 隐藏 file input + 文件名提示），通过 FileReader 读取 `.json` 文件内容填入 textarea。`openWorkflowImportModal` 打开弹窗时重置文件选择状态。
- **styles.css**：新增 `.workflow-import-file-row` 和 `.workflow-import-file-name` 样式。
- **index.html**：版本标签更新至 `v20260802-ux-v107`。

### 测试

- 浏览器验证：工作流页点击"导入 JSON"按钮，弹窗显示"选择文件"按钮和文件名提示，来源格式下拉框包含 API/UI 两个选项。

## v0.8.2 — 2026-07-29 — 阶段1-2验收修复（FK 修复/撤销重做/前端契约对齐）

分支：`dev-260729-GLM5.2-阶段1-2验收修复`（基于 `GLM-MAIN`，不直接合并到 `main`）

### 变更摘要

修复 v0.8.0 验收发现的 3 个阻塞问题并完成部分验收。stale FK 引用修复（v0.8.1 迁移重建受影响表）、`move_large_scene` 新增操作记录支持撤销重做（§7.3）、前端 ComfyUI 实例字段对齐后端契约、API JSON 导入推导输出端口。§13.4 阶段1 API 验收 10/10 通过，§13.5 浏览器质量验收 9/11 通过。剩余未完成项交接给下一个 AI，详见需求文档 §18.4。

### 后端变更

- **database.py v0.8.1 迁移**：新增 `_migrate_fix_materials_fk_references`，扫描所有表的 `REFERENCES` 子句，修复对 `materials_old_v052`/`characters_old_v053`/`projects_old_v051`/`material_pages_old_v081`/`chapters_tmp_v081`/`character_variants_tmp_v081` 的 stale FK 引用。采用 CREATE+COPY+DROP+RENAME 重建受影响表，保留索引。备份 `atelier.production.sqlite3.bak-v081`。
- **app_factory.py `move_large_scene`**：新增 `record_operation` 调用捕获 before/after 状态，新增 `_lookup_project_id_for_large_scene` 反查项目 ID。记录失败仅 warning 不阻断主流程。

### 前端变更

- **runtime-api.js ComfyUI 实例字段对齐**：创建/编辑实例请求体改用 `base_url`/`websocket_url`/`timeout_seconds`，替代 `httpUrl`/`wsUrl`，对齐后端契约。
- **runtime-api.js `parse_api_json` 推导输出端口**：根据已建立的 links 反推每个节点的输出端口 slot 数量和 link 关联，修复 API JSON 导入后无法新增连线问题。

### 其他变更

- `.gitignore` 新增 `data/databases/*.bak-*` 忽略迁移备份文件。

### 验收状态

- §13.4 阶段1 API 验收：10/10 通过（生产数据库，端口 8110）
- §13.5 浏览器质量验收：9/11 通过，2 项需复核（快照入口、设置页 ComfyUI 控件）
- 后端自动化测试：本轮未重跑全量（上一轮 v0.8.0 已通过 1175/1175）
- 真实 ComfyUI 验收（§9）：未执行
- 浏览器端完整流程验收（§13.1/§13.2/§13.3/§13.4）：未执行
- 实际生图状态：**未进入阶段 3，未实际生图**

### 未完成项交接

详见归档需求文档 `需求归档/全功能开发阶段一与阶段二验收整改需求.md` §18.4 未完成项交接清单。

## v0.8.0 — 2026-07-29 — 阶段1-2验收整改（工作流导出修复/ComfyUI多实例/前端全量接入）

分支：`dev-260729-GLM5.2-全功能开发`

### 变更摘要

按《全功能开发阶段一与阶段二验收整改需求》完成3大阻塞问题修复和前端全量接入：工作流导出旧数据修复（编辑后基于normalized_graph导出）、ComfyUI多实例管理（新增comfyui_instances表/8个API路由/探测/激活/旧设置迁移）、移除测试数据库路由（生产模式不注册）、工作流库真实接入（列表/创建/导入/复制/归档/删除）、工作流画布真实接入（节点显示/编辑/连线/插槽/保存/预检查/发布/导出）、设置页ComfyUI多实例管理、顶部连接状态指示器、阶段1功能补齐（分支CRUD/快照/撤销重做/预检查UI）。新增52项后端测试，修复3个前端路由契约不匹配。全量1175项测试通过。

### 后端变更

- **工作流导出旧数据修复（§5）**：
  - `app_factory.py` `update_node_in_draft` 新增 `widgets_values` → `inputs[].value` 同步逻辑
  - 编辑后导出基于 `normalized_graph` 生成，不返回旧JSON
  - 11项导出测试覆盖编辑后场景
- **ComfyUI多实例管理（§4）**：
  - `database.py` 新增 `_migrate_comfyui_instances` 迁移（v0.8.0），创建 `comfyui_instances` 表
  - 8个DatabaseManager方法：list/get/create/update/delete/activate/get_active/update_status
  - 8个API路由：GET/POST/PATCH/DELETE实例、激活、测试连接、同步节点、探测
  - 旧 `app_settings` 中的单实例设置自动迁移为一条活动实例记录
  - `_build_comfyui_client` 优先从活动实例读取配置
  - 38项测试覆盖CRUD、活动实例唯一约束、探测不自动切换、旧设置迁移
- **移除测试数据库路由（§8）**：
  - `/api/settings/databases`、`/activate`、`/verify-isolation` 三个路由仅 `environment=="test"` 时注册
  - 生产模式访问返回404
  - 3项测试验证生产模式404

### 前端变更

- **runtime-api.js API客户端（§6.2）**：新增35个工作流和ComfyUI相关API端点
- **工作流库真实接入（§6.3）**：
  - `renderProductionWorkflows()` 真实列表渲染，骨架/空态/错误态
  - 创建/导入JSON/从图片提取/复制/归档/删除弹窗
  - 防竞态分页加载
- **工作流画布真实接入（§6.4）**：
  - `renderProductionWorkflowCanvas()` 三栏布局（节点库+画布+检查器）
  - 节点显示/选中/编辑widgets_values/新增/删除
  - 连线显示/新增（点击端口）/删除（点击连线）
  - 语义插槽绑定/删除（13种插槽类型）
  - 保存草稿/预检查/发布版本/导出JSON
- **设置页ComfyUI多实例管理（§6.5）**：
  - `renderProductionSettings()` 实例列表渲染
  - 添加/编辑/删除/激活/测试连接/同步节点/探测候选实例
- **顶部连接状态（§6.6）**：
  - 顶部导航栏ComfyUI连接状态指示器（绿/橙/红）
  - 30秒轮询，页面切换时刷新
- **阶段1功能补齐（§7）**：
  - 剧本分支CRUD弹窗（创建/编辑/删除/启用禁用/覆盖项管理）
  - 剧本快照弹窗（创建/查看/恢复）
  - 撤销/重做按钮（调用操作历史API）
  - 编译预检查弹窗（范围选择/执行/结果显示阻塞错误和警告）

### 测试

- `test_comfyui_instances.py`（新增）：38项测试
- `test_foundation_constraints.py`（修改）：3项生产路由隐藏测试
- `test_workflow_publish.py`（已有）：11项导出修复测试
- `test_frontend_route_contract.py`（修复）：3个路由契约不匹配已修复
- 全量1175项测试通过

### 验收状态

- 后端自动化测试：1175/1175 通过
- 真实ComfyUI验收（§9）：待执行（需启动应用连接127.0.0.1:8188）
- 浏览器验收（§13）：待执行

## v0.7.0 — 2026-07-29 — 编译、批量生成与任务中心（阶段 3）

分支：`dev-260729-GLM5.2-全功能开发`（继续在同一开发分支）

### 变更摘要

按《Atelier 全功能产品与技术开发需求》阶段 3「编译、批量生成与任务中心」完成后端全链路：页级编译器（配置继承优先级/确定性编译/阻塞错误收集/六类素材页映射解析/工作流版本固定/语义插槽解析）、跑图列表和批量配置（草稿管理/范围选择/批量覆盖/不可变批次快照）、持久化任务队列（批次/任务/attempt/事件/租约、原子领取、失败重试不覆盖旧记录、暂停/继续/取消、重启恢复）、ComfyUI 提交（API JSON 构建/幂等性保护/超时处理）、实时进度与故障恢复（WebSocket 监听/ProgressTracker/SSE 推送/断开重连/history 轮询兜底/重启核对）、输出和图片实例（输出解析/流式下载/原子移动/files/image_instances/thumbnails/background_jobs 表）、任务中心 API（跨批次列表/统计摘要/错误详情）。新增 7 个业务模块、5 张数据库表、50+ 个 API 路由。新增 239 个测试用例，全量 1111 项测试通过。

### 后端变更

- `backend/app/compiler.py`（新增）：页级编译器
  - `compile_project`：从项目结构编译页级跑图项，支持范围选择（项目/章节/大场景/小场景/分支/页面）。
  - 配置继承优先级：项目 → 章节 → 大场景 → 小场景 → 分支 → 页面。
  - `_compile_page`：返回 (RenderItem, blocking_errors, warnings) 三元组，收集阻塞错误不生成 item。
  - `_compute_input_hash`：基于完整输入快照生成稳定哈希，支持去重和变更检测。
  - `_resolve_semantic_slots`：可选在编译期解析工作流语义插槽。
  - `sort_key` 确定性生成，相同输入产生相同顺序。
- `backend/app/batch_drafts.py`（新增）：跑图列表和批量配置
  - 草稿 CRUD、范围选择预览、批量覆盖（实例数/工作流/尺寸/种子策略）。
  - `commit_batch`：从草稿生成不可变批次快照。
  - 阻塞错误和警告收集，阻塞项不能提交。
- `backend/app/task_queue.py`（新增）：持久化任务队列
  - `create_tasks_from_batch`：从批次快照展开为页级任务（幂等）。
  - `claim_next_task`：原子领取，创建 attempt 和租约，防止并发重复领取。
  - `mark_attempt_submitted/completed/failed`：状态机更新，失败时根据重试次数决定 retrying 或 failed。
  - `pause_batch/resume_batch/cancel_batch/cancel_task/retry_task/set_task_priority`：批次和任务级控制。
  - `recover_submitted_attempts`：应用重启后核对 ComfyUI 队列和历史，标记 unknown 状态。
  - `list_all_tasks/get_task_center_summary`：跨批次任务列表和统计摘要。
- `backend/app/comfyui_submit.py`（新增）：ComfyUI 提交
  - `build_api_json_for_item`：从跑图项构建 API JSON，应用已解析插槽。
  - `submit_task_to_comfyui`：调用 /prompt 提交，幂等性保护（已有 prompt_id 不重复提交）。
  - `check_comfyui_history`：查询 ComfyUI 历史获取执行结果。
- `backend/app/comfyui_progress.py`（新增）：实时进度与故障恢复
  - `ProgressTracker`：内存索引，按 prompt_id 存储进度、事件和状态。
  - `ComfyUIWebSocketListener`：连接 ComfyUI WebSocket，自动重连，解析事件更新 tracker。
  - `sse_progress_generator`：SSE 推送给前端，支持超时和订阅清理。
  - `poll_comfyui_history_for_attempt`：WebSocket 断开时的 history 轮询兜底。
  - `recover_submitted_attempts`：重启后核对未终结任务，标记 unknown 状态。
- `backend/app/output_receiver.py`（新增）：输出和图片实例
  - `collect_attempt_outputs`：查询 ComfyUI 历史，解析所有输出节点和图片。
  - `download_and_validate_image`：流式下载到临时文件，PIL 校验格式和完整性，原子移动到目标目录。
  - `files`/`image_instances`/`thumbnails`/`background_jobs` 表持久化文件元数据和图片实例关系。
  - 创建缩略图后台任务基础（多尺寸级别）。
- `backend/app/database.py`：新增 v0.7.0 迁移
  - 新增 5 张表：batches、tasks、task_attempts、task_events、task_leases。
  - 新增 4 张表：files、image_instances、thumbnails、background_jobs。
  - 全部表支持 `revision` 乐观并发控制和 `deleted_at` 软删除。
- `backend/app/app_factory.py`：应用版本从 0.6.0 升至 0.7.0
  - 集成 ProgressTracker 和 ComfyUIWebSocketListener 到应用生命周期。
  - 新增 50+ 个 API 路由：编译预览、草稿 CRUD、批次提交、任务领取、attempt 状态、ComfyUI 提交、SSE 进度、history 轮询、输出收集、任务中心列表/摘要/错误详情。
  - 应用启动时启动 WebSocket 监听器，停止时优雅关闭。
- `requirements.txt`：新增 `websockets>=12.0`

### 测试

- `backend/tests/test_compiler.py`（新增）：页级编译器测试
  - 配置继承优先级、确定性编译、阻塞错误收集、范围选择、素材映射解析、工作流版本固定。
- `backend/tests/test_batch_drafts.py`（新增）：跑图列表和批量配置测试
  - 草稿 CRUD、范围预览、批量覆盖、阻塞错误、不可变批次快照。
- `backend/tests/test_task_queue.py`（新增）：持久化任务队列测试
  - 任务创建幂等性、原子领取、并发限制、失败重试不覆盖旧记录、暂停/继续/取消、重启恢复。
- `backend/tests/test_comfyui_submit.py`（新增）：ComfyUI 提交测试
  - API JSON 构建、提交成功、幂等性、超时处理、历史查询。
- `backend/tests/test_comfyui_progress.py`（新增）：实时进度与故障恢复测试
  - ProgressTracker 更新和订阅、WebSocket 监听器、SSE 推送、history 轮询兜底、重启恢复。
- `backend/tests/test_output_receiver.py`（新增）：输出和图片实例测试
  - 输出解析、图片下载和校验、原子移动、数据库写入、缩略图任务创建。
- `backend/tests/test_task_center.py`（新增）：任务中心 API 测试
  - 跨批次任务列表、多维筛选、统计摘要、错误详情。
- 全量 1111 项测试通过（872 项回归 + 239 项新增 + 9 项 ComfyUI 真实集成跳过），无回归。

### 阶段 3 集成验收状态

后端自动化测试已全部通过。需求 §3.8 集成验收的以下项需要真实 ComfyUI 服务（`192.168.3.5:8188`）和浏览器验收，未在本轮自动化测试覆盖：

- 从一个真实小场景编译多页、多分支跑图列表并提交到 ComfyUI。
- 验证实时进度和全部输出。
- 断开网络再恢复。
- 重启 Atelier 再恢复。
- 重跑失败项且不重复成功项。

前端任务中心页面（`page=tasks`）未接入，待阶段 4 或前端专项开发时实现。

## v0.6.0 — 2026-07-29 — 转换、校验和发布（阶段 2.6）

分支：`dev-260729-GLM5.2-全功能开发`（继续在同一开发分支）

### 变更摘要

按《Atelier 全功能产品与技术开发需求》阶段 2.6「转换、校验和发布」完成工作流转换与发布闭环：实现规范化结构→API JSON 转换（节点/连线/widgets_values/禁用·旁路节点跳过）、规范化结构→UI JSON 转换（节点/连线/分组/last_node_id·last_link_id）、导出功能（优先原始 JSON 或从规范化结构生成）、发布前预检查（节点定义同步/必填输入连线/模型资源/悬空连线/未知节点/语义插槽绑定）、基于草稿发布不可变版本、往返测试（导入—导出—重新导入数据完整性验证）。新增 4 个 API 路由。新增 38 个测试用例，全部 881 项测试通过。

### 后端变更

- `backend/app/workflow_publish.py`（新增）：工作流转换、校验和发布模块
  - `normalized_to_api_json`：规范化结构→ComfyUI API JSON（节点ID作key/class_type/inputs/连线引用为 [source_node, source_slot]/值参数保留/widgets_values 补充/禁用·旁路节点跳过）。
  - `normalized_to_ui_json`：规范化结构→ComfyUI UI JSON（last_node_id/last_link_id/nodes/links/groups/mode 映射禁用=2/旁路=4）。
  - `export_workflow`：导出为 api_json 或 ui_json 格式，优先返回原始 JSON，否则从规范化结构生成，返回 format/data/node_count/checksum。
  - `precheck_publish`：发布前预检查，返回 can_publish/blocking_errors/warnings/summary，检查 6 类问题（unknown_node/missing_definition/missing_required_input/dangling_link/slot_node_not_found/empty_widget_value）。
  - `roundtrip_test`：往返测试，解析→导出 API JSON→重新解析→比较节点数和连线数，返回 success/original_count/roundtrip_count/errors。
- `backend/app/app_factory.py`
  - 应用版本从 0.5.9 升至 0.6.0。
  - 新增 2 个请求模型：ExportWorkflowRequest（format 校验）/RoundtripTestRequest（source_format 校验）。
  - 新增 4 个 API 路由：
    - POST /api/workflows/{workflow_id}/export — 导出工作流为 API JSON 或 UI JSON
    - POST /api/workflows/{workflow_id}/precheck — 发布前预检查
    - POST /api/workflows/{workflow_id}/publish — 基于草稿发布不可变版本
    - POST /api/workflows/roundtrip-test — 往返测试（不写入数据库）

### 测试

- `backend/tests/test_workflow_publish.py`（新增）：38 个测试用例
  - NormalizedToApiJsonTests（5）：基本转换/连线引用/值参数保留/禁用节点跳过/旁路节点跳过。
  - NormalizedToUiJsonTests（6）：基本结构/节点字段/连线格式/禁用模式/旁路模式/分组保留。
  - ExportWorkflowTests（5）：API JSON 导出/UI JSON 导出/优先原始 API JSON/优先原始 UI JSON/不支持格式报错。
  - PrecheckPublishTests（7）：干净工作流通过/缺失定义阻塞/未知节点阻塞/必填输入缺失阻塞/悬空连线阻塞/插槽节点不存在阻塞/统计正确。
  - RoundtripTests（4）：UI JSON 往返/API JSON 往返/无效输入返回失败/自动格式检测。
  - ExportApiTests（4）：API 导出 API JSON/导出 UI JSON/草稿不存在 404/无效格式 422。
  - PrecheckApiTests（3）：预检查通过/缺失定义阻塞/草稿不存在 404。
  - PublishApiTests（3）：基于草稿发布/版本号递增/草稿不存在 404。
  - RoundtripApiTests（1）：API 往返测试。
- 全量 881 项测试通过，无回归。

## v0.5.9 — 2026-07-29 — 语义插槽（阶段 2.5）

### 变更摘要

实现工作流语义插槽系统：13 个内置插槽定义（positive_prompt/negative_prompt/character_prompt/lora_name/lora_weight/checkpoint/vae/seed/width/height/batch_size/output_prefix/custom）、转换规则模板（{value}/{character_name} 等上下文变量）、插槽解析优先级（业务上下文值 > 节点值 > 默认值）、4 种冲突策略（overwrite/skip/merge/error）、必填校验、插槽绑定校验、插槽应用到工作流。新增 4 个 API 路由。新增 35 个测试用例。

### 后端变更

- `backend/app/workflow_slots.py`（新增）：语义插槽模块
- `backend/app/app_factory.py`：新增 4 个路由（GET/PUT/DELETE /api/workflows/{id}/semantic-slots、POST /api/workflows/{id}/slots/resolve、POST /api/workflows/{id}/slots/validate、GET /api/slot-definitions）

## v0.5.8 — 2026-07-29 — 规整布局（阶段 2.4）

### 变更摘要

实现工作流规整布局算法：稳定拓扑分层（Kahn 算法 + 环检测）、结构化调整（移到同层前/后、上/下列、置顶/置底）、分组泳道（创建/编辑/删除/分配节点）、连线合束（同源长连线合并 + 端口附近拆分）、聚焦上/下游/完整路径/错误节点、500 节点性能测试。新增 14 个 API 路由。新增 53 个测试用例。

### 后端变更

- `backend/app/workflow_layout.py`（新增）：规整布局模块
- `backend/app/app_factory.py`：新增 14 个路由（layout/compute、layout、layout/nodes/reorder、groups CRUD、assign-group、links/bundles、focus、layout/perf-test）

## v0.5.7 — 2026-07-29 — 通用节点编辑器（阶段 2.3）

### 变更摘要

实现通用节点编辑器：动态生成节点端口组件、显示自定义节点、编辑参数、连线创建/删除/重连、端口类型校验（are_ports_compatible）、未知节点只读保留、复制/删除/禁用/旁路/折叠/多选、节点 ID 和连线 ID 分配器、工作流校验（validate_workflow 检查未知节点/缺失定义/悬空连线/重复连线）。新增 9 个 API 路由。新增测试用例。

### 后端变更

- `backend/app/workflow_models.py`：扩展 allocate_node_id/allocate_link_id/are_ports_compatible/validate_workflow
- `backend/app/app_factory.py`：新增 9 个路由（draft/nodes CRUD、draft/nodes/{id}/duplicate、draft/links CRUD、draft/validate）
- `backend/tests/test_workflow_editor.py`（新增）

## v0.5.6 — 2026-07-29 — 工作流数据与导入（阶段 2.2）

### 变更摘要

实现工作流规范化结构和导入解析：NormalizedWorkflow 数据类（nodes/links/groups/metadata）、parse_ui_json（UI JSON→规范化，保留 pos/size/mode/flags/widgets_values/inputs/outputs）、parse_api_json（API JSON→规范化，自动推断值类型）、extract_workflow_from_image（PNG tEXt/iTXt + WebP EXIF 提取 workflow/prompt 元数据）、parse_workflow_from_raw（自动格式检测）、serialize/deserialize_workflow、detect_format。新增数据库表（workflows/workflow_versions/workflow_drafts/workflow_semantic_slots）。新增 12 个 API 路由。新增测试用例。

### 后端变更

- `backend/app/workflow_models.py`（新增）：规范化结构与导入解析器
- `backend/app/database.py`：新增 v0.5.6 迁移（4 张工作流表 + 索引）
- `backend/app/app_factory.py`：新增 12 个路由（workflows CRUD、archive/restore/copy、import、import-from-image、versions、draft GET/PUT）
- `backend/tests/test_workflows.py`（新增）

## v0.5.5 — 2026-07-29 — ComfyUI 连接层（阶段 2.1）

分支：`dev-260729-GLM5.2-全功能开发`（继续在同一开发分支）

### 变更摘要

按《Atelier 全功能产品与技术开发需求》阶段 2.1「ComfyUI 连接层」完成 ComfyUI 服务端集成基础：新建 `comfyui_client.py` HTTP 客户端模块（连接池/超时/错误处理/系统状态/节点定义/模型资源/工作流提交/图片下载/中断释放）、数据库 v0.5.5 迁移新增 4 张表（app_settings/comfyui_node_definitions/comfyui_resource_cache/comfyui_sync_meta）、应用设置读写与 ComfyUI 配置管理、节点定义全量同步与缓存（SHA256 摘要/自定义节点识别/分类/搜索/分页）、模型资源同步与缓存（checkpoints/loras/vaes/embeddings/controlnet/upscale_models）、连接测试与错误展示。新增 11 个 API 路由。新增 55 个测试用例（含 9 个真实 ComfyUI 集成测试），全部 657 项测试通过。

### 后端变更

- `backend/app/comfyui_client.py`（新增）：ComfyUI HTTP 客户端模块
  - `ComfyUIConnectionConfig`：不可变连接配置（base_url/timeout_seconds/websocket_url），支持 derived_websocket_url 自动推导。
  - `ComfyUIClient`：HTTP 客户端，内部持有 httpx.Client 连接池，支持配置热更新。
  - 方法：test_connection/get_system_stats/get_object_info/get_node_info/get_models/get_embeddings/get_queue/get_history/submit_prompt/download_image/interrupt/free。
  - 辅助函数：compute_object_info_sha256（节点定义摘要）/summarize_node_definitions（节点统计）/extract_resource_lists（从 object_info 提取模型资源列表）。
  - 错误处理：ComfyUIError 统一异常，区分连接失败/超时/HTTP 错误/JSON 解析失败。
- `backend/app/database.py`
  - 新增 v0.5.5 迁移 `_migrate_comfyui_connect`：
    - `app_settings` 表（key/value/updated_at），插入默认 ComfyUI 配置（base_url/timeout_seconds/websocket_url）。
    - `comfyui_node_definitions` 表（node_class/python_module/category/display_name/definition_json/is_custom_node/updated_at），索引 category 和 is_custom_node。
    - `comfyui_resource_cache` 表（resource_type/resource_name/updated_at，复合主键）。
    - `comfyui_sync_meta` 表（sync_key/value/updated_at，追踪同步状态）。
  - 新增 12 个方法：
    - 应用设置：get_setting/set_setting/get_settings/get_comfyui_settings/set_comfyui_settings。
    - 节点定义缓存：save_node_definitions（全量替换）/list_node_definitions（分类/自定义/搜索/分页）/get_node_definition/get_node_definition_summary/list_node_categories。
    - 资源缓存：save_resource_cache（全量替换）/list_resource_cache（类型/搜索）。
- `backend/app/app_factory.py`
  - 应用版本从 0.5.4 升至 0.5.5。
  - 新增 ComfyUI 客户端实例管理：`_build_comfyui_client`/`_refresh_comfyui_client`（配置变更后重建连接池）。
  - 新增 2 个请求模型：ComfyUISettingsRequest（地址格式校验/超时范围校验）/SyncResourcesRequest。
  - 新增 11 个 API 路由：
    - 设置：GET/PUT /api/settings/comfyui
    - 连接：POST /api/comfyui/test-connection、GET /api/comfyui/system-stats
    - 同步状态：GET /api/comfyui/sync-status
    - 节点定义：POST /api/comfyui/sync-object-info、GET /api/comfyui/node-definitions、GET /api/comfyui/node-definitions/{node_class}、GET /api/comfyui/node-categories
    - 资源：POST /api/comfyui/sync-resources、GET /api/comfyui/resources
- `requirements.txt`：新增 httpx==0.28.1 依赖。

### 测试

- `backend/tests/test_comfyui_connection.py`（新增）：55 个测试用例
  - `AppSettingsTests`（7）：默认设置/读写/覆盖/前缀查询/部分更新/全量更新。
  - `ComfyuiSettingsApiTests`（6）：API 读写/尾部斜杠清理/空地址拒绝/非 HTTP 拒绝/超时范围拒绝。
  - `NodeDefinitionCacheTests`（9）：保存列表/获取/未找到/自定义筛选/分类筛选/搜索/分类列表/摘要/全量替换。
  - `ResourceCacheTests`（4）：保存列表/全量替换/搜索/多类型。
  - `ComfyUIClientUnitTests`（10）：配置默认值/标准化/WebSocket 推导/HTTPS 推导/覆盖/SHA256 稳定性/节点统计/资源提取/配置更新关闭旧客户端。
  - `ComfyUIClientMockTests`（4）：连接成功/连接失败/模型列表/错误处理。
  - `ComfyuiApiCacheTests`（7）：空同步状态/节点定义列表/节点定义详情/404/分类列表/资源列表/不可达错误。
  - `ComfyuiRealIntegrationTests`（9，条件执行）：真实连接测试/节点定义同步/系统状态/资源同步/节点定义查询/分页/自定义筛选/同步状态/资源缓存。
  - 测试结果：657 passed（原 602 + 新增 55，无回归）。

### 修改文件

- `backend/app/comfyui_client.py`：新增 ComfyUI HTTP 客户端模块。
- `backend/app/database.py`：v0.5.5 迁移、12 个新方法。
- `backend/app/app_factory.py`：版本号、2 个请求模型、11 个新路由、ComfyUI 客户端管理。
- `requirements.txt`：新增 httpx 依赖。
- `backend/tests/test_comfyui_connection.py`：新增测试文件。
- `更新日志.md`：新增 v0.5.5 版本记录。

---

## v0.5.4 — 2026-07-29 — 剧本结构补全（阶段 1.5）+ 阶段集成验收（1.6）

分支：`dev-260729-GLM5.2-全功能开发`（继续在同一开发分支）

### 变更摘要

按《Atelier 全功能产品与技术开发需求》阶段 1.5「剧本结构补全」完成 6 项剧本结构增强：分支条件字段（condition_type/condition_value/return_point）、分支覆盖数据（人物/素材/参数覆盖，支持页面级优先）、剧本快照和版本（序列化/恢复/自动备份）、操作历史与撤销重做（before/after 状态快照模式）、页面级继承来源展示（6层链路+有效值来源）、编译预检查（6种范围/阻塞项+警告项+摘要）。story-tree 接口增强返回分支节点信息。阶段 1.6 集成验收通过 8 个端到端测试（完整项目生命周期、重启持久化、软删除恢复、快照恢复、撤销重做、继承链、无展示数据回退、分支条件覆盖）。新增 44 个测试用例（36 个结构测试 + 8 个集成验收），全部 602 项测试通过。

### 后端变更

- `backend/app/database.py`
  - 新增 v0.5.4 迁移 `_migrate_branches_extend`：
    - branches 表添加 condition_type/condition_value/return_point 字段。
    - 新增 branch_overrides 表（branch_id/override_type/target_id/character_id/variant_id/material_id/material_page_id/param_key/param_value，UNIQUE 约束）。
    - 新增 story_snapshots 表（project_id/label/snapshot_data JSON）。
    - 新增 operation_history 表（project_id/operation_type/entity_type/entity_id/before_state/after_state JSON）。
  - 分支方法扩展：list_branches/get_branch/create_branch/update_branch 支持 condition 字段。
  - 新增分支覆盖方法：list_branch_overrides/create_branch_override/update_branch_override/delete_branch_override/get_effective_overrides（页面级覆盖优先于分支级）。
  - 新增剧本快照方法：create_story_snapshot（序列化章节/大场景/小场景/场景页/分支/映射/覆盖为 JSON）/list_story_snapshots/get_story_snapshot/restore_story_snapshot（恢复前自动备份当前状态）。
  - 新增操作历史方法：record_operation/list_operations/undo_operation（按 before_state 恢复+记录 redo）/redo_operation。辅助方法：_capture_entity_state/_apply_entity_state/_insert_entity_from_state/_upsert_entity_from_state。
  - 新增继承链方法：get_shot_page_inheritance（返回 project→chapter→large_scene→small_scene→branch→shot_page 链 + 有效值 + 来源映射）。
  - 新增编译预检查方法：precheck_compilation（支持 project/chapter/large_scene/small_scene/branch/shot_pages 六种范围，检查空场景/缺失人物绑定/缺失素材映射/空提示词/无效分支引用，返回 blocking/warnings/summary）。
  - story-tree 增强：get_story_tree 每个 small_scene 增加 branches 数组（含 id/name/condition_type/condition_value/is_enabled/pages）。
- `backend/app/app_factory.py`
  - 应用版本从 0.5.3 升至 0.5.4。
  - 新增 4 个请求模型：CreateBranchOverrideRequest/UpdateBranchOverrideRequest/CreateSnapshotRequest/PrecheckRequest。
  - 扩展 CreateBranchRequest/UpdateBranchRequest 支持 condition_type/condition_value/return_point。
  - 新增 14 个 API 路由：
    - 分支覆盖：GET/POST /api/branches/{id}/overrides、PATCH/DELETE /api/branch-overrides/{id}
    - 有效覆盖：GET /api/shot-pages/{id}/effective-overrides
    - 剧本快照：GET/POST /api/projects/{id}/snapshots、GET /api/story-snapshots/{id}、POST /api/story-snapshots/{id}/restore
    - 操作历史：GET /api/projects/{id}/operations、POST /api/operations/{id}/undo、POST /api/operations/{id}/redo
    - 继承链：GET /api/shot-pages/{id}/inheritance
    - 编译预检查：POST /api/projects/{id}/precheck

### 测试

- `backend/tests/test_story_structure.py`（新增）：36 个测试用例
  - BranchConditionTests（5）：分支条件字段 CRUD。
  - BranchOverrideTests（8）：覆盖 CRUD + 有效覆盖查询 + 页面级优先。
  - StorySnapshotTests（6）：快照创建/列表/详情/恢复/自动备份。
  - OperationHistoryTests（5）：操作记录/列表/撤销/重做。
  - InheritanceTests（4）：继承链查询 + 有效值来源。
  - PrecheckTests（5）：编译预检查（阻塞项/警告项/空项目/完整项目/范围筛选）。
  - StoryTreeBranchTests（3）：story-tree 包含分支信息。
- `backend/tests/test_stage1_integration.py`（新增）：8 个端到端集成验收测试
  - test_full_project_lifecycle：完整项目生命周期（2章节/3大场景/6小场景/多场景页/分支/素材映射/人物矩阵/场景页绑定/分支覆盖/快照/预检查）。
  - test_persistence_across_restart：重启后全部数据恢复。
  - test_soft_delete_and_restore：软删除+恢复（场景页用快照恢复验证）。
  - test_story_snapshot_restore：快照后修改结构，恢复后回到快照状态。
  - test_operation_undo_redo：操作撤销+重做闭环。
  - test_inheritance_chain：6层继承链查询。
  - test_no_demo_data_fallback：空实例无展示数据回退。
  - test_branch_with_conditions_and_overrides：分支条件+人物覆盖+参数覆盖+有效覆盖查询。
  - 测试结果：602 passed（原 558 + 新增 44，无回归）。

### 修改文件

- `backend/app/database.py`：v0.5.4 迁移、15+ 新方法、story-tree 增强。
- `backend/app/app_factory.py`：版本号、4 个请求模型、14 个新路由。
- `backend/tests/test_story_structure.py`：新增测试文件。
- `backend/tests/test_stage1_integration.py`：新增集成验收测试。
- `更新日志.md`：新增 v0.5.4 版本记录。

---

## v0.5.3 — 2026-07-29 — 人物库闭环（阶段 1.4）

分支：`dev-260729-GLM5.2-全功能开发`（继续在同一开发分支）

### 变更摘要

按《Atelier 全功能产品与技术开发需求》阶段 1.4「人物库闭环」完成人物库全生命周期管理：人物库表结构扩展迁移（归档/删除/来源/标签/封面字段）、变体表扩展（预览图/默认LoRA/归档/来源字段）、规格表扩展（说明/必填/默认值）、新增 character_tags 和 shot_page_characters 表；人物归档/恢复/软删除/回收站/永久删除、引用反查（项目/小场景/场景页）、标签管理、封面上传/缩略图、人物复制（含变体和规格值）；变体 CRUD/归档/恢复/复制/排序/预览图上传；规格矩阵查询/批量更新；场景页人物绑定/解绑；项目-人物关联管理。前端人物库 UI 全面增强（列表搜索/筛选/归档切换/分页、卡片操作按钮组/封面/标签、详情页标签管理/封面/变体预览图/规格矩阵编辑）。修复后端 get_character/list_character_variants 关键字参数 bug，修复旧表迁移索引创建失败问题。新增 60 个测试用例，全部 558 项测试通过。

### 后端变更

- `backend/app/database.py`
  - 新增 v0.5.3 迁移 `_migrate_characters_extend`：
    - characters 表：添加 description/cover_path/archived_at/deleted_at/source/source_identifier/external_url 字段，去除 UNIQUE(name) 约束（支持同名/复制），name 列添加 COLLATE NOCASE。重建表时保留旧数据。
    - character_variants 表：添加 description/default_prompt/default_lora_name/default_lora_weight/default_model_override/preview_original_path/preview_thumbnail_path/archived_at/source_variant_id 字段。
    - specs 表：添加 description/is_required/default_value 字段。
    - character_spec_values 表：添加 preview_original_path/preview_thumbnail_path 字段。
    - 新增 character_tags 表（id/character_id/tag/created_at，UNIQUE(character_id, tag)）。
    - 新增 shot_page_characters 表（shot_page_id/character_id/variant_id/created_at）。
    - 索引创建移到迁移函数中无条件执行（IF NOT EXISTS），修复旧表缺列时 executescript 失败的问题。
  - 修复 `get_character` 和 `list_character_variants` 的关键字参数 bug（environment 是 keyword-only 参数，但不能以位置参数调用）。
  - `list_characters` 重写：支持 search/tag/include_archived/include_deleted/sort/limit/offset 参数，返回 {items, total, limit, offset, has_more} 结构。每个 item 含 variant_count。
  - `get_character` 扩展：返回 tags 数组。
  - `create_character` 扩展：支持 description/source/source_identifier/external_url/tags 参数，自动创建默认变体和已有规格的空值。
  - 新增 20+ 个人物方法：
    - `archive_character`/`restore_character`/`soft_delete_character`/`list_deleted_characters`/`permanent_delete_character`（级联删除变体、规格值、标签、项目关联、场景页绑定）
    - `get_character_references`（JOIN projects/small_scenes/shot_pages，返回引用列表和计数）
    - `set_character_tags`（PUT 替换语义，去重、空字符串清理）
    - `set_character_cover_path`（设置/清除 cover_path）
    - `copy_character`（source 溯源，复制变体和规格值，不复制预览图文件）
    - `get_character_spec_matrix`（返回 character/specs/variants/values/missing_required 结构）
    - `batch_update_spec_values`（批量更新，事务回滚）
  - 变体方法扩展：
    - `list_character_variants` 增加 include_archived 参数
    - `create_character_variant` 支持预览图、默认LoRA等字段
    - `archive_character_variant`/`restore_character_variant`
    - `copy_character_variant`（source_variant_id 溯源）
    - `reorder_character_variants`（按 variant_ids 列表重排 sort_order）
    - `set_character_variant_preview_paths`/`get_character_variant_preview_paths`
  - 场景页人物引用：
    - `set_shot_page_character`（绑定人物+变体到场景页）
    - `get_shot_page_character`（返回绑定信息含人物名/变体名/默认提示词等）
    - `clear_shot_page_character`（解绑）
- `backend/app/app_factory.py`
  - 应用版本从 0.5.2 升至 0.5.3。
  - 新增 15 个请求模型（CreateCharacterRequest/UpdateCharacterRequest/CopyCharacterRequest/SetCharacterTagsRequest/SetShotPageCharacterRequest 等）。
  - 新增 40 个人物相关 API 路由（第 1495-2184 行）：
    - 人物 CRUD + from-role 创建
    - 归档/恢复/软删除/永久删除/复制
    - 标签管理（PUT 替换）
    - 封面上传/删除/原图/缩略图（支持 PNG/JPEG/WebP，20MB 限制，512px 缩略图）
    - 引用反查
    - 变体 CRUD + 归档/恢复/复制/排序/预览图上传删除读取
    - 规格 CRUD + 矩阵查询 + 批量更新
    - 场景页人物绑定/读取/解绑
    - 项目-人物关联/取消关联

### 前端变更

- `design/ui-preview/runtime-api.js`
  - API 常量新增 23 个人物相关路由（characterArchive/characterRestore/characterCopy/characterTags/characterCover/characterMatrix/characterVariants/characterVariantPreview 等）。
  - `renderProductionCharacters` 重写：工具栏（搜索框/标签筛选/归档切换/回收站切换）、骨架屏、空状态、错误重试、卡片网格、分页加载更多。
  - `characterCard` 增强：封面图显示（缩略图/首字母占位）、标签 chips（最多3个+N）、操作按钮组（归档/恢复/复制/移入回收站/永久删除）、归档角标。
  - `renderCharacterDetail` 扩展：封面图+上传按钮、标签管理区（chips+添加/删除）、变体列表增强（预览图缩略图/编辑/复制/归档/删除/排序按钮）、规格矩阵编辑视图（横轴规格×纵轴变体，单元格编辑 prompt/lora/weight/model_override）。
  - 新增 `characterTagsSection`/`characterMatrixSection`/`characterVariantList` 等函数。
  - 新增 34 个 data-api-action 事件处理（archive-character/restore-character/copy-character/upload-character-cover/character-tag-add/remove/archive-character-variant/copy-character-variant/upload-variant-preview/move-variant-up/down 等）。
  - 修复变体创建表单重复问题（characterVariantList 中的重复表单已移除）。
- `design/ui-preview/styles.css`
  - 新增约 41 处人物库样式：卡片操作按钮组、归档角标、回收站卡片、标签 chips、封面图区域、变体预览图、规格矩阵表格。
- `design/ui-preview/index.html`：静态资源版本号更新为 `v=20260729-characters-v053`。

### 测试

- `backend/tests/test_characters_library.py`（新增）：60 个测试用例
  - `CharacterArchiveTests`（10 用例）：归档/恢复/软删除/回收站/永久删除/级联清理。
  - `CharacterCopyTests`（5 用例）：人物复制（含变体、规格值、source 溯源）。
  - `CharacterTagsTests`（6 用例）：标签替换/去重/清空/筛选。
  - `CharacterCoverTests`（9 用例）：PNG/JPEG/WebP 上传、原图/缩略图读取、删除、错误格式。
  - `CharacterReferencesTests`（3 用例）：项目引用、场景页引用反查。
  - `CharacterVariantExtendedTests`（10 用例）：变体归档/恢复/复制/排序/预览图。
  - `CharacterMatrixTests`（5 用例）：矩阵结构、必填缺失。
  - `BatchSpecValueTests`（2 用例）：批量更新、事务回滚。
  - `ShotPageCharacterTests`（5 用例）：场景页人物绑定/读取/解绑。
  - `ProjectCharacterLinkTests`（5 用例）：项目人物关联/取消/列表。
  - 测试结果：558 passed（原 498 + 新增 60，无回归）。
- `backend/tests/test_characters.py`：修复 4 个过时测试
  - `test_duplicate_character_name_in_same_project_rejected` → `_allowed`（v0.5.3 去除 UNIQUE 约束，允许同名）。
  - `test_rename_character_rejects_duplicate` → `_allows_duplicate`（允许重名）。
  - `test_duplicate_variant_name_rejected` → `_allowed`（允许变体同名）。
  - `test_character_data_stays_in_test_database`（适配 list_characters 返回 dict 结构）。
  - `test_delete_character`（适配软删除语义，检查 deleted_at 而非 None）。
  - `test_delete_character_cascades_to_variants` → `test_soft_delete_character_keeps_variants`（软删除保留变体）。

### 修改文件

- `backend/app/database.py`：v0.5.3 迁移、20+ 新方法、get/list_characters 重写、bug 修复。
- `backend/app/app_factory.py`：版本号、15 个请求模型、40 个新路由。
- `design/ui-preview/runtime-api.js`：API 常量、列表/卡片/详情/矩阵重写、34 个事件处理。
- `design/ui-preview/styles.css`：人物库新样式。
- `design/ui-preview/index.html`：版本号。
- `backend/tests/test_characters_library.py`：新增测试文件。
- `backend/tests/test_characters.py`：适配测试更新。
- `更新日志.md`：新增 v0.5.3 版本记录。

---

## v0.5.2 — 2026-07-29 — 素材库闭环（阶段 1.3）

分支：`dev-260729-GLM5.2-全功能开发-项目管理闭环`（继续在同一开发分支）

### 变更摘要

按《Atelier 全功能产品与技术开发需求》阶段 1.3「素材库闭环」完成素材库全生命周期管理：素材表扩展迁移（归档/删除/来源字段）、素材页预览图与来源字段、版本历史表；素材归档/恢复/软删除/回收站/永久删除、引用反查（项目/小场景/场景页）、素材页预览图上传、素材页复制、素材版本历史（快照/列表/恢复）、素材复制（复制后独立）。新增 39 个测试用例，全部 498 项测试通过。

### 后端变更

- `backend/app/database.py`
  - 新增 v0.5.2 迁移 `_migrate_materials_extend`：
    - materials 表添加 archived_at/deleted_at/source_material_id 字段，去除 UNIQUE(material_type, name) 约束以支持同名素材（复制场景）。
    - material_pages 表添加 preview_original_path/preview_thumbnail_path/source_page_id 字段。
    - 新增 material_versions 表（id/material_id/version_number/snapshot/label/created_at）。
  - `get_material` SELECT 增加新字段，WHERE 增加 deleted_at IS NULL（软删除素材不可访问）。
  - `list_materials` 增加 include_archived/include_deleted 参数（默认排除归档和删除）。
  - `list_material_pages`/`get_material_page` SELECT 增加预览图和来源字段。
  - `update_material` 末尾自动调用 create_material_version 创建版本快照。
  - 新增 13 个方法：
    - `archive_material`/`restore_material`/`soft_delete_material`/`list_deleted_materials`/`permanent_delete_material`（级联删除 6 张关联表）
    - `get_material_references`（JOIN small_scenes→large_scenes→chapters→projects + small_scene_page_mappings→shot_pages，返回引用列表和计数）
    - `set_material_page_preview_paths`/`copy_material_page`（"副本"后缀+序号去重，source_page_id 溯源）
    - `create_material_version`/`list_material_versions`/`get_material_version`/`restore_material_version`（JSON 快照，恢复时重建素材页）
    - `copy_material`（source_material_id 溯源，status='unverified'，复制素材页，不复制预览图文件）
- `backend/app/app_factory.py`
  - 应用版本从 0.5.1 升至 0.5.2。
  - DELETE /api/materials/{id} 改为软删除（不再删除预览图目录）。
  - list_materials 路由增加 archived/trash 查询参数。
  - 新增 16 个路由：archive/restore/permanent/trash/references/copy、versions CRUD+restore、material-pages preview 上传/删除/读取/缩略图/copy。
  - 新增 CopyMaterialRequest、CreateMaterialVersionRequest 请求模型。

### 前端变更

- `design/ui-preview/runtime-api.js`
  - API 常量新增 12 条素材相关路由。
  - 素材列表页：工具栏增加归档/回收站切换按钮；卡片操作按钮组（归档/恢复、复制、移入回收站）；回收站卡片变体（恢复/永久删除）；删除确认框显示引用影响（调用 references API）；复制模态框。
  - 素材详情页：新增素材页面板（列表/编辑/删除/复制/预览图上传/排序/新建）和版本历史面板（列表/保存快照/恢复版本）。
  - 注册 19 个新 data-api-action 事件处理。
- `design/ui-preview/styles.css`
  - 新增约 290 行 CSS：素材视图切换栏、卡片操作区、归档角标、回收站卡片、素材页面板及卡片、版本面板及卡片。
- `design/ui-preview/index.html`：静态资源版本号更新为 `v=20260729-materials-v052`。

### 测试

- `backend/tests/test_materials_library.py`（新增）：39 个测试用例
  - `MaterialArchiveTests`（6 用例）：归档、恢复、归档排除、参数包含、回收站恢复。
  - `MaterialSoftDeleteTests`（7 用例）：软删除、回收站列表、永久删除、关联清除。
  - `MaterialReferencesTests`（5 用例）：空引用、小场景引用、场景页映射引用、计数。
  - `MaterialPagePreviewTests`（8 用例）：PNG/JPEG 上传、读取、404、删除、非法格式。
  - `MaterialPageCopyTests`（3 用例）：副本命名、序号、source_page_id。
  - `MaterialVersionTests`（6 用例）：快照创建、列表、详情、恢复、自动版本。
  - `MaterialCopyTests`（4 用例）：source_material_id、页复制、空名 422。
  - 测试结果：498 passed（原 459 + 新增 39，无回归）。
- `backend/tests/test_materials.py`：3 个同名测试更新为允许同名（v0.5.2 去除 UNIQUE 约束）。
- `backend/tests/test_scene_materials.py`：1 个级联删除测试改用 permanent 端点。

### 修改文件

- `backend/app/database.py`：v0.5.2 迁移、13 个新方法、get/list_materials 更新。
- `backend/app/app_factory.py`：版本号、16 个新路由、请求模型。
- `design/ui-preview/runtime-api.js`：API 常量、列表页增强、详情页面板、事件处理。
- `design/ui-preview/styles.css`：素材库新样式。
- `design/ui-preview/index.html`：版本号。
- `backend/tests/test_materials_library.py`：新增测试文件。
- `backend/tests/test_materials.py`、`backend/tests/test_scene_materials.py`：适配测试更新。
- `更新日志.md`：新增 v0.5.2 版本记录。

---

## v0.5.1 — 2026-07-29 — 项目管理闭环（阶段 1.2）

分支：`dev-260729-GLM5.2-全功能开发-项目管理闭环`（从 `dev-260729-GLM5.2-全功能开发-基础约束` 拉取新建）

### 变更摘要

按《Atelier 全功能产品与技术开发需求》阶段 1.2「项目管理闭环」完成项目全生命周期管理：项目扩展字段迁移、项目 CRUD 与同名支持、搜索/筛选/排序/分页、归档与恢复、软删除/回收站/永久删除、项目复制（结构连同关联映射）、项目概览统计与阻塞项检测、项目封面上传/删除/读取。新增 49 个测试用例，全部 459 项测试通过。

### 后端变更

- `backend/app/database.py`
  - 新增 v0.5.1 迁移 `_migrate_projects_extend`：扩展 projects 表，添加 description/cover_path/archived_at/deleted_at 字段，去除 name 的 UNIQUE 约束以支持同名项目；幂等执行，重建表时保留 revision 字段。
  - `list_projects` 重写：支持 search/status/created_after/archived/include_archived/include_deleted/sort/page/page_size 参数，返回 items/total/page/page_size/has_more 结构。
  - 新增 `update_project`：更新 name/description，自增 revision。
  - 新增 `archive_project`：设置 archived_at 和 status='archived'。
  - 新增 `restore_project`：清除 archived_at 和 deleted_at，恢复 status='draft'。
  - 新增 `soft_delete_project`：设置 deleted_at。
  - 新增 `list_deleted_projects`：列出回收站项目。
  - 新增 `permanent_delete_project`：级联删除项目及其章节、大场景、小场景、场景页、分支、关联和映射。
  - 新增 `copy_project`：复制项目及章节、大场景、小场景、场景页、分支、关联素材、素材页映射；新项目 status='draft'。
  - 新增 `get_project_stats`：单查询统计章节/大场景/小场景/场景页/关联素材/人物数量。
  - 新增 `set_project_cover_path`：设置或清除 cover_path，自增 revision。
- `backend/app/app_factory.py`
  - 新增 `UpdateProjectRequest`、`CopyProjectRequest` 请求模型。
  - 新增 4 个全局异常处理器（StarletteHTTPException/RequestValidationError/DatabaseSafetyError/Exception），统一错误响应为 `{detail, error:{code, message, details, request_id}}`。
  - 新增 `request_id_middleware`：为每个请求注入唯一 request_id，响应头返回 X-Request-ID。
  - 新增项目管理 API 路由：
    - `GET /api/projects`（搜索/筛选/排序/分页）
    - `POST /api/projects`（创建，支持 description）
    - `GET /api/projects/{id}`（详情，含全部扩展字段）
    - `PATCH /api/projects/{id}`（改名/改描述）
    - `POST /api/projects/{id}/archive`（归档）
    - `POST /api/projects/{id}/restore`（恢复）
    - `DELETE /api/projects/{id}`（软删除）
    - `DELETE /api/projects/{id}/permanent`（永久删除，清理封面目录）
    - `POST /api/projects/{id}/copy`（复制结构）
    - `GET /api/projects/{id}/overview`（统计与阻塞项）
    - `POST /api/projects/{id}/cover`（封面上传，支持 JPG/PNG/WebP，20MB 限制，生成 512px 缩略图）
    - `DELETE /api/projects/{id}/cover`（删除封面）
    - `GET /api/projects/{id}/cover`（读取原图）
    - `GET /api/projects/{id}/cover/thumbnail`（读取缩略图）
  - 新增 `_detect_project_blockers`：检测无章节/无场景页/无素材/无人物阻塞项。
  - 应用版本从 `0.5.0` 升至 `0.5.1`。

### 前端变更

- `design/ui-preview/runtime-api.js`
  - 扩充 `API` 常量：新增 projectArchive/projectRestore/projectCopy/projectOverview/projectPermanent/projectCover/projectCoverThumbnail 路由。
  - 重写 `renderProductionProjects`：实现项目列表页骨架屏、空状态、错误重试、工具栏（搜索/状态筛选/排序/归档切换/回收站）、卡片网格、分页加载更多。
  - 重写 `renderProductionOverview`：实现项目概览页，展示统计、阻塞项和跳转入口。
  - 新增 `projectCard`：卡片显示状态、名称、描述、创建/更新时间，支持封面图显示（有 cover_path 时显示缩略图，加载失败回退首字母占位），悬浮显示「换封面/加封面」按钮。
  - 新增 `projectTrashCard`：回收站卡片显示恢复和永久删除操作。
  - 新增项目操作函数：archiveProject/restoreProject/deleteProject/permanentDeleteProject/copyProject。
  - 新增封面操作函数：openProjectCoverPicker/uploadProjectCover/removeProjectCover，支持文件选择、FormData 上传、错误提示和列表刷新。
  - 注册 upload-project-cover 和 remove-project-cover 事件处理。
- `design/ui-preview/styles.css`
  - `.real-project-cover` 增加 position:relative 和 overflow:hidden。
  - 新增 `.real-project-cover-img`：封面图绝对定位铺满。
  - 新增 `.real-project-cover-upload`：悬浮显示的上传按钮，半透明背景。
- `design/ui-preview/index.html`：静态资源版本号更新为 `v=20260729-projects-cover-v051`。

### 测试

- `backend/tests/test_project_management.py`（新增）：49 个测试用例
  - `ProjectCrudTests`（7 用例）：创建含描述、无描述、空名拒绝、完整字段、不存在 404、改名、改描述。
  - `ProjectSearchFilterSortTests`（6 用例）：搜索、状态筛选、排序、分页、归档默认排除、删除默认排除。
  - `ProjectArchiveTests`（3 用例）：归档、恢复、归档项目默认不出现。
  - `ProjectSoftDeleteTests`（4 用例）：软删除、回收站列表、恢复、永久删除。
  - `ProjectCopyTests`（4 用例）：基础复制、含章节复制、不存在 404、空名 422。
  - `ProjectOverviewTests`（4 用例）：空项目统计、阻塞项、有章节移除阻塞项、不存在 404。
  - `DuplicateProjectNameTests`（2 用例）：同名允许、列表出现两次。
  - `ProjectCoverTests`（13 用例）：PNG/JPEG/WEBP 上传、读取原图、读取缩略图、无封面 404、删除清除、替换封面、不存在项目 404、空文件 422、错误格式 415、删除不存在 404、永久删除清理封面文件。
  - 测试结果：459 passed（原 410 + 新增 49，无回归）。

### 修改文件

- `backend/app/database.py`：v0.5.1 迁移、list_projects 重写、7 个项目方法、set_project_cover_path。
- `backend/app/app_factory.py`：4 个异常处理器、request_id 中间件、14 个项目路由、版本号。
- `design/ui-preview/runtime-api.js`：API 常量、项目列表/概览/卡片/封面函数、事件处理。
- `design/ui-preview/styles.css`：封面图和上传按钮样式。
- `design/ui-preview/index.html`：版本号。
- `backend/tests/test_project_management.py`：新增测试文件。
- `更新日志.md`：新增 v0.5.1 版本记录。

---

## v0.5.0 — 2026-07-29 — 基础约束和迁移能力（阶段 1.1）

分支：`dev-260729-GLM5.2-全功能开发-基础约束`（从 `GLM-MAIN` 拉取新建）

### 变更摘要

按《Atelier 全功能产品与技术开发需求》阶段 1.1「基础约束和迁移能力」完成 7 项基础设施改造：版本化数据库迁移框架、9 张核心表 revision 乐观并发字段、统一 API 错误响应与 request ID 中间件、移除前端环境切换 UI、前端集中 API 客户端、测试隔离基类固化。新增 18 个测试用例，全部 410 项测试通过。

### 后端变更

- `backend/app/database.py`
  - 新增 `schema_migrations` 表，记录迁移版本、描述和应用时间。
  - 新增 `_run_migration` 方法：迁移函数每次 initialize 都执行（必须幂等），版本记录只插入一次。支持手动破坏后的数据修复。
  - 整合 6 个历史迁移到版本化框架：v0.1.7（角色表迁移）、v0.2.0（scene_type 列）、v0.4.0（v0.4.0 表）、v0.4.1（material_pages/mappings 表）、v0.4.1.1（默认素材页）、v0.4.1.2（空 link_id 修复）。
  - 新增 v0.5.0 迁移 `_migrate_add_revision_columns`：为 9 张核心编辑表（projects/chapters/large_scenes/small_scenes/shot_pages/materials/material_pages/characters/character_variants）幂等添加 `revision INTEGER NOT NULL DEFAULT 1` 字段。
  - 9 张核心编辑表的建表语句同步添加 `revision` 字段。
- `backend/app/app_factory.py`
  - 新增 `_STATUS_CODE_TO_ERROR_CODE` 映射和 `_build_error_payload` 辅助函数，构建统一错误响应 `{detail, error: {code, message, details, request_id}}`。
  - 新增 `request_id_middleware`：为每个请求注入唯一 request_id（优先复用客户端 X-Request-ID），响应头返回 X-Request-ID。
  - 新增 4 个全局异常处理器：
    - `StarletteHTTPException` → 统一错误格式，保留 detail 兼容旧测试。
    - `RequestValidationError` → 422 状态码 + error 结构，使用 `jsonable_encoder` 处理不可序列化的 ctx 对象。
    - `DatabaseSafetyError` → 403 状态码。
    - `Exception` → 500 状态码兜底。
  - 应用版本从 `0.4.3` 升到 `0.5.0`。

### 前端变更

- `design/ui-preview/app.js`
  - 移除顶部 `environment-pill` 环境标识。
  - 设置页数据库环境面板重写：移除双数据库切换卡片和切换按钮，改为说明「界面只连接生产数据库」。
  - 数据安全策略卡片更新为「测试隔离」描述。
- `design/ui-preview/runtime-api.js`
  - 新增 `API` 路径常量对象（40+ 路由），新代码应使用 `API.*` 而非硬编码字符串。
  - 增强 `request` 函数错误处理：支持新错误格式 `error.message`，附加 `errorCode` 和 `requestId` 到错误对象，读取响应头 `X-Request-ID`。
  - 移除 `setEnvironmentPill` 函数及其调用。
  - 移除 `activate-database` 事件处理器（含确认弹窗和 POST /api/settings/databases/activate 调用）。
  - 移除离线状态 pill 更新逻辑。
- `design/ui-preview/index.html`：静态资源版本号更新为 `v=20260729-foundation-v050`。

### 测试

- `backend/tests/__init__.py`：新增 `IsolatedTestCase` 基类，自动创建临时目录并锁定 test 环境；文档化测试隔离约定。
- `backend/tests/test_foundation_constraints.py`（新增）：18 个测试用例
  - `SchemaMigrationsTests`（4 用例）：表存在、版本记录、不重复插入、幂等迁移。
  - `RevisionColumnTests`（3 用例）：9 张表有 revision 列、默认值 1、旧数据库迁移。
  - `RequestIdMiddlewareTests`（3 用例）：响应头存在、客户端 ID 保留、生成 UUID hex。
  - `UnifiedErrorResponseTests`（6 用例）：error 结构、404/409/422 错误码、request_id 一致性、数据库安全错误。
  - `IsolatedTestCaseTests`（2 用例）：基类提供 app/client/manager、使用临时目录。
- 测试结果：410 passed（原 392 + 新增 18，无回归）。

### 修改文件

- `backend/app/database.py`：schema_migrations 表、_run_migration、_migrate_add_revision_columns、9 张表建表语句。
- `backend/app/app_factory.py`：request_id 中间件、4 个异常处理器、_build_error_payload、版本号。
- `design/ui-preview/app.js`：移除环境切换 UI。
- `design/ui-preview/runtime-api.js`：API 常量、增强 request、移除环境切换逻辑。
- `design/ui-preview/index.html`：版本号。
- `backend/tests/__init__.py`：IsolatedTestCase 基类。
- `backend/tests/test_foundation_constraints.py`：新增测试文件。
- `更新日志.md`：新增 v0.5.0 版本记录。

---

## v0.4.4 — 2026-07-29 — 小场景画布第三轮验收整改

分支：`dev-260729-GLM5.2-小场景第三轮整改`（从 `GLM-MAIN` 拉取新建）

### 变更摘要

按《Atelier 小场景画布第三轮验收整改需求》修复两个边界问题：分支页面可建立素材页映射、不存在页面 PUT null 错误返回成功。新增 11 个测试用例，全部 392 项测试通过。

### 后端变更

- `backend/app/database.py`
  - `set_small_scene_page_mapping` 方法重写校验顺序：
    - 校验 `material_type` → 查询场景页（含 `branch_id`）→ 校验页面存在 → 校验 `branch_id IS NULL` → 处理 `material_page_id`
    - 删除了 `material_page_id is None` 的提前返回分支，改为在场景页校验通过后再处理取消映射
    - 分支页面（`branch_id IS NOT NULL`）设置或取消映射均抛出 `ValueError("分支页面不能设置素材页映射")`，路由层返回 422
    - 不存在页面设置或取消映射均抛出 `ValueError("场景页不存在")`，路由层返回 404
    - 取消映射在同一事务内完成 DELETE，不再调用独立的 `unset_small_scene_page_mapping`

### 路由层

- `backend/app/app_factory.py`：路由层错误处理逻辑无需修改，现有 `code = 404 if "不存在" in msg else 422` 已正确区分 404 和 422

### 测试

- `backend/tests/test_third_round_rectification.py`（新增）：11 个测试用例
  - 分支页面设置映射 → 422
  - 分支页面取消映射 → 422
  - 不存在页面设置映射 → 404
  - 不存在页面取消映射 → 404
  - 正常页面幂等取消 → 200, mapping: null
  - 已关联素材可以映射
  - 未关联素材返回 422
  - 同类型映射原子替换
  - 一个素材页映射到多个场景页
  - 正常页面设置后取消
  - 四次失败请求后原有映射保持不变

### 生产数据库历史异常映射检查

- 只读查询 `SELECT COUNT(*) FROM small_scene_page_mappings m JOIN shot_pages p ON p.id = m.scene_page_id WHERE p.branch_id IS NOT NULL`
- 结果：`invalid_mapping_count=0`（生产数据库无历史异常映射）

### 修改文件

- `backend/app/database.py`：修复 `set_small_scene_page_mapping` 方法
- `backend/tests/test_third_round_rectification.py`：新增测试文件
- `小场景画布第二轮验收整改需求.md`：纠正 19.5 节状态为"待重新验收"
- `更新日志.md`：新增 v0.4.4 版本记录

### 测试结果

- 前端语法校验：`node --check` 通过
- 后端全量测试：392 passed（原 381 + 新增 11，无回归）

### 仍未完成

状态：待重新验收。浏览器最小回归和 API 负向验收需用户手动执行或确认。

---

## v0.4.3 — 2026-07-29 — 小场景画布第二轮验收整改

分支：`dev-260729-GLM5.2-小场景第二轮整改`（从 `GLM-MAIN` 拉取新建）

### 变更摘要

按《Atelier 小场景画布第二轮验收整改需求》修复 10 个失败项：story-tree 缺字段、未关联素材页可直接映射、移除关联后映射残留、孤立映射、排序非单事务、状态码与返回结构不符、PUT null 不支持、旧接口产生无 link_id 关联。新增 35 个测试用例，全部 381 项测试通过。

### 后端变更

- `backend/app/database.py`
  - `set_small_scene_materials`：重写为差量更新，保留原 `id`（link_id），新增生成 UUID，移除时级联删除该小场景内映射，单事务完成。
  - 新增 `reorder_scene_pages`：单事务排序，完整集合校验（缺失/重复/跨小场景/分支页面/不存在/空数组全部返回 422），失败时原顺序保留，成功后 sort_order 从 1 连续递增。
  - `remove_small_scene_resource_link`：返回 `deleted_mapping_count` 字段，记录级联删除的映射数量。
  - `set_small_scene_page_mapping`：`material_page_id=None` 时调用 `unset_small_scene_page_mapping` 并返回 `None`（API 层据此返回 `mapping: null`）。
  - `get_story_tree`：为每个 resource 添加 `page_count` 字段。
  - 新增 `_migrate_fix_empty_link_ids`：幂等修复 `small_scene_materials.id` 为 NULL 或空字符串的历史数据；重复初始化不修改已有有效 ID。
- `backend/app/app_factory.py`
  - `SetMappingRequest.material_page_id` 改为 `str | None`，允许 `null`。
  - 创建场景页 `POST /api/small-scenes/{id}/pages`：状态码 200→201，返回结构改为 `{"page": {...}}`。
  - 修改场景页 `PATCH /api/small-scene-pages/{id}`：返回结构改为 `{"page": {...}}`。
  - 关联素材 `POST /api/small-scenes/{id}/resources`：状态码 200→201，返回结构改为 `{"resource": {...}}`，包含 `link_id/material_id/name/material_type/pages`；重复关联返回 409。
  - 移除关联 `DELETE /api/small-scene-resource-links/{id}`：返回结构改为 `{"deleted": {link_id, small_scene_id, material_id, deleted_mapping_count}}`。
  - 设置映射 `PUT /api/small-scene-pages/{id}/mappings/{type}`：返回结构改为 `{"mapping": {...}}`；PUT null 取消返回 `{"mapping": null}`。
  - 取消映射 `DELETE /api/small-scene-pages/{id}/mappings/{type}`：返回结构改为 `{"mapping": {...}}`。
  - 排序 `PUT /api/small-scenes/{id}/pages/order`：改用 `manager.reorder_scene_pages` 单事务方法，不再循环调用 `move_shot_page`；所有页面 ID 校验失败返回 422。
  - 应用版本从 `0.4.2` 升到 `0.4.3`。

### 前端变更

- `design/ui-preview/runtime-api.js`
  - 取消映射从 `DELETE` 改为 `PUT + null`，符合第二轮契约 8.5。
- `design/ui-preview/index.html`：静态资源版本号更新为 `v=20260729-scene-canvas-v043`。

### 测试

- 新增 `backend/tests/test_second_round_rectification.py`：35 个测试用例，覆盖：
  - 11.1 story-tree：`page_count`/`resource_count`/`resources` 含 `pages` 和 `page_count`、排序、空数组。
  - 11.2 映射校验：未关联 422、跨小场景 422、类型不一致 422、同类型替换、一页多映射、PUT null 取消。
  - 11.3 移除关联：级联删除映射、`deleted_mapping_count`、不删素材/素材页、不删其他小场景映射、无孤立映射。
  - 11.4 排序：完整集合成功、缺失/重复/跨场景/分支/不存在 422、失败后原顺序保留、成功后连续编号。
  - 11.5 状态码与响应契约：201+page、200+page、201+resource、409 重复、200+mapping、null mapping。
  - 11.6 link_id：新接口非空、workspace 非空、旧批量接口保留 id、新素材生成新 id、历史空 id 修复、重复初始化不变。
- 修改 `backend/tests/test_scene_pages_api.py` 和 `backend/tests/test_scene_page_mappings_api.py`：适配新的包裹返回结构。
- 测试结果：381 passed（原 346 + 新增 35，无回归）。

### 修改文件

- `backend/app/database.py`
- `backend/app/app_factory.py`
- `design/ui-preview/runtime-api.js`
- `design/ui-preview/index.html`
- `backend/tests/test_scene_pages_api.py`
- `backend/tests/test_scene_page_mappings_api.py`
- `backend/tests/test_second_round_rectification.py`（新增）
- `更新日志.md`

### 仍未完成

无。全部验收项已通过：
- 381 项自动化测试全部通过。
- API 端到端 12 步主流程 + 5 项负向测试全部通过。
- 浏览器 UI 验证 8 项全部通过，控制台无错误/警告。
- 测试数据已清理，生产数据库无残留。

---

## v0.4.2 — 2026-07-29 — 小场景画布前后端联调验收整改

分支：`dev-260729-GLM5.2-小场景联调整改`（从 `GLM-MAIN` 拉取新建）

### 变更摘要

按《小场景画布前后端联调验收整改需求》完成前后端联调整改，解决 v0.4.0 后端已交付但前端无法使用的 7 个阻断问题。新增 15 个 API 路由、2 张数据库表、6 个测试文件（51 个用例），实现前后端真实闭环。

### 数据库整改

- 新增 `material_pages` 表：素材页，`UNIQUE(material_id, name)`，支持一素材多页
- 新增 `small_scene_page_mappings` 表：场景页↔素材页映射，`UNIQUE(scene_page_id, material_type)` 实现单类型唯一
- 改造 `small_scene_materials` 表：新增 `id` 列（UUID）作为稳定 `link_id`
- 新增迁移方法 `_migrate_v041_tables()` 和 `_migrate_default_material_pages()`
- `create_material` 增强：新建素材时自动生成 1 个默认素材页

### 新增 API 路由（15 个）

| 路由 | 方法 | 说明 |
|---|---|---|
| `/api/projects/{project_id}/story-tree` | GET | 项目剧本树聚合（4 层批量查询） |
| `/api/small-scenes/{id}/workspace` | GET | 小场景工作区聚合 |
| `/api/small-scenes/{id}/pages` | POST | 创建场景页（name↔title 适配） |
| `/api/small-scene-pages/{id}` | PATCH/DELETE | 更新/删除场景页 |
| `/api/small-scenes/{id}/pages/order` | PUT | 场景页排序 |
| `/api/small-scenes/{id}/resources` | POST | 关联素材 |
| `/api/small-scene-resource-links/{link_id}` | DELETE | 移除关联 |
| `/api/small-scene-pages/{id}/mappings/{type}` | PUT/DELETE | 设置/取消映射 |
| `/api/materials/{id}/pages` | GET/POST | 素材页列表/创建 |
| `/api/material-pages/{id}` | GET/PATCH/DELETE | 素材页读改删 |
| `/api/materials/{id}/pages/order` | PUT | 素材页排序 |

### 前端适配

- `smallSceneBackendAvailable` 初始值从 `false` 改为 `true`
- workspace 返回 pages 使用 `name` 字段（内部 `title` 转换）
- workspace 的 resources 包含 `pages` 数组（素材页只读显示）
- workspace 包含 `chapter` 和 `large_scene` 字段（面包屑导航）
- 取消映射从 `PUT + null` 改为 `DELETE`，符合需求书 6.5 契约
- `index.html` 版本号更新为 `v=20260729-scene-canvas-v041`

### 测试

- 新增 6 个测试文件，51 个用例：
  - `test_story_tree_contract.py`（7 用例）
  - `test_small_scene_workspace_contract.py`（7 用例）
  - `test_scene_pages_api.py`（10 用例）
  - `test_material_pages_api.py`（10 用例）
  - `test_scene_page_mappings_api.py`（8 用例）
  - `test_frontend_route_contract.py`（9 用例，契约测试）
- 测试结果：346 passed（原 295 + 新增 51，无回归）

### 修改文件

- `backend/app/database.py`：新增表、迁移方法、11 个 DatabaseManager 方法
- `backend/app/app_factory.py`：8 个请求模型、15 个新路由、版本号 0.4.0→0.4.2
- `design/ui-preview/runtime-api.js`：前端适配
- `design/ui-preview/index.html`：版本号
- `小场景画布前后端联调验收整改需求.md`：追加第 17-19 章实际解决方案和测试结果
- `backend/tests/test_*.py`：6 个新测试文件

---

## v0.4.1 — 2026-07-29 — 清理需求书与更新日志遗留合并冲突

分支：`fix-260729-GLM5.2-需求书清理冲突`（从 `GLM-MAIN` 拉取新建）

### 变更摘要

清理 `dev-280728-GLM-小场景画布与素材页映射` 合并到 `GLM-MAIN` 时遗留的未解决 git 合并冲突标记，保留与当前 v0.4.0 已交付代码一致的 dev 版本内容。

### 修改文件

- `需求归档/小场景画布与素材页映射后端开发需求书.md`：删除 4 处冲突区块中的 HEAD 版本（v1.0 简化模型：场景页+素材页映射），保留 dev 版本（v0.4.0 已交付：分镜页+条件分支+提示词）。清理后 1146 行，无冲突标记残留。
- `更新日志.md`：清理 1 处冲突，保留 v0.3.0 和 v0.4.0 两个版本的开发记录，按版本号降序排列（v0.4.0 在前，v0.3.0 在后）。

### 验证

- Grep 正则 `^(<<<<<<<|=======|>>>>>>>)` 全项目扫描：无匹配，验证通过。

---

## v0.4.0 — 2026-07-28 — 小场景画布与素材页映射后端

分支：`dev-280728-GLM-小场景画布与素材页映射`（从 `main` 拉取新建）

### 变更摘要

1. **新增素材库（Materials）**：全局素材 CRUD，支持 6 种类型（composition/expression/scene/lighting/prompt/composite_template）、标签、验证状态、预览图。
2. **新增小场景（Small Scenes）**：大场景下的小场景 CRUD 和排序，支持 content/transition 类型，同大场景内名称唯一。
3. **新增分镜页（Shot Pages）**：小场景下的分镜页 CRUD 和排序，含标题、描述、正向提示词、负向提示词，支持主线和分支两种归属。
4. **新增条件分支（Branches）**：多态父级（大场景或小场景）的分支 CRUD，支持启用/停用，分支下可创建独立分镜页。
5. **新增素材关联**：小场景级素材关联（默认素材）和分镜页级素材关联（具体引用），完整替换模式，支持排序。
6. **级联删除**：删除大场景级联删除小场景、分支、分镜页和素材关联；删除小场景级联删除分支、分镜页和素材关联；删除分支级联删除分镜页和素材关联；删除素材级联清除所有引用。
7. **数据库迁移**：新增 7 张表（materials、material_tags、small_scenes、branches、shot_pages、small_scene_materials、shot_page_materials），安全迁移不覆盖现有数据。
8. **版本号**：从 0.2.1 升级到 0.4.0。

### 新增 API 接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/materials` | GET | 素材列表 |
| `/api/materials` | POST | 创建素材 |
| `/api/materials/{id}` | GET | 素材详情 |
| `/api/materials/{id}` | PATCH | 更新素材 |
| `/api/materials/{id}` | DELETE | 删除素材 |
| `/api/large-scenes/{id}/small-scenes` | GET | 小场景列表 |
| `/api/large-scenes/{id}/small-scenes` | POST | 创建小场景 |
| `/api/small-scenes/{id}` | GET | 小场景详情 |
| `/api/small-scenes/{id}` | PATCH | 更新小场景 |
| `/api/small-scenes/{id}/move` | POST | 移动小场景排序 |
| `/api/small-scenes/{id}` | DELETE | 删除小场景 |
| `/api/small-scenes/{id}/shot-pages` | GET | 分镜页列表 |
| `/api/small-scenes/{id}/shot-pages` | POST | 创建分镜页 |
| `/api/shot-pages/{id}` | GET | 分镜页详情 |
| `/api/shot-pages/{id}` | PATCH | 更新分镜页 |
| `/api/shot-pages/{id}/move` | POST | 移动分镜页排序 |
| `/api/shot-pages/{id}` | DELETE | 删除分镜页 |
| `/api/{parent_type}/{id}/branches` | GET | 分支列表 |
| `/api/{parent_type}/{id}/branches` | POST | 创建分支 |
| `/api/branches/{id}` | GET | 分支详情 |
| `/api/branches/{id}` | PATCH | 更新分支 |
| `/api/branches/{id}` | DELETE | 删除分支 |
| `/api/small-scenes/{id}/materials` | GET | 小场景素材列表 |
| `/api/small-scenes/{id}/materials` | PUT | 设置小场景素材 |
| `/api/shot-pages/{id}/materials` | GET | 分镜页素材列表 |
| `/api/shot-pages/{id}/materials` | PUT | 设置分镜页素材 |

### 修改文件

- `backend/app/database.py`：新增 7 张表、迁移方法、素材/小场景/分镜页/分支/素材关联的 DatabaseManager 方法。
- `backend/app/app_factory.py`：新增 Pydantic 请求模型、26 个 API 路由，版本号升级到 0.4.0。
- `backend/tests/test_small_scenes.py`：新增 35 个小场景测试用例。
- `backend/tests/test_shot_pages.py`：新增 38 个分镜页测试用例。
- `backend/tests/test_branches.py`：新增 31 个分支测试用例。
- `backend/tests/test_scene_materials.py`：新增 25 个素材关联和级联删除测试用例。

### 测试

- 全量 239 个测试通过，含 129 个新增测试和 110 个原有回归测试。

---

## v0.3.0 — 2026-07-28 — 素材库后端开发与前端对接

分支：`dev-260728-GLM5.2-materials-backend`（从 `GLM-MAIN` 拉取新建）

### 变更摘要

实现素材库后端开发与前端对接需求书定义的全部后端能力，与已完成的前端素材库页面（`?page=materials`、`?page=material-detail`）对接，形成全局素材的基础管理闭环：创建、列表查询、搜索筛选、详情、局部更新、删除、标签管理与预览图上传/读取/替换/删除。

### 后端变更

- `backend/app/database.py`
  - 新增 `materials` 表：`id` / `name`(NOCASE) / `material_type` / `description` / `content` / `prompt_text` / `negative_prompt` / `validation_status` / `notes` / `preview_original_path` / `preview_thumbnail_path` / `created_at` / `updated_at`，含 `UNIQUE(material_type, name)` 同类型内名称唯一约束、`material_type` 与 `validation_status` CHECK 约束、三个索引（类型+更新时间、状态+更新时间、名称）。
  - 新增 `material_tags` 表：`id` / `name`(NOCASE UNIQUE) / `created_at`。
  - 新增 `material_tag_links` 关联表：`material_id` + `tag_id` 复合主键，双向 `ON DELETE CASCADE`，`tag_id, material_id` 索引。
  - 新增 `DatabaseManager` 方法：
    - `list_materials(query, material_type, validation_status, tag, limit, offset, sort, environment)`：SQL 层完成搜索（name/description/content/tag 四字段 LIKE，转义 `%`/`_`/`\`）、类型/状态/标签精确筛选、四种排序、分页，一次 JOIN 聚合返回标签列表避免 N+1。
    - `get_material(material_id)`：返回完整字段 + 标签列表 + `preview_url`/`thumbnail_url`。
    - `create_material(...)`：单事务创建素材 + 标签关联，忽略大小写去重。
    - `update_material(material_id, **updates)`：区分字段未提交与明确清空，`tags` 为完整替换，单事务。
    - `delete_material(material_id)`：级联清理标签关联与 `data/materials/{material_id}` 图片目录，路径钳制防止越权删除。
    - `list_material_tags(query, limit)`：按引用数降序、名称升序，服务端上限 100。
    - `set_material_preview_paths(material_id, original_path, thumbnail_path)`：更新预览图路径字段。
- `backend/app/app_factory.py`
  - 新增 `CreateMaterialRequest` / `UpdateMaterialRequest` Pydantic 模型：字段级校验（名称/正文非空、标签数 ≤30、单标签 ≤40 字、类型与状态枚举、LoRA 权重范围），`tags_clean` 清洗空白并忽略大小写去重，`UpdateMaterialRequest` 用 `model_validator` 保证至少一个字段。
  - 新增 9 个路由：
    - `GET /api/materials`：列表查询，参数 `q`/`material_type`/`validation_status`/`tag`/`limit`/`offset`/`sort`，列表项不含 `content`/`prompt_text`/`negative_prompt`/`notes`/`preview_url`。
    - `POST /api/materials`：新建素材，成功 201。
    - `GET /api/materials/{material_id}`：详情，不存在 404。
    - `PATCH /api/materials/{material_id}`：局部更新，至少一个字段否则 422，重名 409。
    - `DELETE /api/materials/{material_id}`：删除素材并清理图片目录。
    - `GET /api/material-tags`：标签候选，按引用数降序、名称升序。
    - `POST /api/materials/{material_id}/preview`：上传预览图，JPG/PNG/WebP，20MB 上限，Pillow 按内容解码校验，最长边 16384 像素上限，生成 512px WebP 缩略图（质量 82），先写临时文件再原子替换。
    - `DELETE /api/materials/{material_id}/preview`：删除预览图文件并清空路径。
    - `GET /api/materials/{material_id}/preview` / `GET /api/materials/{material_id}/thumbnail`：FileResponse 返回图片，显式映射扩展名到 MIME 类型，路径穿越校验，不存在 404。
  - 应用版本从 `0.2.0` 升到 `0.3.0`。
- `requirements.txt`：新增 `Pillow==12.3.0`、`python-multipart==0.0.32`。

### 测试

- `backend/tests/test_materials.py`（新增）：53 项测试用例，覆盖：
  - **CRUD**：空列表、创建六种素材、名称与标签空白清理、空名称/空正文/超长字段拒绝、非法类型与状态拒绝、同类型重名 409、不同类型允许同名、详情查询、局部更新、明确清空可选字段、删除与 404。
  - **列表**：搜索名称/简介/正文/标签、类型/状态/标签组合筛选、四种排序、`limit`/`offset`/`has_more`、列表不返回大文本字段、列表无 N+1 标签查询。
  - **标签**：创建时去重、忽略大小写去重、更新时完整替换、删除素材级联删除关联、候选限量和搜索。
  - **图片**：上传 JPG/PNG/WebP、生成缩略图、损坏图片/伪造扩展名/超大图片拒绝、替换失败保留旧图、删除预览不删除素材、删除素材清理自己的图片目录、路径穿越无法读取数据目录外文件。
  - **数据安全**：测试使用 `create_app(data_root=临时目录, environment="test")`，不导入 `backend.app.main.app`，现有全部测试继续通过。

### 验证

- 单元测试：163 项全部通过（含新增 53 项素材库测试）。
- 前端语法校验：`node --check` 通过（`runtime-api.js`、`app.js`）。
- 浏览器端到端：素材库前端在上一版本已完成，本次后端对接后前端可直接工作，待用户手动验收。

### 涉及文件

- `backend/app/database.py`
- `backend/app/app_factory.py`
- `backend/tests/test_materials.py`（新增）
- `requirements.txt`
- `design/ui-preview/index.html`：静态资源版本号更新。

---

## v0.2.1 — 2026-07-28 — 移除大场景拖动把手视觉元素

分支：`fix-260728-GLM5.2-remove-drag-handle`（从 `main` 拉取新建）

### 变更摘要

1. **移除拖动把手 DOM**：`largeSceneBlock()` 中删除 `.large-scene-drag-handle` 子元素，拖动把手视觉元素不再渲染。
2. **保留拖动功能**：`draggable="true"` 与 `aria-label` 上移到 `.large-scene-block` 根元素，整张卡片均可触发拖动，拖动逻辑不变。
3. **清理 CSS**：删除 `.large-scene-drag-handle`、`:hover`、`:active` 三段样式规则。
4. **版本号更新**：静态资源版本号更新为 `v=20260728-remove-drag-handle`。

### 修改文件

- `design/ui-preview/runtime-api.js`：移除 `.large-scene-drag-handle` div，`aria-label` 上移至根元素。
- `design/ui-preview/styles.css`：删除 `.large-scene-drag-handle` 相关样式。
- `design/ui-preview/index.html`：静态资源版本号更新。

### 测试

- `node --check` 前端语法校验通过。

## v0.2.0 — 2026-07-28 — 大场景组织功能（类型管理 + 跨章节移动 + 拖动排序）

分支：`dev-260728-GLM5.2-large-scene-organize`（从 `GLM-MAIN` 拉取新建）

### 变更摘要

1. **大场景类型字段**：`large_scenes` 表新增 `scene_type` 字段（`content` 内容段 / `transition` 过渡段），旧库通过 PRAGMA 自动迁移，已有数据默认为 `content`。
2. **跨章节移动接口**：新增 `POST /api/large-scenes/{id}/move`，支持同章节排序与跨章节移动，单事务内重新编号源/目标章节，校验同项目约束与同名冲突，越界位置自动钳制到首尾。
3. **编辑接口扩展**：`PATCH /api/large-scenes/{id}` 从仅改名升级为名称/类型/所属章节三字段可选更新，跨章节移动时自动追加到目标章节末尾并重排。
4. **删除后重排**：删除大场景后立即将剩余场景重新编号为 1..N，保持 sort_order 连续。
5. **单场景查询**：新增 `GET /api/large-scenes/{id}` 接口，供前端编辑弹窗加载详情。
6. **前端类型徽标**：大场景卡片显示"内容段"（蓝色突出）或"过渡段"（灰色低调）徽标。
7. **新建弹窗加类型选择**：新建大场景弹窗增加类型下拉选择。
8. **编辑弹窗（三字段）**：右键大场景→改名，弹出"编辑大场景"弹窗，包含名称、类型、所属章节三个字段，可同时修改。
9. **拖动排序**：卡片顶部 6px 横纹把手支持拖动，同章节横向排序与跨章节移动，拖动时显示虚线占位指示器，失败自动恢复并提示错误。
10. **可访问性**：拖动把手带 `aria-label`，键盘用户可通过编辑弹窗完成跨章节移动。

### 后端变更

- `backend/app/database.py`
  - `large_scenes` 建表语句新增 `scene_type TEXT NOT NULL DEFAULT 'content'`。
  - 新增 `_migrate_large_scenes_scene_type()` 方法，用 PRAGMA table_info 检测列是否存在，不存在则 ALTER TABLE ADD COLUMN。
  - `list_large_scenes` / `get_large_scene` / `create_large_scene` / `delete_large_scene` 的 SELECT/INSERT 语句适配 scene_type。
  - 新增 `update_large_scene()` 方法（名称/类型/章节三字段可选，跨章节移动时追加到末尾并重排）。
  - `rename_large_scene()` 保留为向后兼容包装，内部调用 `update_large_scene(name=...)`。
  - 新增 `move_large_scene()` 方法（单事务，同/跨章节重排，越界钳制，同名/跨项目拒绝，返回 source/target items）。
  - `delete_large_scene()` 删除后重排剩余场景为 1..N。

- `backend/app/app_factory.py`
  - `CreateLargeSceneRequest` 新增 `scene_type: Literal["content", "transition"]`。
  - 新增 `UpdateLargeSceneRequest`（三字段可选，model_validator 校验至少一个）。
  - 新增 `MoveLargeSceneRequest`（target_chapter_id + target_sort_order，ge=1）。
  - `PATCH /api/large-scenes/{id}` 路由改为 `update_large_scene`，404/409 区分返回。
  - 新增 `POST /api/large-scenes/{id}/move` 路由。
  - 新增 `GET /api/large-scenes/{id}` 路由。
  - 应用版本从 `0.1.7` 升到 `0.2.0`。

### 前端变更

- `design/ui-preview/runtime-api.js`
  - `largeSceneBlock()`：卡片新增 `scene-type-{type}` 类、`data-scene-type` 属性、`draggable="true"`、顶部 `.large-scene-drag-handle` 把手（aria-label）、`.large-scene-type-badge` 徽标。
  - `chapterBlock()`：`.large-scene-lane` / `.large-scene-track` / `.large-scene-empty` 添加 `data-drop-zone` 与 `data-chapter-id`。
  - `ensureLargeSceneModal()` / `openLargeSceneModal()` / `submitLargeScene()`：新建弹窗增加类型 select，请求 body 带 `scene_type`。
  - 新增 `openLargeSceneEditModal()` / `closeLargeSceneEditModal()` / `submitLargeSceneEdit()`：编辑弹窗加载场景详情 + 章节列表，三字段可改，仅当章节变化时传 chapter_id。
  - 右键菜单 "改名" 对 large-scene 重定向到 `openLargeSceneEditModal`。
  - 新增 `initLargeSceneDrag()`：dragstart/dragover/drop 事件，计算插入位置，虚线占位指示器，调用 /move 接口，失败恢复 + toast 提示。
  - 新增 `clearDropIndicators()` 清理占位元素。
  - ESC 键监听增加 `closeLargeSceneEditModal()`。
  - click 监听增加 `close-large-scene-edit-modal` action。

- `design/ui-preview/styles.css`
  - 新增 `.large-scene-type-badge` 样式（内容段蓝色 rgba(111,126,213,0.18)/#5463c4，过渡段灰色 rgba(144,152,168,0.18)/#6b7280）。
  - 新增 `.large-scene-drag-handle` 顶部横纹样式（6px 高，点阵渐变，grab 光标，hover 蓝色加深）。
  - 新增 `.large-scene-block.dragging` 拖动中半透明状态。
  - 新增 `.large-scene-lane[data-drop-zone]` 拖放区域过渡。
  - 新增 `.large-scene-drop-indicator` 虚线占位框（168px 宽，蓝色虚线边框）。

- `design/ui-preview/index.html`：静态资源版本号更新为 `v=20260728-large-scene-organize`。

### 测试

- `backend/tests/test_large_scenes.py` 新增 `LargeSceneOrganizeApiTests` 测试类，共 22 项测试用例：
  - scene_type 字段：默认值、显式 transition、非法值 422、list 返回、旧库迁移
  - PATCH 三字段：仅改名/仅改类型/跨章节移动到末尾/缺失章节 404/同名冲突 409/至少一个字段 422
  - /move 接口：同章节前移/后移、跨章节到空章节/特定位置、越界钳制、跨项目拒绝、缺失目标 404、缺失场景 404、同名回滚
  - 删除后重排、双数据库隔离

### 验收

- 单元测试：106 项全部通过（含新增 22 项）。
- 前端语法校验：`node --check` 通过。
- 浏览器端到端：类型徽标颜色区分正确，新建弹窗类型选择正常工作。编辑弹窗与拖动交互因浏览器自动化限制未完成全部验收，代码逻辑已通过单元测试覆盖。

## v0.1.9 — 2026-07-28 — 角色查询性能优化 + 懒加载分页

分支：`dev-260728-GLM5.2-character-db-lazy-load`（从 `main` 拉取新建）

### 变更摘要

1. 角色查询后端从「Python 全量扫描 24.4 万行 CSV」改为「SQLite + FTS5 全文索引」，查询从秒级降到毫秒级（8ms）。
2. 首次启动时 CSV 导入 SQLite 在后台 daemon 线程执行，批量 INSERT + sleep 释放 GIL，不阻塞 FastAPI 请求循环，不再导致鼠标掉帧。
3. 导入完成后写入 csv_mtime 标记，后续启动直接复用 SQLite，毫秒级进入 ready 状态。
4. 导入过程写到临时文件，完成后原子 rename，避免中途退出留下损坏的索引。
5. 前端改为 IntersectionObserver 滚动懒加载：滚动到表格底部自动加载下一页并追加，移除"上一页/下一页"分页按钮。
6. 表格容器加 max-height: 70vh + overflow-y: auto，表格内部滚动，表头 sticky 固定。
7. 新增 `/api/character-database/status` 端点，前端首次进入页面时轮询加载状态，导入完成后自动加载数据。

### 问题描述

- 现象：第一次进入角色查询页面时整机卡顿、鼠标掉帧持续 3-5 秒。
- 根因：后端 `_load()` 同步读取 37.2MB / 24.4 万行 CSV 构造 dict 列表，Python 进程 CPU 跑满一个核心。单机环境下浏览器与后端争抢 CPU，导致鼠标掉帧。查询时每次请求都对全量数据做 Python for 循环 + 多次 .lower() + sort。
- 表格无滚动条：`.character-database-results` 只有 `overflow-x: auto`，没有 `max-height + overflow-y`，表格撑开整个 page-scroll。

### 后端变更

- `backend/app/character_database.py`（完全重写）
  - 独立 SQLite 文件 `data/character-database/danbooru_character.sqlite3`。
  - 表结构：`characters` 表（id, character, copyright, trigger, core_tags, count, solo_count, url）+ `characters_fts` 虚拟表（FTS5，tokenize=unicode61 separators '_-.'）+ `db_meta` 表（存 csv_mtime）。
  - 索引：idx_characters_count, idx_characters_character, idx_characters_copyright。
  - 导入流程：后台 daemon 线程，批量 INSERT（每 2000 行 commit），批次间 sleep(0.005) 释放 GIL；写到 .sqlite3.tmp 临时文件，完成后 rename 覆盖。
  - 复用策略：启动时检查 SQLite 是否存在且 db_meta.csv_mtime 与 CSV mtime 一致，一致则跳过导入直接 ready。
  - 查询：FTS5 MATCH 前缀匹配（下划线/连字符当分隔符），SQL ORDER BY + LIMIT/OFFSET 分页。
  - 状态管理：`pending` / `loading` / `ready` / `error`，对外暴露 `status()` 函数。
  - `CharacterDatabaseNotReadyError` 异常，查询接口在非 ready 状态抛出。
- `backend/app/app_factory.py`
  - 新增 `/api/character-database/status` 端点。
  - search / copyrights 接口 catch `CharacterDatabaseNotReadyError` 返回 503。
  - `create_app` 末尾调用 `character_database.status()` 触发后台导入预热。
  - sort 参数默认值从 `"count"` 改为 `"count_desc"`，支持四种排序。

### 前端变更

- `design/ui-preview/runtime-api.js`
  - 移除 `renderCharacterDatabasePagination()` / `bindCharacterDatabasePagination()`。
  - `renderCharacterDatabaseTable()` 改为 `renderCharacterDatabaseRow()`，只返回单个 `<tr>`，table 结构在初始化时创建一次。
  - 新增 `setupCharacterDatabaseScrollObserver()`：IntersectionObserver 监听底部哨兵元素，进入视口时自动 page++ 并 append 加载。
  - `loadCharacterDatabaseResults(append)`：append=false 重置表格从第1页开始；append=true 追加行到 tbody。
  - 新增 `showCharacterDatabaseLoading(progress)` / `pollCharacterDatabaseStatus()`：首次进入页面时检查后端 status，loading 时显示进度并每 2 秒轮询，ready 后自动加载数据。
  - 防重复请求：`isLoading` 标志位。
  - `hasMore` 标志位：已加载数 >= total 或返回空时停止加载。
- `design/ui-preview/app.js`：移除 `character-database-pagination` div。
- `design/ui-preview/styles.css`
  - `.character-database-results`：`max-height: 70vh; overflow: auto; border + border-radius`。
  - `.character-database-table thead`：`position: sticky; top: 0` 表头固定。
  - 新增 `.character-database-sentinel` / `.character-database-loading-more` / `.character-database-end` / `.character-database-loading-error` 样式。
  - 移除 `.character-database-pagination` / `.character-database-page-info` 样式。
- `design/ui-preview/index.html`：静态资源版本号更新为 `20260728-character-db-lazy`。
- `.gitignore`：新增 `data/character-database/*.sqlite3*` 和 `.sqlite3.tmp` 忽略规则。

### 性能数据

| 指标 | v0.1.7 | v0.1.9 |
|---|---|---|
| 首次进入页面 | 阻塞 3-5 秒，鼠标掉帧 | 后台导入，前端显示进度，不阻塞其他页面 |
| 搜索查询 | Python 全量扫描 + sort，500-1000ms | SQLite FTS5 + 索引，8ms |
| 后续启动 | 每次重新读 CSV | 直接复用 SQLite，毫秒级 ready |
| 分页方式 | 上一页/下一页按钮 | 滚动到底部自动加载 |
| 表格滚动 | 无滚动条，撑开页面 | max-height 70vh，表头 sticky |

### 验证

- 83 个单元测试全部通过。
- 后端 API：`/api/character-database/status` 返回 ready；`/api/character-database/search?q=hatsune` 返回 61 个结果。
- 前端浏览器验证：表格正常显示，垂直滚动条 OK，表头 sticky OK，滚动到底部自动加载更多 OK，搜索"hatsune"后重置为 61 个结果 OK。

## v0.1.8 — 2026-07-28 — 修复启动脚本端口占用识别失败

分支：`fix-260728-GLM5.2-start-script-atelier-detect`（从 `main` 拉取新建）

### 变更摘要

修复 `start.bat` / `start-test.bat` 在端口被本应用旧实例占用时无法识别、误判为"别的应用"导致启动中止的问题。

### 问题描述

- 现象：再次启动 Atelier 时，脚本提示 `Port 8110 is used by another application` 并退出，不杀旧进程。
- 根因：`scripts/start_atelier.ps1` 通过命令行同时匹配 `--atelier-server` 和项目根路径字符串来判断是否本应用。但 Windows 不同启动器传入的项目路径大小写不一致（`Resolve-Path` 返回 `C:\...`，Codex runtime 启动时传入 `c:\...`），`Contains` 大小写敏感导致匹配失败。
- 影响：任何由非项目 venv 启动器（如 Codex runtime）启动的 Atelier 实例都会被误判为外部应用，脚本拒绝自动重启。

### 修复方案

- `scripts/start_atelier.ps1`：判断本应用占用的条件从「`--atelier-server` 且包含项目根路径」简化为「仅 `--atelier-server`」。
- `--atelier-server` 是 Atelier 独有参数，足以识别本应用；不再依赖路径字符串比对，规避大小写差异问题。

### 验证

- 第一次启动：正常监听 8110。
- 第二次启动（端口被本应用占用）：脚本正确识别为 Atelier 旧实例，自动 taskkill 后重启，新进程接管 8110。

### 涉及文件

- `scripts/start_atelier.ps1`：端口占用判断逻辑修改（第 46-48 行）。

## v0.1.7 — 2026-07-28 — 角色全局化 + 角色查询工具

分支：`dev-260728-GLM5.2-global-characters`（从 `main` 拉取新建）

### 变更摘要

1. 角色从项目级改为全局共享：任何项目都可以使用同一份角色库，删除项目不影响角色。
2. 项目规格（project_specs）改为全局规格（specs）：所有项目共用同一套规格定义。
3. 规格值全局唯一：同一角色同一变体的规格值不再按项目区分。
4. 侧边栏调整：人物库从项目内移到"全局资源"分类下；新增"工具"分类，下面有"角色查询"入口。
5. 新增角色查询工具：从 comfyui-manager 移植 Danbooru 角色库 CSV（约 24.4 万角色），支持模糊搜索、作品系列筛选、排序、分页。

### 数据库结构变更

**变更前（v0.1.6）：**
- `characters` 表有 `project_id` 字段（项目级）
- `project_specs` 表有 `project_id` 字段（项目级）
- `character_spec_values` 表的 `project_spec_id` 指向 `project_specs.id`

**变更后（v0.1.7）：**
- `characters` 表移除 `project_id`，改为全局（`UNIQUE(name)` 全局唯一）
- 新增 `project_characters` 关联表（`project_id` + `character_id`，记录哪些项目使用了哪些角色）
- `project_specs` 表改名为 `specs`，移除 `project_id`，改为全局（`UNIQUE(spec_type, custom_label)` 全局唯一）
- `character_spec_values` 表的 `project_spec_id` 改名为 `spec_id`，指向 `specs.id`

**迁移策略：**
- 自动检测旧表结构（characters 表有 project_id 列时触发迁移）
- 备份旧数据 → DROP 所有表 → 用新结构重建 → 恢复数据
- 角色按名称去重（同名角色保留最早创建的）
- 规格按 spec_type + custom_label 去重
- 迁移后自动创建 project_characters 关联（从旧 characters.project_id 恢复）

### 后端变更

- `backend/app/database.py`
  - 表结构重构：`characters` 移除 project_id，新增 `project_characters` 关联表，`project_specs` 改名 `specs` 移除 project_id，`character_spec_values.project_spec_id` 改名 `spec_id`。
  - 新增 `_migrate_legacy_character_schema()` 迁移方法，自动检测旧表并迁移数据。
  - `list_characters(project_id=None)`：project_id 为 None 时返回全局列表，否则返回关联到该项目的角色。
  - `create_character(name, project_id=None)`：创建全局角色，可选关联到项目。
  - 新增 `link_character_to_project()` / `unlink_character_from_project()` 方法。
  - `list_specs()` / `get_spec()` / `create_spec()` / `update_spec()` / `delete_spec()`：全局规格 CRUD。
  - `create_character_variant()` 和 `create_spec()` 互相为对方已有记录生成空规格值。
  - `list_spec_values_for_variant()` 的 JOIN 从 `project_specs` 改为 `specs`。
- `backend/app/character_database.py`（新增）
  - 读取 `data/character-database/danbooru_character.csv`（约 24.4 万行，39MB）。
  - 线程安全的内存缓存，首次查询时加载全部数据。
  - `search(q, copyright_filter, sort, page, page_size)`：模糊搜索 character/copyright/core_tags/trigger，支持作品系列筛选、按数量倒序或字母序排序、分页。
  - `list_copyrights()`：返回所有作品系列列表。
  - `stats()`：返回总角色数和总作品系列数。
- `backend/app/app_factory.py`
  - 角色路由从 `/api/projects/{project_id}/characters` 改为 `/api/characters`（可选 `?project_id=` 查询参数）。
  - 规格路由从 `/api/projects/{project_id}/specs` 改为 `/api/specs`。
  - `PATCH/DELETE /api/project-specs/{id}` 改为 `/api/specs/{id}`。
  - 新增 `POST/DELETE /api/projects/{project_id}/characters/{character_id}` 用于关联/取消关联角色到项目。
  - 新增 `GET /api/character-database/search`、`GET /api/character-database/copyrights`、`GET /api/character-database/stats` 三个角色查询接口。
- `backend/tests/test_characters.py`
  - 适配新 API 路径（36 个测试全部通过）。
  - 移除对 `project_id` 字段的断言。
  - 跨项目隔离测试改为验证 `project_characters` 关联表语义。

### 前端变更（`design/ui-preview/`）

- `app.js`
  - 侧边栏 `pages` 新增 `["character-database", "角色查询", "工具", "CD"]`。
  - `navGroups` 调整：`characters` 从"项目制作"移到"全局资源"分类下；新增"工具"分类，包含 `character-database` 入口。
  - 新增 `characterDatabasePage()` 静态渲染函数：搜索栏（搜索框 + 作品系列下拉 + 排序下拉 + 搜索按钮）+ 结果表格 + 分页。
- `runtime-api.js`
  - `renderProductionCharacters()` 改为无参函数，拉取 `GET /api/characters`（全局列表），不依赖项目上下文。
  - 页面标题"人物库"，副标题"管理全局人物、形象变体与景别规格。"
  - `submitCharacter()` 改为 `POST /api/characters`（不关联项目）。
  - 规格相关路径从 `/api/projects/{id}/specs` 和 `/api/project-specs/{id}` 改为 `/api/specs` 和 `/api/specs/{id}`。
  - 大弹窗文案："项目规格"→"规格"，"全项目共享"→"全局共享"。
  - 新增 `renderCharacterDatabasePage()` 及配套函数：调用角色查询 API，渲染搜索结果表格（6 列：角色名/作品系列/触发词/核心标签/标签数/Danbooru 链接），支持分页。
  - `refreshDatabaseState()` 新增 `page === "character-database"` 路由分支。
- `styles.css`
  - 新增 `.character-database-*` 系列样式（搜索栏、结果表格、表头/单元格、悬停高亮、空状态、分页栏、Danbooru 链接）。
- `index.html` 静态资源版本号 `v=20260728-variant-tabs` → `v=20260728-global-characters`。

### 数据源

- `data/character-database/danbooru_character.csv`：从 `C:\Users\kono707da\Documents\AICode\comfyui-manager\danbooru_character.csv` 复制。
- 来源：Huggingface `Laxhar/noob-wiki` 数据集的 `danbooru_character_webui.csv`（约 24.4 万角色）。
- 列：character, copyright, trigger, core_tags, count, solo_count, url。

### 测试用例与结果

测试环境：本地 `start-test.bat` 启动于 `http://127.0.0.1:8111`（锁定测试库）。

| 用例 | 结果 |
|------|------|
| 侧边栏"全局资源"下有"人物库"入口 | 通过 |
| 侧边栏"工具"分类下有"角色查询"入口 | 通过 |
| 人物库页面不依赖项目上下文 | 通过，标题"人物库" |
| 创建全局角色 | 通过，`POST /api/characters` 返回 201 |
| 角色查询页面加载 | 通过，首次加载显示 50 条结果 |
| 搜索"hatsune" | 通过，返回 hatsune_miku 等结果 |
| 搜索结果 6 列正确显示 | 通过 |
| 分页功能 | 通过，可切换下一页 |
| 浏览器 Console JS 报错检查 | 通过，无阻塞性 JS 异常 |

单元测试（`python -m unittest discover -s backend/tests`）：83 个测试全部通过。

### 后端版本

- `backend/app/app_factory.py` 的 `APP_VERSION` 从 `0.1.6` 更新为 `0.1.7`。

---

## v0.1.6 — 2026-07-28 — 形象变体改为标签页切换

分支：`dev-260728-GLM5.2-variant-tabs`（从 `main` 拉取新建）

### 变更摘要

人物详情大弹窗内的形象变体从纵向列表改为水平标签页（tabs），项目规格列表作为主区域显示在标签页下方。切换变体标签只改变"当前变体"状态（为后续规格值编辑预留），规格列表始终相同（项目级共享）。

### 布局变化

**变更前**：左右分栏（左变体列表 + 右规格列表）
**变更后**：上下布局（顶部变体标签页 + 下方规格主区域）

```
┌─────────────────────────────────────────────┐
│ [默认] [裙装] [+]                            │  ← 变体标签页
├─────────────────────────────────────────────┤
│ 项目规格                                      │
│ • 规格1                                       │  ← 规格主区域
│ • 规格2                                       │
│ [+ 添加规格]                                  │
└─────────────────────────────────────────────┘
```

### 前端变更（`design/ui-preview/`）

- `runtime-api.js`
  - 重写 `characterExpandedPanel`：移除左右分栏结构，改为 `.variant-tabs-bar`（标签栏）+ `.character-expanded-main`（规格主区域）。
  - 新增 `variantTab(variant, isActive)` 渲染函数：每个变体渲染为 `.variant-tab` 按钮，带 `data-api-action="select-variant"`、`data-variant-id`、`data-variant-name`、`data-context-menu="character-variant"` 等属性。
  - 默认变体（`is_default=1`）自动设为 active 状态，标签上加"默认"小标记。
  - 标签页末尾新增 `+` 按钮（`.variant-tab-add`），点击展开内嵌输入表单创建新变体。
  - 主区域副标题显示"当前变体：{name}"，切换标签时实时更新。
  - 新增 `select-variant` 点击处理：移除其他标签的 `.active` 类，给当前标签加 `.active`，更新副标题。
  - 规格列表保持原有结构（列表 + 底部"添加规格"按钮 + 右键删除）。
- `styles.css`
  - 移除 `@media (min-width: 900px)` 下的 `.character-expanded` 左右分栏（`flex-direction: row`）和 `.character-expanded-column` 样式。
  - 新增 `.variant-tabs-bar`（标签栏容器，底部边框分隔）、`.variant-tabs`（标签行，flex-wrap 自适应换行）。
  - 新增 `.variant-tab`（标签项，32px 高，圆角 9px，hover 变深）、`.variant-tab.active`（蓝紫渐变高亮）、`.variant-tab-name`、`.variant-tab-default`（默认标记）。
  - 新增 `.variant-tab-add`（+按钮，虚线边框，hover 变蓝）、`.variant-tab-form`（标签栏内嵌表单）。
  - 新增 `.character-expanded-main`（规格主区域容器）。
- `index.html` 静态资源版本号 `v=20260728-modal-relative` → `v=20260728-variant-tabs`。

### 交互模式

- **切换变体**：点击变体标签，标签高亮切换，副标题更新"当前变体：..."。规格列表不变（项目级共享）。
- **添加变体**：点击标签页末尾 `+` 按钮，展开内嵌输入表单，输入名称后创建新标签。
- **变体改名/删除**：右键变体标签弹出"改名/删除"菜单（与章节/大场景右键菜单一致）。默认变体拒绝删除。
- **添加规格**：主区域底部"添加规格"按钮，展开内嵌表单（类型下拉 + 自定义标签）。
- **规格删除**：右键规格行弹出菜单删除。

### 测试用例与结果

测试环境：本地 `start-test.bat` 启动于 `http://127.0.0.1:8111`（锁定测试库）。

| 用例 | 结果 |
|------|------|
| 变体以标签页形式水平排列 | 通过，2 个标签（默认、裙装） |
| 默认变体标签带"默认"标记且 active | 通过，第一个标签 active 高亮 |
| 标签页末尾有+按钮 | 通过 |
| 点击其他标签切换 active | 通过，点击"裙装"后高亮切换 |
| 副标题更新"当前变体：..." | 通过，切换后显示"当前变体：裙装" |
| 项目规格列表在主区域显示 | 通过，1 个规格行 |
| 右键标签弹出改名/删除菜单 | 通过，菜单项含"改名"和"删除" |
| 大弹窗宽度 92vw | 通过，约 614px（视口 668px） |
| 浏览器 Console JS 报错检查 | 通过，无业务逻辑 JS 异常 |

### 后端变更

- 无。

---

## v0.1.5 — 2026-07-28 — 弹窗尺寸改为相对视口

分支：`dev-260728-GLM5.2-character-modal-detail`（基于 `main` 继续）

### 变更摘要

弹窗尺寸从固定 px 改为相对视口宽度，大弹窗始终占 92vw（左右各留 4vw），不论屏幕大小边距比例一致。大弹窗内的变体/规格栏在宽屏下自动左右分栏，利用额外宽度。

### 弹窗规格定义（更新）

- **小弹窗（size-sm）**：`min(380px, 92vw)` — 固定 380px，小屏幕下不超过 92vw。用于确认删除等简单操作。
- **中弹窗（size-md）**：`min(480px, 92vw)` — 固定 480px，小屏幕下不超过 92vw。用于普通输入交互。
- **大弹窗（size-lg）**：`92vw`，`max-height: 90vh` — 相对视口，左右各留 4vw。用于复杂交互（人物详情等）。

### 前端变更（`design/ui-preview/`）

- `styles.css`
  - 移除三个 `@media (min-width: 1024px/1440px/1920px)` 断点（之前让宽度分级跳变，不符合"相对视口"需求）。
  - `.atelier-modal.size-sm` 从 `min(360px, 100%)` 改为 `min(380px, 92vw)`。
  - `.atelier-modal.size-md` 从 `min(420px, 100%)` 改为 `min(480px, 92vw)`。
  - `.atelier-modal.size-lg` 从 `min(1200px, 96vw)` 改为 `92vw`，`max-height` 从 `min(90vh, 860px)` 改为 `90vh`。
  - `.atelier-modal.character-detail-modal` 同步改为 `92vw` / `90vh`。
  - 新增 `@media (min-width: 900px)` 断点：`.character-expanded` 从 `flex-direction: column` 改为 `row`，`.character-expanded-column` 设为 `flex: 1 1 0; min-width: 0;` 实现等宽左右分栏。窄屏下保持上下堆叠。
- `index.html` 静态资源版本号 `v=20260728-modal-sizes` → `v=20260728-modal-relative`。

### 设计原则

- **弹窗尺寸相对视口**：大弹窗在 1200px 屏幕上宽 1104px，在 2300px 屏幕上宽 2116px，左右边距始终是视口的 4%。
- **弹窗内容固定尺寸**：变体行、规格行、按钮等内部元素保持固定样式，不随视口缩放。
- **内容布局自适应**：大弹窗变宽后，变体/规格栏自动左右分栏，利用额外空间；窄屏下回退到上下堆叠。

### 测试用例与结果

测试环境：本地 `start-test.bat` 启动于 `http://127.0.0.1:8111`（锁定测试库）。

| 用例 | 结果 |
|------|------|
| 大弹窗 width 为 92vw | 通过，CSS 源码与计算样式一致 |
| 大弹窗左右边距为视口 4% | 通过 |
| 大弹窗内变体/规格栏宽屏下左右分栏 | 通过，`@media (min-width: 900px)` 下 `flex-direction: row` |
| 小弹窗宽度约 380px（固定） | 通过 |
| 中弹窗宽度约 480px（固定） | 通过 |
| 浏览器 Console JS 报错检查 | 通过，无 JS 异常 |

### 后端变更

- 无。

---

## v0.1.4 — 2026-07-28 — 弹窗三级规格 + 删除确认弹窗化

分支：`dev-260728-GLM5.2-character-modal-detail`（从 `main` 拉取新建）

### 变更摘要

将所有弹窗统一为三级规格（小/中/大），并以自定义小弹窗替代所有 `window.confirm` 原生确认框。同时修复人物详情弹窗人物名称显示为 undefined 的接口字段解包 bug。

### 弹窗规格定义

- **小弹窗（size-sm，360px）**：确认删除等简单操作。红色危险图标、标题、说明、取消/确认按钮（危险操作确认按钮为红色实色 `.btn.danger`）。
- **中弹窗（size-md，420px）**：普通输入交互（新建项目/章节/大场景/人物/改名等）。保持原有基础 `.atelier-modal` 样式。
- **大弹窗（size-lg，920px）**：复杂交互（人物详情等）。`padding: 0`、flex 纵向布局、自带 header 与可滚动内容区。

### 前端变更（`design/ui-preview/`）

- `styles.css`
  - 新增 `.atelier-modal.size-sm` / `.size-md` / `.size-lg` 三个尺寸修饰类。
  - `.atelier-modal.character-detail-modal` 宽度从 680px 升级到 920px（与 size-lg 一致），`padding: 0`。
  - 新增 `.atelier-modal.confirm-dialog .confirm-icon`（红色渐变圆形图标）、`.confirm-dialog h2` / `p`、`.confirm-actions` 按钮区样式。
  - 新增 `.btn.danger`（红色实色渐变按钮）用于危险确认操作。
- `runtime-api.js`
  - 新增 `confirmDialog({title, message, confirmText, cancelText, danger})` 函数：返回 Promise<boolean>，使用 `size-sm` + `confirm-dialog` 修饰类渲染小弹窗；危险操作确认按钮加 `.danger` 类。
  - 新增 `resolveConfirmDialog(result)` / `closeConfirmDialog()` 辅助函数管理弹窗关闭与 Promise 解析。
  - `ensureCharacterDetailModal` 在 `.atelier-modal` 上追加 `size-lg` 修饰类。
  - 修复 `renderCharacterDetail` 的接口字段解包 bug：`request(/api/characters/{id})` 返回 `{character, stats, database_environment}`，之前直接用 `character.name` 导致 undefined，改为 `characterPayload.character` 取出真实人物对象。
  - 替换 5 处 `window.confirm` 为 `await confirmDialog(...)`：
    - `deleteCharacter`：删除人物，标题「删除人物「{name}」」，提示变体与规格值级联删除。
    - `deleteCharacterVariant`：删除形象变体，标题「删除形象变体「{name}」」。
    - `deleteProjectSpec`：删除项目规格，标题「删除项目规格「{name}」」，提示规格值级联删除。
    - `deleteChapter`：删除章节，标题「删除章节「{name}」」，提示大场景级联删除数量。
    - `deleteLargeScene`：删除大场景，标题「删除大场景「{name}」」。
    - 数据库环境切换：标题「切换到生产/测试数据库」，生产环境 danger=true，测试环境 danger=false。
  - ESC 键监听新增 `closeConfirmDialog()` 调用。
- `index.html` 静态资源版本号 `v=20260728-character-modal` → `v=20260728-modal-sizes`。

### 测试用例与结果

测试环境：本地 `start-test.bat` 启动于 `http://127.0.0.1:8111`（锁定测试库），使用浏览器子代理执行。

| 用例 | 结果 |
|------|------|
| 点击人物卡片打开大弹窗 | 通过，弹窗显示 `size-lg` 类，宽度自适应到视口允许的最大值 |
| 大弹窗人物名称正确显示 | 通过，显示「角色 A」（修复前为 undefined） |
| 大弹窗副标题格式 | 通过，显示「2 个变体 · 1 个项目规格 · 规格 0/0」 |
| 右键人物卡片 → 删除菜单 → 小弹窗 | 通过，弹出 360px 宽小弹窗 |
| 小弹窗标题与说明 | 通过，标题「删除人物「角色 A」」，说明包含级联删除提示 |
| 小弹窗按钮文案与样式 | 通过，取消/删除按钮，删除按钮含 `.danger` 红色样式 |
| 点击取消关闭小弹窗 | 通过 |
| 关闭大弹窗 | 通过 |
| 浏览器 Console JS 报错检查 | 通过，无 JS 异常 |

### 后端变更

- 无。本次仅前端 UI 重构与 bug 修复，未触及后端接口或数据库结构。

---

## v0.1.3 — 2026-07-28 — 修复 sidebar 无法滚动

分支：`dev-260728-fix-sidebar-scroll`（从 `main` 拉取新建）

### 变更摘要

侧边栏导航项过多时被裁剪无法滚动，原因是 `.nav-scroll` 设置了 `overflow: hidden`。

### 前端变更

- `design/ui-preview/styles.css`：`.nav-scroll` 的 `overflow: hidden` 改为 `overflow-y: auto`，并添加 `overscroll-behavior: contain` 防止滚动传播到页面其他区域。
- `design/ui-preview/index.html`：静态资源版本号更新为 `v=20260728-sidebar-scroll`。

### 测试结果

- 浏览器 JS evaluate 验证：`overflowY` 计算值为 `auto`，`scrollHeight=686 > clientHeight=466`，确认可滚动。
- 控制台无报错。

---

## v0.1.2 — 2026-07-27 — 人物库布局重构：左右分栏 + 卡片紧凑化

分支：`dev-260727-glm-character-layout`（从 `main` 拉取新建）

### 变更摘要

人物库卡片从固定 2 列大卡片改为自适应多列紧凑卡片，展开管理从卡片内部展开改为右侧固定详情面板（左右分栏，可边看列表边编辑）。卡片新增规格完整度显示。

### 前端变更（`design/ui-preview/`）

- `styles.css`
  - `.character-grid` 从 `repeat(2, minmax(0, 1fr))` 改为 `repeat(auto-fill, minmax(200px, 1fr))`，宽屏一行可容纳多个卡片。
  - `.character-block` 从 84px 缩略图 + 内容的双列网格改为纵向 flex 布局，padding 从 14 减到 10，border-radius 从 17 减到 13，去掉 `.expanded` 状态。
  - `.character-block-thumb` 从 84×96 首字母方块改为 100%×72 的纯色占位条（留给未来人物图片）。
  - 新增 `.character-block-stats` / `.stats-pill` / `.stats-pill.muted` 样式，显示规格完整度标签。
  - 新增 `.character-workspace` 左右分栏布局（`grid-template-columns: minmax(0,1fr) 460px`），右侧 `.character-detail-pane` sticky 定位可独立滚动；1100px 以下响应式改为单列。
  - 新增 `.character-detail-empty` / `.character-detail-card` / `.character-detail-header` / `.character-detail-body` 等右侧详情面板样式。
  - `.character-expanded` 从双列网格改为纵向 flex，适配右侧面板宽度。
- `runtime-api.js`
  - `characterCard()` 改为紧凑卡片：去掉首字母缩略图和"展开管理"按钮，新增规格完整度标签（`规格 filled/total`），卡片整体可点击（`data-api-action="select-character"`）。
  - `renderProductionCharacters()` 改为渲染 `.character-workspace` 左右分栏：左侧卡片列表，右侧空状态提示。
  - 新增 `renderCharacterDetail(characterId)`：点击卡片后在右侧面板渲染详情卡片（含变体/规格管理区），卡片标记 `.active` 高亮。
  - 新增 `refreshCharacterDetail()`：刷新当前选中人物的详情面板。
  - 移除 `refreshCharacterExpanded()`，所有刷新调用改为 `refreshCharacterDetail()` 或 `refreshExpandedOrAll()`（后者改为查找 `.active` 卡片）。
  - `refreshAfterRename()` 增加 id 参数，character 改名后保留选中状态。
  - `add-variant` / `add-spec` 按钮的表单查找从 `.character-block` 改为 `#character-detail-pane`。
- `index.html` 静态资源版本号更新为 `v=20260727-character-layout`。

### 后端变更

- `backend/app/database.py`：新增 `get_character_stats(character_id)` 方法，单次 SQL 聚合返回 `{variant_count, spec_total, spec_filled}`（已填 prompt 的规格值数 / 总规格值数）。
- `backend/app/app_factory.py`：
  - `GET /api/projects/{id}/characters` 列表接口对每个 character 附加 `stats` 字段。
  - 新增 `GET /api/characters/{character_id}` 单人物接口，返回 character + stats。

### 测试用例与结果

- 新增 `test_list_characters_includes_stats_aggregate`：验证列表接口返回 stats 聚合字段（2 变体 × 2 规格 = 4 总规格值，已填 0）。
- `python -m unittest discover -s backend/tests`：83 个测试全部通过。
- 浏览器 JS evaluate 验证：卡片点击后右侧详情面板正确渲染 `<div class="character-detail-card">`；多列网格、紧凑卡片、规格完整度显示均正常。

### 已知限制

- 缩略图占位区暂为纯色块，未来接入人物图片后替换。
- 右键菜单在浏览器自动化工具中截图困难，但代码绑定正确（`data-context-menu="character"`）。

---

## v0.1.1 — 2026-07-27 — 人物库基础功能接入真实 API

分支：`dev-260727-glm-character-api`（从 `main` 拉取新建）

### 变更摘要

人物库页面（`page=characters`）此前使用 mock 数据展示「角色 A/B/C/D」，本次将其接入后端真实 API，覆盖人物、形象变体、项目规格三组接口（规格值编辑不在本期范围）。

### 前端变更（`design/ui-preview/`）

- `runtime-api.js`
  - 新增 `renderProductionCharacters(project)`：拉取人物列表与项目规格列表，对每个人物并发拉取变体列表，渲染为卡片网格。空项目显示「还没有人物」空状态。
  - 新增 `characterCard()` / `characterEmptyState()` / `characterExpandedPanel()` / `variantRow()` / `specRow()` / `specLabel()` 等渲染辅助函数。
  - 新增 `ensureCharacterModal()` / `openCharacterModal()` / `closeCharacterModal()` / `submitCharacter()`：新建人物弹窗，调用 `POST /api/projects/{id}/characters`。
  - 新增 `deleteCharacter()` / `deleteCharacterVariant()` / `deleteProjectSpec()` 三个删除函数，分别调用对应 DELETE 接口，含二次确认与级联提示。默认变体拒绝删除。
  - 新增 `refreshCharacterExpanded()` / `refreshExpandedOrAll()`：展开区局部刷新，保留卡片展开状态。
  - 新增 `submitInlineVariant()` / `submitInlineSpec()`：展开区内嵌表单提交，分别调用 `POST /api/characters/{id}/variants` 与 `POST /api/projects/{id}/specs`。
  - 扩展 `openRenameModal()` / `submitRename()` / `renameRequestPath()` / `refreshAfterRename()` 支持 `character`、`character-variant`、`project-spec` 三种类型。`project-spec` 改名仅 `custom` 类型允许，请求体为 `{custom_label: name}`。
  - 扩展 `openMenuFromElement()` 处理 `character`、`character-variant`、`project-spec` 三种触发源。
  - 扩展 `showContextMenu()` / `hideContextMenu()` 额外保存 `contextIsDefault` 与 `contextSpecType`，用于菜单项行为判定。
  - 扩展全局 `click` 监听新增 `open-character-modal` / `close-character-modal` / `toggle-character` / `add-variant` / `cancel-add-variant` / `add-spec` / `cancel-add-spec` 七个分支。
  - 新增全局 `submit` 监听，按 `form[data-inline-action]` 分发到 `submitInlineVariant` / `submitInlineSpec`。
  - `refreshDatabaseState()` 新增 `pageKey === "characters"` 路由分支。
  - `ESC` 关键字监听新增 `closeCharacterModal()`。
- `styles.css`
  - 新增 `.character-grid` / `.character-block` / `.character-block.expanded` / `.character-block-thumb` / `.character-block-name` / `.character-block-meta` / `.character-block-actions` / `.character-expanded` / `.character-expanded-column` / `.character-expanded-head` / `.character-expanded-title` / `.character-expanded-sub` / `.character-variant-list` / `.character-spec-list` / `.character-variant-row` / `.character-spec-row` / `.character-variant-name` / `.character-spec-name` / `.character-variant-default` / `.character-variant-order` / `.character-spec-order` / `.character-spec-type` / `.character-inline-form` / `.real-character-panel` 等样式，复用既有 `--line` / `--line-strong` / `--green-soft` / `--blue-soft` 等配色与圆角规范。
  - 卡片展开时隐藏 thumb 与 body，只展示展开区，避免视觉重复。
- `index.html`
  - 静态资源版本号 `v=20260727-edit-delete` → `v=20260727-character-api`，强制浏览器刷新缓存。

### 后端变更

- `backend/app/database.py`
  - 新增 `characters`、`character_variants`、`project_specs`、`character_spec_values` 四张表的建表语句（含外键级联、唯一约束、排序索引）。
  - 新增数据库方法：`list_characters` / `get_character` / `create_character` / `update_character` / `delete_character` / `list_character_variants` / `get_character_variant` / `create_character_variant` / `update_character_variant` / `delete_character_variant` / `list_project_specs` / `get_project_spec` / `create_project_spec` / `update_project_spec` / `delete_project_spec` / `list_spec_values_for_variant` / `get_character_spec_value` / `update_character_spec_value`。
  - `create_character` 自动创建「默认」变体（`is_default=1`）；若项目已存在规格，同步为该默认变体生成空规格值。
  - `create_character_variant` 与 `create_project_spec` 互相触发对方已有记录的空规格值生成，保证交叉表行/列完整。
  - 名称统一调用 `" ".join(name.split())` 清洗前后空白；超长、空、重名均抛 `ValueError` 由路由层转 409/422。
- `backend/app/app_factory.py`
  - 新增请求模型：`CreateCharacterRequest` / `RenameCharacterRequest` / `CreateCharacterVariantRequest` / `RenameCharacterVariantRequest` / `CreateProjectSpecRequest` / `UpdateProjectSpecRequest` / `UpdateCharacterSpecValueRequest`，全部带字段级校验。
  - 新增 16 个路由：`GET/POST/PATCH/DELETE` 覆盖人物、形象变体、项目规格、规格值四组资源；`GET /api/projects/{id}/characters` 与 `GET /api/projects/{id}/specs` 支持空列表返回。
  - `CreateProjectSpecRequest` 使用 `model_validator(mode="after")` 校验「custom 类型必须提供 custom_label」，确保未提供标签时返回 422（Pydantic 字段级 `field_validator` 在字段使用默认值时不触发，改用模型级校验器解决）。
  - `UpdateCharacterSpecValueRequest.lora_weight` 限制 0–2 范围，超界返回 422。
- `backend/tests/test_characters.py`（新增）
  - 35 个单元测试，覆盖人物/变体/规格/规格值四组接口的 CRUD、重名拒绝、跨项目隔离、级联删除、默认变体保护、规格值范围校验、双数据库隔离等场景。

### 交互模式

- 人物卡片：右键弹出「改名 / 删除」菜单，与章节/大场景保持一致。
- 卡片「展开管理」按钮：点击切换展开/收起。展开后显示形象变体与项目规格两栏，每栏可独立增删。
- 形象变体：底部「添加变体」按钮展开内嵌输入框；列表项右键可改名/删除（默认变体拒绝删除）。
- 项目规格：底部「添加规格」按钮展开内嵌下拉+标签输入；列表项右键可删除；`custom` 类型可改标签，其他类型拒绝改名。
- 触摸设备：长按 500ms 同样触发右键菜单。

### 测试用例与结果

测试环境：本地 `start-test.bat` 启动于 `http://127.0.0.1:8111`（锁定测试库），使用浏览器子代理执行。

| 用例 | 结果 |
|------|------|
| 项目中心创建项目并进入人物库页面 | 通过，显示「还没有人物」空状态 |
| 点击「新建人物」弹出模态框并创建 | 通过，`POST /api/projects/{id}/characters` 返回 201，卡片显示「1 个形象变体 · 0 个项目规格」 |
| 点击「展开管理」展开卡片 | 通过，显示变体与规格两栏，变体栏含「默认」一项带「默认」标签 |
| 添加形象变体「裙装」 | 通过，`POST /api/characters/{id}/variants` 返回 201，列表新增一项 |
| 添加项目规格「全身」 | 通过，`POST /api/projects/{id}/specs` 返回 201，列表新增一项带「全身」类型标签 |
| 添加自定义规格「近景特写」 | 通过，列表新增一项，名称显示为自定义标签 |
| 人物卡片右键菜单 | 通过，弹出「改名 / 删除」菜单，样式与章节一致 |
| 人物改名 | 通过，`PATCH /api/characters/{id}` 返回 200，卡片名更新 |
| 变体行右键菜单 | 通过，弹出菜单 |
| 删除非默认变体 | 通过，`DELETE /api/character-variants/{id}` 返回 200，列表移除 |
| 非自定义规格拒绝改名 | 通过，提示「仅自定义规格可改标签」 |
| 删除自定义规格 | 通过，`DELETE /api/project-specs/{id}` 返回 200，列表移除 |
| 浏览器 Console JavaScript 报错检查 | 通过，无 JS 异常 |

单元测试（`python -m unittest discover -s backend/tests`）：82 个测试全部通过，其中 `backend/tests/test_characters.py` 覆盖人物/变体/规格/规格值四组接口 35 个用例，含自定义规格缺标签返回 422、LoRA 权重超界返回 422、跨数据库隔离等边界场景。

### 已知限制

- 不支持规格值（变体 × 规格的交叉表）编辑，下一期与人物替换矩阵页一起做。
- 人物卡片不显示分类标签、规格完整度状态、引用分镜页数等 UI 元素（后端暂无对应字段）。
- 全局人物模板 tab、「从项目复制」「打开替换矩阵」按钮暂不实现。

---

## v0.1.0 — 2026-07-27 — ChatGPT 协作版本基线

首次纳入 Git 版本控制的基线版本，详细能力见 `DEVELOPMENT_LOG.md`。
