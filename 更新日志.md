# Atelier 更新日志

本文件记录每个版本的详细变更，最新版本在前。提交代码前追加新版本记录。

## v0.1.3 — 2026-07-28 — 修复 sidebar 无法滚动

分支：`dev-260728-fix-sidebar-scroll`（从 `main` 拉取新建）

### 变更摘要

侧边栏导航项过多时被裁剪无法滚动，原因是 `.nav-scroll` 设置了 `overflow: hidden`。

### 前端变更

- `design/ui-preview/styles.css`：`.nav-scroll` 的 `overflow: hidden` 改为 `overflow-y: auto`，并添加 `overscroll-behavior: contain` 防止滚动传播到页面其他区域。
- `design/ui-preview/index.html`：静态资源版本号更新为 `v=20260728-sidebar-scroll`。

### 测试结果

- 浏览器 JS evaluate 验证：`overflowY` 计算值为 `auto`，`scrollHeight=686 > clientHeight=466`，确认可滚动。
- 控制台无报错。

---

## v0.1.2 — 2026-07-27 — 人物库布局重构：左右分栏 + 卡片紧凑化

分支：`dev-260727-glm-character-layout`（从 `main` 拉取新建）

### 变更摘要

人物库卡片从固定 2 列大卡片改为自适应多列紧凑卡片，展开管理从卡片内部展开改为右侧固定详情面板（左右分栏，可边看列表边编辑）。卡片新增规格完整度显示。

### 前端变更（`design/ui-preview/`）

- `styles.css`
  - `.character-grid` 从 `repeat(2, minmax(0, 1fr))` 改为 `repeat(auto-fill, minmax(200px, 1fr))`，宽屏一行可容纳多个卡片。
  - `.character-block` 从 84px 缩略图 + 内容的双列网格改为纵向 flex 布局，padding 从 14 减到 10，border-radius 从 17 减到 13，去掉 `.expanded` 状态。
  - `.character-block-thumb` 从 84×96 首字母方块改为 100%×72 的纯色占位条（留给未来人物图片）。
  - 新增 `.character-block-stats` / `.stats-pill` / `.stats-pill.muted` 样式，显示规格完整度标签。
  - 新增 `.character-workspace` 左右分栏布局（`grid-template-columns: minmax(0,1fr) 460px`），右侧 `.character-detail-pane` sticky 定位可独立滚动；1100px 以下响应式改为单列。
  - 新增 `.character-detail-empty` / `.character-detail-card` / `.character-detail-header` / `.character-detail-body` 等右侧详情面板样式。
  - `.character-expanded` 从双列网格改为纵向 flex，适配右侧面板宽度。
- `runtime-api.js`
  - `characterCard()` 改为紧凑卡片：去掉首字母缩略图和"展开管理"按钮，新增规格完整度标签（`规格 filled/total`），卡片整体可点击（`data-api-action="select-character"`）。
  - `renderProductionCharacters()` 改为渲染 `.character-workspace` 左右分栏：左侧卡片列表，右侧空状态提示。
  - 新增 `renderCharacterDetail(characterId)`：点击卡片后在右侧面板渲染详情卡片（含变体/规格管理区），卡片标记 `.active` 高亮。
  - 新增 `refreshCharacterDetail()`：刷新当前选中人物的详情面板。
  - 移除 `refreshCharacterExpanded()`，所有刷新调用改为 `refreshCharacterDetail()` 或 `refreshExpandedOrAll()`（后者改为查找 `.active` 卡片）。
  - `refreshAfterRename()` 增加 id 参数，character 改名后保留选中状态。
  - `add-variant` / `add-spec` 按钮的表单查找从 `.character-block` 改为 `#character-detail-pane`。
- `index.html` 静态资源版本号更新为 `v=20260727-character-layout`。

### 后端变更

- `backend/app/database.py`：新增 `get_character_stats(character_id)` 方法，单次 SQL 聚合返回 `{variant_count, spec_total, spec_filled}`（已填 prompt 的规格值数 / 总规格值数）。
- `backend/app/app_factory.py`：
  - `GET /api/projects/{id}/characters` 列表接口对每个 character 附加 `stats` 字段。
  - 新增 `GET /api/characters/{character_id}` 单人物接口，返回 character + stats。

### 测试用例与结果

- 新增 `test_list_characters_includes_stats_aggregate`：验证列表接口返回 stats 聚合字段（2 变体 × 2 规格 = 4 总规格值，已填 0）。
- `python -m unittest discover -s backend/tests`：83 个测试全部通过。
- 浏览器 JS evaluate 验证：卡片点击后右侧详情面板正确渲染 `<div class="character-detail-card">`；多列网格、紧凑卡片、规格完整度显示均正常。

### 已知限制

- 缩略图占位区暂为纯色块，未来接入人物图片后替换。
- 右键菜单在浏览器自动化工具中截图困难，但代码绑定正确（`data-context-menu="character"`）。

---

## v0.1.1 — 2026-07-27 — 人物库基础功能接入真实 API

分支：`dev-260727-glm-character-api`（从 `main` 拉取新建）

### 变更摘要

人物库页面（`page=characters`）此前使用 mock 数据展示「角色 A/B/C/D」，本次将其接入后端真实 API，覆盖人物、形象变体、项目规格三组接口（规格值编辑不在本期范围）。

### 前端变更（`design/ui-preview/`）

- `runtime-api.js`
  - 新增 `renderProductionCharacters(project)`：拉取人物列表与项目规格列表，对每个人物并发拉取变体列表，渲染为卡片网格。空项目显示「还没有人物」空状态。
  - 新增 `characterCard()` / `characterEmptyState()` / `characterExpandedPanel()` / `variantRow()` / `specRow()` / `specLabel()` 等渲染辅助函数。
  - 新增 `ensureCharacterModal()` / `openCharacterModal()` / `closeCharacterModal()` / `submitCharacter()`：新建人物弹窗，调用 `POST /api/projects/{id}/characters`。
  - 新增 `deleteCharacter()` / `deleteCharacterVariant()` / `deleteProjectSpec()` 三个删除函数，分别调用对应 DELETE 接口，含二次确认与级联提示。默认变体拒绝删除。
  - 新增 `refreshCharacterExpanded()` / `refreshExpandedOrAll()`：展开区局部刷新，保留卡片展开状态。
  - 新增 `submitInlineVariant()` / `submitInlineSpec()`：展开区内嵌表单提交，分别调用 `POST /api/characters/{id}/variants` 与 `POST /api/projects/{id}/specs`。
  - 扩展 `openRenameModal()` / `submitRename()` / `renameRequestPath()` / `refreshAfterRename()` 支持 `character`、`character-variant`、`project-spec` 三种类型。`project-spec` 改名仅 `custom` 类型允许，请求体为 `{custom_label: name}`。
  - 扩展 `openMenuFromElement()` 处理 `character`、`character-variant`、`project-spec` 三种触发源。
  - 扩展 `showContextMenu()` / `hideContextMenu()` 额外保存 `contextIsDefault` 与 `contextSpecType`，用于菜单项行为判定。
  - 扩展全局 `click` 监听新增 `open-character-modal` / `close-character-modal` / `toggle-character` / `add-variant` / `cancel-add-variant` / `add-spec` / `cancel-add-spec` 七个分支。
  - 新增全局 `submit` 监听，按 `form[data-inline-action]` 分发到 `submitInlineVariant` / `submitInlineSpec`。
  - `refreshDatabaseState()` 新增 `pageKey === "characters"` 路由分支。
  - `ESC` 关键字监听新增 `closeCharacterModal()`。
- `styles.css`
  - 新增 `.character-grid` / `.character-block` / `.character-block.expanded` / `.character-block-thumb` / `.character-block-name` / `.character-block-meta` / `.character-block-actions` / `.character-expanded` / `.character-expanded-column` / `.character-expanded-head` / `.character-expanded-title` / `.character-expanded-sub` / `.character-variant-list` / `.character-spec-list` / `.character-variant-row` / `.character-spec-row` / `.character-variant-name` / `.character-spec-name` / `.character-variant-default` / `.character-variant-order` / `.character-spec-order` / `.character-spec-type` / `.character-inline-form` / `.real-character-panel` 等样式，复用既有 `--line` / `--line-strong` / `--green-soft` / `--blue-soft` 等配色与圆角规范。
  - 卡片展开时隐藏 thumb 与 body，只展示展开区，避免视觉重复。
- `index.html`
  - 静态资源版本号 `v=20260727-edit-delete` → `v=20260727-character-api`，强制浏览器刷新缓存。

### 后端变更

- `backend/app/database.py`
  - 新增 `characters`、`character_variants`、`project_specs`、`character_spec_values` 四张表的建表语句（含外键级联、唯一约束、排序索引）。
  - 新增数据库方法：`list_characters` / `get_character` / `create_character` / `update_character` / `delete_character` / `list_character_variants` / `get_character_variant` / `create_character_variant` / `update_character_variant` / `delete_character_variant` / `list_project_specs` / `get_project_spec` / `create_project_spec` / `update_project_spec` / `delete_project_spec` / `list_spec_values_for_variant` / `get_character_spec_value` / `update_character_spec_value`。
  - `create_character` 自动创建「默认」变体（`is_default=1`）；若项目已存在规格，同步为该默认变体生成空规格值。
  - `create_character_variant` 与 `create_project_spec` 互相触发对方已有记录的空规格值生成，保证交叉表行/列完整。
  - 名称统一调用 `" ".join(name.split())` 清洗前后空白；超长、空、重名均抛 `ValueError` 由路由层转 409/422。
- `backend/app/app_factory.py`
  - 新增请求模型：`CreateCharacterRequest` / `RenameCharacterRequest` / `CreateCharacterVariantRequest` / `RenameCharacterVariantRequest` / `CreateProjectSpecRequest` / `UpdateProjectSpecRequest` / `UpdateCharacterSpecValueRequest`，全部带字段级校验。
  - 新增 16 个路由：`GET/POST/PATCH/DELETE` 覆盖人物、形象变体、项目规格、规格值四组资源；`GET /api/projects/{id}/characters` 与 `GET /api/projects/{id}/specs` 支持空列表返回。
  - `CreateProjectSpecRequest` 使用 `model_validator(mode="after")` 校验「custom 类型必须提供 custom_label」，确保未提供标签时返回 422（Pydantic 字段级 `field_validator` 在字段使用默认值时不触发，改用模型级校验器解决）。
  - `UpdateCharacterSpecValueRequest.lora_weight` 限制 0–2 范围，超界返回 422。
- `backend/tests/test_characters.py`（新增）
  - 35 个单元测试，覆盖人物/变体/规格/规格值四组接口的 CRUD、重名拒绝、跨项目隔离、级联删除、默认变体保护、规格值范围校验、双数据库隔离等场景。

### 交互模式

- 人物卡片：右键弹出「改名 / 删除」菜单，与章节/大场景保持一致。
- 卡片「展开管理」按钮：点击切换展开/收起。展开后显示形象变体与项目规格两栏，每栏可独立增删。
- 形象变体：底部「添加变体」按钮展开内嵌输入框；列表项右键可改名/删除（默认变体拒绝删除）。
- 项目规格：底部「添加规格」按钮展开内嵌下拉+标签输入；列表项右键可删除；`custom` 类型可改标签，其他类型拒绝改名。
- 触摸设备：长按 500ms 同样触发右键菜单。

### 测试用例与结果

测试环境：本地 `start-test.bat` 启动于 `http://127.0.0.1:8111`（锁定测试库），使用浏览器子代理执行。

| 用例 | 结果 |
|------|------|
| 项目中心创建项目并进入人物库页面 | 通过，显示「还没有人物」空状态 |
| 点击「新建人物」弹出模态框并创建 | 通过，`POST /api/projects/{id}/characters` 返回 201，卡片显示「1 个形象变体 · 0 个项目规格」 |
| 点击「展开管理」展开卡片 | 通过，显示变体与规格两栏，变体栏含「默认」一项带「默认」标签 |
| 添加形象变体「裙装」 | 通过，`POST /api/characters/{id}/variants` 返回 201，列表新增一项 |
| 添加项目规格「全身」 | 通过，`POST /api/projects/{id}/specs` 返回 201，列表新增一项带「全身」类型标签 |
| 添加自定义规格「近景特写」 | 通过，列表新增一项，名称显示为自定义标签 |
| 人物卡片右键菜单 | 通过，弹出「改名 / 删除」菜单，样式与章节一致 |
| 人物改名 | 通过，`PATCH /api/characters/{id}` 返回 200，卡片名更新 |
| 变体行右键菜单 | 通过，弹出菜单 |
| 删除非默认变体 | 通过，`DELETE /api/character-variants/{id}` 返回 200，列表移除 |
| 非自定义规格拒绝改名 | 通过，提示「仅自定义规格可改标签」 |
| 删除自定义规格 | 通过，`DELETE /api/project-specs/{id}` 返回 200，列表移除 |
| 浏览器 Console JavaScript 报错检查 | 通过，无 JS 异常 |

单元测试（`python -m unittest discover -s backend/tests`）：82 个测试全部通过，其中 `backend/tests/test_characters.py` 覆盖人物/变体/规格/规格值四组接口 35 个用例，含自定义规格缺标签返回 422、LoRA 权重超界返回 422、跨数据库隔离等边界场景。

### 已知限制

- 不支持规格值（变体 × 规格的交叉表）编辑，下一期与人物替换矩阵页一起做。
- 人物卡片不显示分类标签、规格完整度状态、引用分镜页数等 UI 元素（后端暂无对应字段）。
- 全局人物模板 tab、「从项目复制」「打开替换矩阵」按钮暂不实现。

---

## v0.1.0 — 2026-07-27 — ChatGPT 协作版本基线

首次纳入 Git 版本控制的基线版本，详细能力见 `DEVELOPMENT_LOG.md`。
