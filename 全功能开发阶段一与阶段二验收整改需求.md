# Atelier 全功能开发阶段一与阶段二验收整改需求

版本：1.0  
日期：2026-07-29  
状态：待开发  
验收目标分支：`dev-260729-GLM5.2-全功能开发`  
验收目标提交：`571e28087cc76873a3a96af2be8767a57875e7ed`  
项目路径：`C:\Users\kono707da\Documents\AICode\atelier`  
依据文档：`Atelier全功能产品与技术开发需求.md`

---

## 1. 任务结论

第三轮小场景修复已经通过验收，但全功能开发的阶段 1、阶段 2 尚未形成完整的前后端闭环，不得合并到 `main`，不得标记为阶段完成。

本轮整改目标：

1. 修复阶段 2 工作流编辑后导出旧数据的问题。
2. 将阶段 2 已有后端能力完整接入真实前端。
3. 让 ComfyUI 地址可配置、可探测、可明确选择，不再假设只有某一个固定地址。
4. 补齐阶段 1 仍然只有后端、没有完整前端入口的功能。
5. 移除用户界面中已经取消的测试数据库设计和数据库环境提示。
6. 建立阶段 1、阶段 2 的真实浏览器验收和真实 ComfyUI 验收。
7. 明确阶段 2 不包含实际生图；实际生图从阶段 3 开始。

---

## 2. 已确认的验收基线

### 2.1 GitHub 状态

- GitHub 已存在 `dev-260729-GLM5.2-全功能开发`。
- 远端头提交为 `571e28087cc76873a3a96af2be8767a57875e7ed`。
- 该分支相对 `main` 领先 20 个提交、落后 0 个提交。
- 第三轮修复提交 `94f22528560cac9350a050cb5dd3d14e39b74d39` 已包含在该分支。
- 阶段 2 提交 `f57531e0b52e66e6790f38fdb233f009fa9257c5` 已包含在该分支。
- 该分支尚未合并到 `main`。

### 2.2 自动化测试

验收时执行：

```bat
.\.venv\Scripts\python.exe -m unittest discover -s backend\tests -v
```

结果：

- 881 项通过。
- 0 项失败。
- 9 项跳过。

跳过的 9 项均为真实 ComfyUI 集成测试。跳过原因不是 ComfyUI 实际不可达，而是测试只探测 `127.0.0.1:8188`。

### 2.3 真实 ComfyUI 连接

验收时发现 `192.168.3.5:8188` 可达，并通过隔离临时数据库完成：

- `/system_stats` 返回 200。
- 识别 ComfyUI 版本 `0.27.1`。
- 同步 2065 个节点定义。
- 识别 1275 个自定义节点。
- 同步 373 个 LoRA。
- 可读取 `CheckpointLoaderSimple` 节点定义。

这只证明连接层和同步层基本可用，不代表已经实际生成图片。

### 2.4 实际生图状态

截至本次验收：

- 没有通过 Atelier 调用 `/prompt` 完成实际生图。
- 没有获得真实 `prompt_id`。
- 没有监听真实生成进度。
- 没有从 ComfyUI history 读取真实任务结果。
- 没有通过 `/view` 下载真实产出。
- 没有把真实产出保存为 Atelier 图片实例。

上述能力属于阶段 3，不属于本轮阶段 1、阶段 2 整改。

---

## 3. 阶段边界

### 3.1 本轮必须完成

- 阶段 1 所有已经声明完成的功能形成前后端闭环。
- 阶段 2 的 ComfyUI 连接、同步、工作流库、工作流画布、语义插槽、转换、校验、版本和导出形成前后端闭环。
- 使用至少一台验收时真实可达的 ComfyUI 完成节点与资源同步。
- 使用真实 ComfyUI 工作流完成导入、编辑、导出、重新导入和校验。

### 3.2 本轮不得扩展

- 不开发阶段 3 的跑图编译器。
- 不开发持久化生成任务队列。
- 不调用 `/prompt` 生成图片。
- 不开发任务中心真实生成进度。
- 不拉取和保存 ComfyUI 图片产出。
- 不开发审片、最终装配和导出成片。
- 不整体重写现有前端视觉风格。
- 不迁移前端框架。

如果开发 AI 认为必须提前调用 `/prompt` 才能验证阶段 2，应停止开发并向用户请求进入阶段 3，不得擅自扩大范围。

---

## 4. ComfyUI 地址与实例识别规则

### 4.1 核心规则

ComfyUI 地址不是固定常量。

当前环境中至少可能存在：

- `http://127.0.0.1:8188`
- `http://192.168.3.5:8188`

两者都可能是有效、独立的 ComfyUI 实例。代码、测试、文档和开发 AI 都不得假设其中某一个永远是唯一正确地址。

### 4.2 地址来源优先级

运行时的活动 ComfyUI 地址按以下顺序确定：

1. 用户已经在 Atelier 设置中保存并明确选中的活动实例。
2. 环境变量 `ATELIER_COMFYUI_URL`。
3. 开发或验收时明确传入的测试地址。
4. 首次设置时探测到的候选实例，由用户选择后保存。

不得使用“哪个先返回就永久选择哪个”的隐式策略。

### 4.3 开发 AI 的识别流程

开发 AI 开始 ComfyUI 相关开发或验收前必须：

1. 读取当前 Atelier 已保存的 ComfyUI 设置。
2. 检查 `ATELIER_COMFYUI_URL`。
3. 对当前已知候选地址执行只读 `/system_stats` 探测。
4. 输出所有可达实例：
   - 地址。
   - ComfyUI 版本。
   - 设备名称。
   - 队列状态。
   - 节点数量。
5. 说明本次准备使用哪一个地址。
6. 如果同时存在多台可达实例，而用户没有指定活动实例，必须询问用户选择。
7. 验收报告记录实际使用地址，不能只写“ComfyUI 已连接”。

### 4.4 产品内实例管理

阶段 2 至少支持：

- 保存一个或多个 ComfyUI 实例。
- 每个实例包含：
  - `id`
  - 用户输入名称
  - HTTP 地址
  - 可选 WebSocket 地址
  - 连接超时
  - 下载超时
  - 是否为活动实例
  - 最近连接状态
  - 最近检测时间
  - ComfyUI 版本
  - 设备摘要
  - 节点定义摘要
- 同一时间只有一个活动实例。
- 切换活动实例需要用户明确操作。
- 切换实例后不删除另一实例的设置和历史同步信息。
- 工作流校验结果必须记录使用了哪个实例和哪个节点定义摘要。

第一版可以只在界面中重点展示活动实例，但数据模型不得把地址写死为单个常量。

### 4.5 自动探测

首次设置时允许提供“发现本机与局域网 ComfyUI”按钮。

探测范围必须受限：

- 当前保存的地址。
- `ATELIER_COMFYUI_URL`。
- `127.0.0.1:8188`。
- 当前产品预设或用户明确添加的局域网地址。

禁止扫描整个局域网或任意端口。

探测只调用只读接口，例如：

```text
GET /system_stats
```

探测结果只作为候选，不自动覆盖当前活动实例。

### 4.6 测试地址

真实集成测试使用：

```text
ATELIER_COMFYUI_TEST_URL
```

规则：

- 设置变量时，只测试该地址。
- 未设置时，可以按受限候选列表探测。
- 找到多台时，测试输出所有候选并按明确规则选择。
- 没有可达实例时允许跳过，但测试摘要必须明确写出探测过的地址。
- 不允许只探测 `127.0.0.1` 后就宣布所有 ComfyUI 不可达。
- 自动化测试使用临时数据库，不修改生产设置。

---

## 5. 阻塞问题一：工作流编辑后导出旧数据

### 5.1 复现

1. 导入一个 UI JSON，节点组件值为 `OLD_VALUE`。
2. 调用节点编辑接口把值改为 `NEW_VALUE`。
3. 编辑接口返回 200。
4. 导出 UI JSON。
5. 导出结果仍为 `OLD_VALUE`。

### 5.2 原因

当前导出逻辑：

```python
data = raw_ui_json if raw_ui_json else normalized_to_ui_json(normalized)
```

节点编辑只更新 `normalized_graph`，仍然保留导入时的 `raw_ui_json`。导出因此无条件返回编辑前的原始 JSON。

API JSON 存在同样风险。

### 5.3 数据模型要求

必须区分：

- 导入来源快照：不可变，只用于审计和来源追踪。
- 当前草稿：用户正在编辑的规范化工作流。
- 当前可导出结果：必须来自当前草稿。

建议字段：

```text
source_raw_ui_json
source_raw_api_json
normalized_graph
source_checksum
draft_checksum
draft_revision
is_dirty
```

已有 `raw_ui_json`、`raw_api_json` 可以迁移为来源快照，但不得继续充当编辑后的导出结果。

### 5.4 导出规则

未编辑的刚导入工作流：

- 允许原样导出来源 JSON。
- 必须保证未知字段不丢失。

发生任何节点、连线、组件、分组、布局或插槽编辑后：

- `is_dirty=true`。
- 导出必须基于当前 `normalized_graph`。
- 所有已支持字段反映最新值。
- 来源中无法理解的未知字段应通过稳定合并策略保留。
- 不允许整份返回旧的来源 JSON。

### 5.5 必须新增测试

至少覆盖：

1. 导入 UI JSON 后不编辑，原样往返。
2. 修改节点组件值后，UI JSON 导出为新值。
3. 修改节点组件值后，API JSON 导出为新值。
4. 新增节点后，导出包含新节点。
5. 删除节点后，导出不再包含该节点。
6. 新增、替换和删除连线后，导出反映当前连线。
7. 分组和布局调整后，UI JSON 反映当前结构。
8. 未知顶层字段在编辑后仍保留。
9. 未知节点原始字段在允许的编辑范围内不丢失。
10. 发布版本保存当前草稿，而不是来源旧 JSON。
11. 编辑后导出并重新导入，节点、连线和组件值一致。

---

## 6. 阻塞问题二：阶段 2 前端未接入

### 6.1 当前问题

验收发现：

- 工作流库使用 `app.js` 中写死的卡片。
- 工作流画布使用写死节点。
- `runtime-api.js` 没有工作流和 ComfyUI API 客户端定义。
- API 创建真实工作流后，刷新页面仍显示“还没有工作流”。
- “新建工作流”按钮无操作。
- “导入 JSON”按钮无操作。
- “从图片提取”按钮无操作。
- 真实工作流 ID 无法打开真实画布。
- 设置页没有可编辑 ComfyUI 地址。
- 设置页没有测试连接和同步节点按钮。

### 6.2 前端 API 常量

`runtime-api.js` 或新的独立 API 模块必须补充：

```text
GET    /api/comfyui/instances
POST   /api/comfyui/instances
PATCH  /api/comfyui/instances/{id}
DELETE /api/comfyui/instances/{id}
POST   /api/comfyui/instances/{id}/activate
POST   /api/comfyui/instances/{id}/test
POST   /api/comfyui/instances/{id}/sync

GET    /api/workflows
POST   /api/workflows
GET    /api/workflows/{id}
PATCH  /api/workflows/{id}
DELETE /api/workflows/{id}
POST   /api/workflows/{id}/archive
POST   /api/workflows/{id}/restore
POST   /api/workflows/{id}/copy
POST   /api/workflows/{id}/import
POST   /api/workflows/import-from-image
GET    /api/workflows/{id}/draft
PUT    /api/workflows/{id}/draft
GET    /api/workflows/{id}/versions
POST   /api/workflows/{id}/draft/validate
POST   /api/workflows/{id}/precheck
POST   /api/workflows/{id}/publish
POST   /api/workflows/{id}/export
```

如果后端保留现有单实例接口，可在不破坏数据的前提下增加兼容层，但前端不得继续依赖静态地址。

### 6.3 工作流库

必须实现：

- 首次加载骨架屏。
- 真实工作流列表。
- 全局、项目、归档筛选。
- 搜索和排序。
- 新建工作流弹窗。
- 名称由用户输入。
- 导入 UI JSON。
- 导入 API JSON。
- 从图片元数据提取。
- 打开工作流。
- 复制、改名、归档、恢复和删除。
- 显示真实版本、节点数、插槽数和校验状态。
- 空状态按钮真实可操作。
- 刷新后数据保持。

不得：

- 回退到 `app.js` 中的演示工作流。
- 显示虚构版本、节点数和同步状态。
- 在 API 错误时显示“还没有工作流”来掩盖错误。

### 6.4 工作流画布

必须从 URL 中读取真实工作流 ID：

```text
?page=workflow-canvas&workflow=<workflow-id>
```

进入后加载：

- 工作流详情。
- 当前草稿。
- 节点定义。
- 语义插槽。
- 布局状态。
- 校验状态。
- 版本列表。

必须真实支持：

- 显示标准节点。
- 显示自定义节点。
- 未知节点只读保留。
- 搜索并添加节点。
- 编辑全部组件参数。
- 创建、替换和删除连线。
- 禁用、旁路、折叠、复制和删除节点。
- 结构化前移、后移、换列、置顶和置底。
- 分组泳道。
- 自动布局。
- 聚焦上游、下游和错误节点。
- 创建、编辑和删除语义插槽。
- 保存草稿。
- 校验。
- 发布版本。
- 导出 UI JSON 和 API JSON。

节点仍然禁止自由拖动。

### 6.5 设置与 ComfyUI 页面

必须提供：

- 实例列表。
- 添加实例。
- 实例名称。
- HTTP 地址。
- 可选 WebSocket 地址。
- 连接超时。
- 下载超时。
- 测试连接。
- 设为活动实例。
- 同步节点定义。
- 同步模型和 LoRA 资源。
- 显示 ComfyUI 版本。
- 显示设备。
- 显示节点数量。
- 显示最后同步时间。
- 显示同步错误。

页面不得把 `192.168.3.5:8188` 写成无法编辑的静态文字。

### 6.6 顶部或侧栏连接状态

连接状态必须来自 API：

- 未配置。
- 检测中。
- 已连接。
- 不可达。
- 需要重新同步。

不得永久显示静态“尚未检测”。

---

## 7. 阻塞问题三：阶段 1 前端闭环不足

阶段 1 的部分后端和测试已经存在，但以下功能缺少完整前端入口。

### 7.1 剧本分支

补齐：

- 在大场景或小场景上创建分支。
- 编辑分支名称、说明、条件、返回点和启用状态。
- 删除分支。
- 显示分支页面。
- 编辑人物、素材和参数覆盖。
- 显示有效覆盖来源。
- 分支操作刷新后保持。

### 7.2 剧本快照

补齐：

- 创建用户命名快照。
- 快照列表。
- 查看快照时间和结构统计。
- 恢复前显示影响。
- 恢复后刷新画布。
- 恢复失败保留当前结构。

### 7.3 撤销和重做

补齐：

- 画布工具栏撤销和重做。
- 显示最近操作。
- 没有可撤销操作时按钮禁用。
- 撤销或重做后刷新目录和画布。
- 失败时显示错误，不伪装成功。

### 7.4 编译预检查

补齐：

- 从项目或剧本画布运行预检查。
- 显示阻塞、警告和涉及对象。
- 点击问题跳转到对应小场景或页面。
- 本阶段只检查，不生成跑图列表。

### 7.5 人物与素材高级入口

逐项核对阶段 1 文档，确保以下已有后端能力可以在前端使用：

- 人物预览图。
- 人物标签、搜索、归档和恢复。
- 变体预览、复制、排序和归档。
- 人物矩阵真实编辑和批量操作。
- 素材版本、恢复和引用反查。
- 素材复制、归档、回收站和永久删除。
- 素材页预览、复制和排序。

---

## 8. 移除测试数据库产品设计

### 8.1 用户界面

删除以下内容：

- “管理生产数据、测试数据和运行环境”
- “验证数据库隔离”
- “测试隔离”
- “当前使用生产数据库”
- “生产数据库目前为空”
- “生产数据库已就绪”
- 所有生产 / 测试环境名称和切换提示

用户只需要看到：

- 数据是否可用。
- 存储目录。
- 数据库健康状态。
- 备份和恢复入口。

### 8.2 前端请求

生产前端不得再调用：

```text
GET  /api/settings/databases
POST /api/settings/databases/activate
POST /api/settings/databases/verify-isolation
```

### 8.3 后端测试隔离

测试隔离能力可以保留在：

- `create_app(data_root=<temporary>, environment="test", locked_environment="test")`
- 测试基类。
- 测试辅助函数。

但它不是用户产品功能。

生产环境不应暴露数据库切换接口。可选择：

1. 完全删除生产路由；或
2. 仅在明确测试模式注册。

默认生产启动访问这些路由应返回 404。

---

## 9. ComfyUI 真实验收要求

### 9.1 阶段 2 必须完成

使用验收时识别并明确选择的真实 ComfyUI：

1. 测试连接。
2. 读取 `/system_stats`。
3. 同步 `/object_info`。
4. 显示标准节点。
5. 显示自定义节点。
6. 同步模型、LoRA 和其他资源。
7. 导入该 ComfyUI 可识别的真实工作流。
8. 编辑至少一个组件参数。
9. 修改至少一条连线。
10. 执行自动布局。
11. 设置至少一个语义插槽。
12. 导出 UI JSON。
13. 导出 API JSON。
14. 重新导入导出结果。
15. 使用当前节点定义完成校验。
16. 发布一个不可变版本。

验收报告必须记录：

- 实际使用的 ComfyUI 地址。
- ComfyUI 版本。
- 节点总数。
- 自定义节点数。
- 测试工作流名称。
- 导入前后节点和连线数量。
- 编辑前后参数值。
- 校验结果。

### 9.2 阶段 2 不执行

- 不提交 `/prompt`。
- 不等待采样进度。
- 不下载生成图片。
- 不写入图片实例。

这些操作必须明确记录为“尚未进入阶段 3”，不能写成“已完成 ComfyUI 生成闭环”。

---

## 10. 阶段 3 的第一项真实生图验收

完成本整改并通过阶段 1、阶段 2 验收后，进入阶段 3 时，不应立刻开发大批量生成。

阶段 3 的第一个纵向模块必须是：

```text
单个场景页
→ 单个已发布工作流版本
→ 单个生成任务
→ 提交当前活动 ComfyUI
→ 获得 prompt_id
→ 接收真实进度
→ 从 history 获取结果
→ 通过 /view 下载图片
→ 保存一条 Atelier 图片实例
→ 在页面上显示图片和追踪信息
```

必须使用真实 ComfyUI 实际生成至少一张图片。

只有该最小闭环通过，才能继续：

- 每页多实例。
- 多页批量。
- 持久化队列。
- 暂停、取消和重试。
- 网络断开恢复。
- 应用重启恢复。

阶段 3 首次真实生图前，编程 AI 必须再次向用户确认活动 ComfyUI 实例。

---

## 11. API 与数据迁移

### 11.1 迁移要求

- 不删除生产数据库重建。
- 迁移前备份。
- 使用已有 `schema_migrations`。
- 迁移必须幂等。
- 旧的单实例设置迁移为一个实例记录。
- 无法判断旧地址是否有效时仍保留，不自动改成另一个地址。
- 工作流原始 JSON 迁移为来源快照。
- 当前草稿不丢失。

### 11.2 兼容

- 现有工作流 ID 和版本 ID 保持。
- 现有项目默认工作流关系保持。
- 现有语义插槽保持。
- 现有节点布局保持。
- 旧 API 如需废弃，先提供兼容期和明确日志。

### 11.3 并发

工作流草稿更新必须使用 `revision`：

- 前端提交 `expected_revision`。
- 服务端不匹配返回 409。
- 不允许旧页面覆盖新编辑。
- 导出响应包含当前草稿 revision 和 checksum。

---

## 12. 自动化测试

### 12.1 第三轮回归

继续通过：

- 分支页设置映射 422。
- 分支页取消映射 422。
- 不存在页设置映射 404。
- 不存在页取消映射 404。
- 正常映射不被失败请求破坏。

### 12.2 阶段 1

新增或补齐前端契约和后端测试：

- 分支 CRUD 和覆盖。
- 快照创建和恢复。
- 撤销、重做。
- 预检查。
- 人物、素材高级功能。
- 测试数据库路由在生产模式不可见。

### 12.3 阶段 2

必须覆盖：

- 多实例保存和活动实例唯一约束。
- 实例探测。
- 同时发现两台实例时不自动切换。
- 环境变量测试地址。
- 节点同步和摘要。
- 工作流列表、创建、导入和版本。
- 全部节点编辑操作。
- 布局操作。
- 插槽。
- 编辑后导出当前值。
- UI / API JSON 往返。
- 未知字段保留。
- 发布版本不可变。

### 12.4 前端测试

至少覆盖：

- 工作流库读取真实 API。
- 空状态创建工作流。
- API 错误不会显示成空状态。
- 创建后刷新保持。
- 打开指定工作流 ID。
- 节点参数编辑保存。
- 连线编辑保存。
- 插槽编辑保存。
- 导出得到最新数据。
- 设置页切换活动实例。
- 页面不出现生产 / 测试数据库环境文字。

### 12.5 真实集成测试

运行真实测试时使用：

```bat
set ATELIER_COMFYUI_TEST_URL=http://<detected-host>:8188
.\.venv\Scripts\python.exe -m unittest backend.tests.test_comfyui_connection -v
```

实际命令可按最终测试模块调整，但必须确保真实测试没有被跳过。

---

## 13. 浏览器验收

使用独立临时数据目录和独立端口。

### 13.1 设置

1. 打开设置页。
2. 页面没有生产 / 测试数据库设计。
3. 添加或发现至少两台候选 ComfyUI（若当前均可达）。
4. 明确选择一台活动实例。
5. 测试连接。
6. 同步节点。
7. 显示真实版本、设备、节点数和同步时间。
8. 切换另一台实例。
9. 状态和节点定义更新。
10. 刷新后活动实例保持。

### 13.2 工作流库

1. 新建用户命名工作流。
2. 刷新后仍存在。
3. 导入真实 UI JSON。
4. 导入真实 API JSON。
5. 从图片提取工作流。
6. 搜索、筛选和归档。
7. 打开工作流画布。

### 13.3 工作流画布

1. 显示真实节点。
2. 显示自定义节点。
3. 编辑节点参数。
4. 修改连线。
5. 自动布局。
6. 结构化调整节点位置。
7. 设置语义插槽。
8. 保存并刷新。
9. 数据保持。
10. 导出 UI JSON 和 API JSON。
11. 导出值与编辑后值一致。
12. 重新导入成功。
13. 校验并发布版本。

### 13.4 阶段 1

1. 创建分支和覆盖。
2. 创建快照。
3. 修改结构后恢复快照。
4. 撤销和重做。
5. 运行预检查。
6. 使用人物和素材高级管理入口。

### 13.5 浏览器质量

- 控制台无错误和警告。
- 页面有滚动条。
- 1280×720 下可操作。
- 所有按钮有加载和错误反馈。
- 刷新不丢数据。
- 不显示演示工作流。
- 不显示虚构连接状态。

---

## 14. 文档与进度

开发 AI 不得自行宣布阶段通过。

开发完成后：

- 阶段 1、阶段 2 相关清单保持 `[~]`。
- 第三轮可以记录为“已通过”。
- 等独立验收通过后，再把相关功能改为 `[x]`。
- `更新日志.md` 必须区分：
  - 已开发。
  - 自动化测试通过。
  - 真实 ComfyUI 验收通过。
  - 浏览器验收通过。
  - 实际生图尚未开始。

禁止写：

- “ComfyUI 全链路已完成”，如果没有实际生图。
- “阶段 2 已完成”，如果前端仍然是静态页面。
- “真实集成测试全部通过”，如果测试被跳过。

---

## 15. 交付要求

整改完成后必须提交：

1. 修改文件列表。
2. 数据库迁移版本和备份说明。
3. ComfyUI 实例识别和选择规则。
4. 本次检测到的全部实例和实际使用实例。
5. 工作流来源快照与当前草稿的区别。
6. 编辑后导出新值的测试证明。
7. 阶段 1 前端入口清单。
8. 阶段 2 前端入口清单。
9. 新增和修改的 API。
10. 自动化测试总数、通过数、失败数和跳过数。
11. 真实 ComfyUI 测试结果。
12. 浏览器逐项验收结果。
13. 浏览器控制台结果。
14. 未完成功能。
15. 实际生图状态，必须明确写“未进入阶段 3”。
16. 分支名和提交号。

---

## 16. 完成定义

同时满足以下条件，本整改才算开发完成：

- ComfyUI 地址不再被写死为唯一地址。
- 可以保存、探测和明确选择活动实例。
- 真实测试使用本次实际选择的实例。
- 工作流库接入真实 API。
- 新建、导入、复制、归档和删除真实可用。
- 工作流画布读取真实工作流。
- 标准节点和自定义节点真实显示。
- 节点参数和连线真实可编辑。
- 节点仍然禁止自由拖动。
- 自动布局和结构化排序真实可用。
- 语义插槽真实可编辑。
- 编辑后导出的是最新数据。
- 未知字段得到保留。
- 发布版本不可变。
- 阶段 1 的分支、覆盖、快照、撤销、重做和预检查有真实前端。
- 用户界面不出现测试数据库设计。
- 生产模式不暴露数据库切换接口。
- 全部自动化测试通过。
- 真实 ComfyUI 连接与工作流验收通过。
- 全部浏览器验收通过。
- 控制台无错误或警告。
- 文档没有声称已经实际生图。

任意一项不满足，阶段 1、阶段 2均不得标记为完成，不得合并到 `main`。

---

## 17. 下一阶段说明

本整改通过后，停止开发并请求用户确认是否进入阶段 3。

阶段 3 的第一项任务必须是“单页面、单任务、单张真实图片生成闭环”，而不是直接进行大规模批量开发。

只有通过 Atelier 实际获得 `prompt_id`、跟踪进度、读取 history、下载图片并写入图片实例，才能首次写：

```text
Atelier 已完成真实 ComfyUI 生图测试。
```

