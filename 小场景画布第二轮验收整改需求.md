# Atelier 小场景画布第二轮验收整改需求

版本：1.0  
日期：2026-07-29  
状态：待开发  
验收失败基线：`GLM-MAIN`，提交 `0f1738d`  
项目路径：`C:\Users\kono707da\Documents\AICode\atelier`

---

## 1. 文档用途

本文件是《小场景画布前后端联调验收整改需求》的第二轮整改补充。

上一轮实现已经打通主要页面，但仍存在数据契约、关联约束和数据清理问题。本轮只修复已经实测确认的问题，不重做现有 UI，不扩展其他业务模块。

完成本文件全部条目并通过重新验收后，才允许将“小场景画布前后端闭环”标记为完成。

---

## 2. 第二轮验收结论

验收结果：**不通过**。

已通过：

- 346 项后端自动化测试全部通过。
- `design/ui-preview/app.js` 语法检查通过。
- `design/ui-preview/runtime-api.js` 语法检查通过。
- 浏览器可以进入小场景画布。
- 场景页新增、修改、删除、排序可以从页面操作。
- 素材关联、素材页映射、同类型映射替换可以从页面操作。
- 排序和映射刷新后仍然存在。
- 浏览器控制台没有错误或警告。

未通过：

1. `story-tree` 没有返回素材、素材页和统计字段。
2. 左侧目录在素材已关联后仍显示“关联素材 0”。
3. 未关联到小场景的素材页可以直接映射。
4. 移除素材关联后，相关页面映射没有删除。
5. 页面出现“素材 · 未命名页”的孤立映射。
6. 场景页排序接口接受缺失、重复和其他小场景的页面 ID。
7. 场景页排序不是单事务操作。
8. 创建、关联、映射接口的状态码和返回结构不符合既定契约。
9. `PUT` 映射接口不接受 `material_page_id: null`。
10. 旧素材关联接口可能产生没有 `link_id` 的关联记录。

---

## 3. 本轮开发范围

必须修改：

- `backend/app/database.py`
- `backend/app/app_factory.py`
- 对应后端测试文件
- 必要时小范围修改 `design/ui-preview/runtime-api.js`
- `更新日志.md`

原则上不得修改：

- 剧本画布现有布局
- 小场景画布现有布局
- 苹果明亮风格
- 左侧目录结构
- 场景页卡片样式
- 素材页只读展示方式
- 其他已能使用的页面

本轮不是 UI 重构任务。

---

## 4. 必须修复：项目剧本树

接口：

```http
GET /api/projects/{project_id}/story-tree
```

### 4.1 小场景必须返回的字段

每个 `small_scene` 必须至少包含：

```json
{
  "id": "small-scene-id",
  "large_scene_id": "large-scene-id",
  "name": "小场景名称",
  "scene_type": "content",
  "description": "",
  "sort_order": 1,
  "page_count": 10,
  "resource_count": 2,
  "pages": [],
  "resources": []
}
```

### 4.2 `resources` 字段

每个关联素材必须包含：

```json
{
  "link_id": "link-id",
  "material_id": "material-id",
  "name": "素材名称",
  "material_type": "expression",
  "sort_order": 1,
  "page_count": 4,
  "pages": [
    {
      "id": "material-page-id",
      "material_id": "material-id",
      "name": "素材页名称",
      "description": "",
      "sort_order": 1
    }
  ]
}
```

### 4.3 查询要求

- 必须按 `sort_order ASC` 返回章节、大场景、小场景、场景页、关联素材和素材页。
- 使用批量查询组装结果。
- 不允许对每个章节、大场景或小场景逐条建立连接并查询。
- `page_count` 必须等于 `pages.length`。
- `resource_count` 必须等于 `resources.length`。
- 没有数据时返回空数组和数字 `0`，不得省略字段或返回 `null`。

### 4.4 前端结果

素材关联成功后返回项目结构：

- 左侧“关联素材”数量必须立即正确。
- 左侧必须显示关联素材及其素材页数量。
- 中间画布的小场景卡片必须显示正确关联素材数量。
- 刷新后数量保持一致。

---

## 5. 必须修复：素材页映射的关联校验

接口：

```http
PUT /api/small-scene-pages/{scene_page_id}/mappings/{material_type}
```

设置映射前必须在同一事务中完成以下校验：

1. 场景页存在。
2. 场景页是小场景直属页，不是其他层级的错误页面。
3. 素材页存在。
4. 素材页所属素材存在。
5. 素材真实类型与 URL 中的 `material_type` 一致。
6. 素材已经通过 `small_scene_materials` 关联到该场景页所属的小场景。
7. 同一场景页、同一素材类型最多保留一个映射。

未关联素材的素材页不得映射，返回：

```http
422 Unprocessable Entity
```

建议错误信息：

```json
{
  "detail": "该素材尚未关联到场景页所属的小场景"
}
```

不得因为素材类型正确就跳过小场景关联校验。

---

## 6. 必须修复：移除素材关联

接口：

```http
DELETE /api/small-scene-resource-links/{link_id}
```

必须在一个数据库事务内完成：

1. 根据 `link_id` 找到 `small_scene_id` 和 `material_id`。
2. 找到该小场景的全部直属场景页。
3. 删除这些场景页中引用该素材任意素材页的全部映射。
4. 删除 `small_scene_materials` 关联行。
5. 保留素材。
6. 保留素材页。

禁止仅删除关联行而留下页面映射。

成功返回：

```json
{
  "database_environment": "production",
  "deleted": {
    "link_id": "link-id",
    "small_scene_id": "small-scene-id",
    "material_id": "material-id",
    "deleted_mapping_count": 2
  }
}
```

移除完成后：

- `workspace.resources` 不再包含该素材。
- `workspace.mappings` 不再包含该素材的映射。
- 场景页不得显示“素材 · 未命名页”。
- 素材库中原素材和素材页仍然存在。

---

## 7. 必须修复：场景页排序

接口：

```http
PUT /api/small-scenes/{small_scene_id}/pages/order
```

请求：

```json
{
  "page_ids": [
    "page-id-2",
    "page-id-1",
    "page-id-3"
  ]
}
```

### 7.1 完整集合校验

`page_ids` 必须与该小场景全部直属场景页 ID 集合完全一致。

以下请求全部返回 `422`：

- 缺少任意页面 ID。
- 同一页面 ID 重复出现。
- 包含其他小场景的页面 ID。
- 包含分支页面 ID。
- 包含不存在的页面 ID。
- 空数组。

### 7.2 单事务排序

- 排序校验和全部 `sort_order` 更新必须在一个事务中完成。
- 任意校验或写入失败时，原顺序必须完整保留。
- 不得在路由中循环调用每次独立提交的 `move_shot_page`。
- 排序成功后，`sort_order` 必须从 1 开始连续递增。

成功返回：

```json
{
  "database_environment": "production",
  "small_scene_id": "small-scene-id",
  "pages": []
}
```

---

## 8. 必须修复：API 状态码与返回结构

以下契约作为唯一标准。

### 8.1 创建场景页

```http
POST /api/small-scenes/{small_scene_id}/pages
```

成功状态码：`201`

```json
{
  "database_environment": "production",
  "page": {
    "id": "page-id",
    "small_scene_id": "small-scene-id",
    "name": "第 1 页",
    "description": "",
    "sort_order": 1
  }
}
```

### 8.2 修改场景页

```http
PATCH /api/small-scene-pages/{page_id}
```

成功状态码：`200`

```json
{
  "database_environment": "production",
  "page": {}
}
```

### 8.3 关联素材

```http
POST /api/small-scenes/{small_scene_id}/resources
```

成功状态码：`201`

```json
{
  "database_environment": "production",
  "resource": {
    "link_id": "link-id",
    "material_id": "material-id",
    "name": "素材名称",
    "material_type": "expression",
    "pages": []
  }
}
```

同一素材重复关联返回 `409`。

### 8.4 设置映射

成功状态码：`200`

```json
{
  "database_environment": "production",
  "mapping": {
    "id": "mapping-id",
    "scene_page_id": "scene-page-id",
    "material_page_id": "material-page-id",
    "material_type": "expression"
  }
}
```

### 8.5 取消映射

同一个 `PUT` 接口必须支持：

```json
{
  "material_page_id": null
}
```

成功返回：

```json
{
  "database_environment": "production",
  "mapping": null
}
```

现有 `DELETE` 取消接口可以保留兼容，但前端和测试必须以 `PUT + null` 的唯一契约为准。

### 8.6 前端兼容

修改返回结构后，必须同步检查 `runtime-api.js`。

页面不得因为从平铺结果改为 `page`、`resource`、`mapping` 包裹而失效。

---

## 9. 必须修复：关联记录 `link_id`

`small_scene_materials.id` 是稳定的素材关联 ID。

必须满足：

- 所有现存关联行都有非空 `id`。
- 所有新增关联行都生成 UUID。
- `id` 全局唯一。
- `workspace.resources[*].link_id` 永远是非空字符串。
- `story-tree.resources[*].link_id` 永远是非空字符串。

### 9.1 旧接口兼容

必须修复：

```http
PUT /api/small-scenes/{small_scene_id}/materials
```

不得继续使用“全部删除后重新插入且不写 `id`”的实现。

建议使用差量更新：

- 仍保留的素材关联保留原 `id`。
- 新增素材关联生成新 `id`。
- 被移除素材的相关页面映射一并删除。
- 最后重新写入连续 `sort_order`。

### 9.2 迁移修复

数据库初始化时必须幂等修复历史空值：

- 查找 `small_scene_materials.id IS NULL` 或空字符串的行。
- 为每行生成唯一 UUID。
- 重复初始化不得再次修改已有有效 ID。

---

## 10. 数据库一致性要求

- 所有关联清理依赖真实外键关系和明确 SQL，不依赖前端隐藏数据。
- 删除场景页必须级联删除该页映射。
- 删除素材页必须级联删除引用该素材页的映射。
- 删除素材必须级联删除素材页、场景关联和全部相关映射。
- 移除小场景素材关联只删除该小场景内的相关映射。
- 不得误删同一素材在其他小场景中的关联或映射。
- 所有写操作失败时必须回滚。

---

## 11. 必须新增的自动化测试

不能只修改实现，以下测试必须补齐。

### 11.1 `story-tree`

- 返回 `page_count`。
- 返回 `resource_count`。
- 返回 `resources`。
- 返回每个素材的 `pages` 和 `page_count`。
- 素材、素材页和场景页排序正确。
- 无素材时返回 `resources: []` 和 `resource_count: 0`。

### 11.2 映射校验

- 已关联素材可以映射。
- 未关联素材返回 `422`。
- 关联到另一个小场景的素材不能映射到当前小场景。
- 素材类型不一致返回 `422`。
- 同类型新映射替换旧映射。
- 一个素材页可以映射到多个场景页。
- `PUT material_page_id: null` 成功取消映射。

### 11.3 移除关联

- 移除关联删除该素材在当前小场景的全部映射。
- 不删除素材和素材页。
- 不删除同一素材在其他小场景的映射。
- 返回正确的 `deleted_mapping_count`。
- 页面工作区不再返回孤立映射。

### 11.4 排序

- 完整合法集合排序成功。
- 缺失 ID 返回 `422`。
- 重复 ID 返回 `422`。
- 跨小场景 ID 返回 `422`。
- 分支页面 ID 返回 `422`。
- 不存在 ID 返回 `422`。
- 失败后原顺序不变。
- 成功后编号连续。

### 11.5 状态码与响应契约

- 创建场景页返回 `201` 和 `page`。
- 修改场景页返回 `page`。
- 关联素材返回 `201` 和 `resource`。
- 重复关联返回 `409`。
- 设置映射返回 `mapping`。
- 取消映射返回 `mapping: null`。

### 11.6 `link_id`

- 新接口创建的关联拥有非空 `link_id`。
- 旧批量接口创建的关联拥有非空 `link_id`。
- 历史空 ID 初始化后被修复。
- 重复初始化不改变已有 ID。

### 11.7 测试质量

禁止只检查路由字符串存在。

测试必须检查：

- HTTP 状态码。
- JSON 完整结构。
- 数据库最终状态。
- 刷新读取结果。
- 失败后的回滚结果。

---

## 12. 浏览器重新验收流程

使用独立临时数据目录启动应用，不得写入正式生产数据库。

完整执行：

1. 创建 1 个临时项目。
2. 创建 1 个章节。
3. 创建 1 个大场景。
4. 创建 2 个小场景。
5. 创建 10 个场景页。
6. 编辑第 1 页。
7. 删除 1 页并确认编号连续。
8. 调整顺序并刷新确认。
9. 创建 1 个四页表情素材。
10. 创建 1 个两页场景素材。
11. 将两个素材关联到第一个小场景。
12. 返回项目结构。
13. 左侧目录必须显示 2 个关联素材。
14. 左侧目录必须显示素材页数量。
15. 中间小场景卡片必须显示 2 个关联素材。
16. 双击重新进入小场景。
17. 必须只读显示共 6 个素材页。
18. 将表情素材页 1 映射到场景页 1 和场景页 2。
19. 将场景素材页 1 映射到场景页 1。
20. 场景页 1 必须同时显示两种素材映射。
21. 将场景页 1 的表情映射替换为表情素材页 2。
22. 取消场景页 2 的表情映射。
23. 刷新并确认全部结果保留。
24. 移除表情素材关联。
25. 表情映射必须全部消失。
26. 场景素材映射必须继续存在。
27. 页面不得出现“素材 · 未命名页”。
28. 素材库中的表情素材及四个素材页必须仍然存在。
29. 浏览器控制台不得出现错误或警告。

额外负向验收：

1. 使用接口尝试映射未关联素材页，必须返回 `422`。
2. 使用接口提交缺失页面的排序数组，必须返回 `422`。
3. 使用接口提交重复页面的排序数组，必须返回 `422`。
4. 使用接口提交其他小场景页面 ID，必须返回 `422`。
5. 每次失败后重新读取，原数据必须保持不变。

---

## 13. 回归测试

必须全部通过：

```bat
node --check design\ui-preview\app.js
node --check design\ui-preview\runtime-api.js
.\.venv\Scripts\python.exe -m unittest discover -s backend\tests -p "test_*.py" -q
```

还必须确认：

- 项目、章节和大场景功能无回归。
- 小场景创建、改名和删除无回归。
- 素材库 CRUD、筛选、标签和预览图无回归。
- 人物库无回归。
- 开发者管理页面可滚动。
- 正式生产数据库没有验收测试数据。

---

## 14. 开发进度与更新日志

在浏览器端到端验收通过前：

- 不得把相关功能改为 `[x]`。
- 不得在更新日志中写“真实闭环已经完成”。
- 不得声称验收通过。

验收通过后才允许更新：

- `系统功能清单.md`
- `更新日志.md`

更新日志必须记录真实测试总数和浏览器验收结果。

---

## 15. 交付要求

编程 AI 完成后必须提供：

1. 修改文件列表。
2. 每个失败项对应的修复说明。
3. 数据库迁移说明。
4. API 状态码和响应结构清单。
5. 新增测试名称与数量。
6. 完整自动化测试结果。
7. 浏览器端到端验收记录。
8. 临时数据库路径及清理结果。
9. 生产数据库未被写入的证明。
10. 开发分支名称和提交号。
11. 仍未完成的功能。

开发分支必须遵守项目 `AGENTS.md`。

未通过重新验收前，不允许主动合并到 `main`。

---

## 16. 完成定义

同时满足以下条件才算完成：

- `story-tree` 返回完整页面和素材目录数据。
- 左侧目录素材数量与数据库一致。
- 中间小场景卡片素材数量与数据库一致。
- 未关联素材无法建立页面映射。
- 移除素材关联会删除当前小场景内相关映射。
- 不再出现孤立映射。
- 场景页排序严格校验完整集合。
- 排序在单事务中完成。
- 所有接口状态码符合本文。
- 所有接口响应结构符合本文。
- `PUT material_page_id: null` 可以取消映射。
- 所有素材关联都有稳定非空 `link_id`。
- 新增负向测试全部通过。
- 原有自动化测试全部通过。
- 浏览器完整流程全部通过。
- 浏览器控制台无错误或警告。
- 生产数据库没有测试数据。

任意一项不满足，均不得标记为验收通过。
