# Atelier 人物库整改开发指导（单数据库及问题 3–6）

状态：待开发
适用范围：上次人物库验收问题 3、4、5、6
数据库决策：产品只保留一个生产数据库

## 1. 开发目标

本轮只完成以下整改：

1. 将人物永久删除改为归档和恢复。
2. 彻底移除人物页演示数据。
3. 完成人物新建、查看和编辑表单。
4. 修复人物创建后页面空白及 SQLite `unable to open database file`。
5. 移除产品中的测试数据库设计。

本轮不得继续开发形象变体、景别规格、LoRA、全局人物模板、人物替换矩阵或人物与其他模块的联动。

## 2. 单数据库改造

### 2.1 唯一正式数据库

继续使用现有文件，避免无意义且危险的文件迁移：

```text
data/databases/atelier.production.sqlite3
```

代码和界面统一将其称为“数据库”或“生产数据库”，不得再出现：

- 测试数据库。
- 数据库环境切换。
- `production/test` 选择器。
- `locked_environment`。
- “验证双库隔离”功能。
- `atelier.test.sqlite3`。

### 2.2 必须删除的产品功能

- 删除 `start-test.bat`。
- `start.bat` 固定启动唯一数据库，不再接收环境参数。
- 删除 `/api/settings/databases/activate`。
- 删除 `/api/settings/databases/verify-isolation`。
- `/api/settings/databases` 改成单库信息接口，或在没有其他必要信息时直接删除。
- 设置页删除测试数据库卡片、数据库切换按钮和双数据库说明。
- 健康检查只返回服务及数据库可用状态，不再返回环境和锁定状态。

推荐健康检查响应：

```json
{
  "status": "ok",
  "service": "atelier",
  "database": "available"
}
```

### 2.3 测试方式

取消测试数据库不等于允许测试写正式数据库。

自动化测试必须：

- 使用 `TemporaryDirectory`。
- 显式把临时 `data_root` 传给 `create_app`。
- 在临时目录中创建同结构的一次性 SQLite 文件。
- 测试结束后由系统自动删除。
- 不读取、复制或写入正式数据库。
- 不在产品设置页展示临时数据库。

浏览器端到端测试必须由测试脚本完成：

1. 创建系统临时目录。
2. 在空闲临时端口启动 Atelier，并把 `data_root` 指向该目录。
3. 完成人物创建、编辑、归档、恢复和封面测试。
4. 停止临时服务。
5. 删除临时目录。

不得把固定 8111 端口和第二套数据库重新做成产品功能。

### 2.4 生产数据保护

改造前必须记录：

- 数据库 SHA256、大小、修改时间。
- 项目、章节、大场景的数量、ID、名称和排序。
- 当前人物相关表结构及记录数量。

迁移前创建带时间戳的备份：

```text
data/backups/atelier.production.before-character-fix.YYYYMMDD-HHMMSS.sqlite3
```

迁移要求：

- 使用单个事务。
- 失败必须完整回滚。
- 不得删除项目、章节或大场景数据。
- 不得用启动测试的方式验证迁移。
- 备份验证可打开后，才能执行正式迁移。

## 3. 问题 3：人物归档和恢复

### 3.1 数据结构

`characters` 表必须包含：

```sql
archived_at TEXT
```

人物默认处于未归档状态，即 `archived_at IS NULL`。

### 3.2 接口

归档：

```http
DELETE /api/characters/{character_id}
```

行为：

- 不执行 SQL `DELETE`。
- 把 `archived_at` 写为当前 UTC 时间。
- 已归档人物再次归档保持幂等。
- 返回归档后的完整人物资料。

恢复：

```http
POST /api/characters/{character_id}/restore
```

行为：

- 把 `archived_at` 清空。
- 未归档人物再次恢复保持幂等。
- 返回恢复后的完整人物资料。

人物列表：

```http
GET /api/projects/{project_id}/characters?archived=false
```

- 默认只返回未归档人物。
- `archived=true` 只返回已归档人物。
- 不实现永久删除入口。

### 3.3 前端

- 所有“删除人物”文字改为“归档人物”。
- 归档前显示二次确认。
- 确认文案必须说明人物可恢复。
- 默认列表归档成功后立即移除该人物。
- “查看已归档”开启后显示“恢复”按钮。
- 恢复成功后人物从归档列表移除，并重新出现在默认列表。

### 3.4 测试

- 归档后数据库记录仍存在。
- 默认列表不返回已归档人物。
- 已归档列表正确返回。
- 恢复后重新出现在默认列表。
- 重复归档和重复恢复均成功。
- 项目删除时人物仍按外键规则级联删除。

## 4. 问题 4：彻底移除演示人物

### 4.1 删除静态数据

必须删除 `app.js` 中 `charactersPage()` 的：

- 角色 A、角色 B、角色 C、角色 D。
- 虚假人物类型和完整度。
- 虚假分镜引用数量。
- 虚假变体、规格和 LoRA 状态。
- “从项目复制”。
- “打开替换矩阵”。
- “全局人物模板”。

`charactersPage()` 只能输出人物页结构和加载状态，不得输出任何人物卡片。

### 4.2 页面状态

人物页必须明确处理四种状态：

1. 加载中：显示骨架或“正在加载人物库”。
2. 空数据：显示“还没有人物”和“新建人物”。
3. 正常数据：显示真实人物卡片。
4. 加载失败：显示错误说明和“重新加载”按钮。

加载失败时：

- 不得回退到演示数据。
- 不得留下只有标题的空白页面。
- 不得覆盖用户当前输入。
- 控制台不得出现未处理异常。

### 4.3 项目范围

人物页面必须读取 URL 中的 `project`：

```text
?page=characters&project={project_id}
```

请求必须使用：

```http
GET /api/projects/{project_id}/characters
```

项目不存在或 URL 缺少项目 ID 时，显示明确错误和“返回项目中心”，不得加载全局人物。

### 4.4 测试

- API 响应前 DOM 中不存在角色 A/B/C/D。
- 空项目不显示演示人物。
- API 返回 500 时显示错误状态。
- 点击重新加载后可以恢复。
- 不存在虚假引用、规格、变体和 LoRA 文案。

## 5. 问题 5：完整人物资料表单

### 5.1 表单字段

新建和编辑共用一个弹窗或右侧面板，必须包含：

| 字段 | 必填 | 限制 |
|---|---:|---|
| 人物名称 | 是 | 最长 80 字符 |
| 人物类型 | 是 | `lead/major/supporting/cameo/other` |
| 人物说明 | 否 | 最长 2,000 字符 |
| 基础提示词 | 否 | 最长 20,000 字符 |
| 标签 | 否 | 每项 40 字符，最多 50 项 |
| 备注 | 否 | 最长 5,000 字符 |
| 封面图 | 否 | JPEG、PNG、WebP，最大 25 MB |

### 5.2 新建流程

1. 用户填写资料。
2. 前端完成基础校验。
3. 提交人物资料。
4. 后端成功创建人物并返回 ID。
5. 如果选择了封面，再上传到该人物 ID。
6. 上传封面失败时人物资料保留，并在表单内提示重试。
7. 全部完成后重新读取人物列表。

禁止使用创建人物后自动生成“默认变体”的逻辑。

### 5.3 编辑流程

- 打开编辑表单时读取人物完整资料。
- `PATCH` 只提交发生变化的字段。
- 标签提交完整列表，由后端在事务内同步。
- 封面上传、替换和移除使用独立接口。
- 保存期间禁用提交按钮，避免重复请求。
- 后端错误显示在表单内，不只显示短暂 Toast。
- 保存成功后重新请求详情和列表，不直接假定本地状态正确。

### 5.4 封面

- 列表只加载缩略图。
- 缩略图使用 `loading="lazy"`。
- 没有封面或加载失败时显示人物名称首字。
- 原图只在明确查看时加载。
- 上传前后端都检查 25 MB 限制。
- 后端必须用 Pillow 解码验证真实图片内容。

### 5.5 测试

- 所有字段可以创建、读取和编辑。
- 非法人物类型返回 422。
- 字段长度限制正确。
- 标签清理、大小写去重及数量限制正确。
- 封面上传、替换、移除和失败回滚正确。
- 重复点击保存不会创建重复人物。

## 6. 问题 6：SQLite 和页面刷新稳定性

### 6.1 已知故障

验收曾复现：

- 创建人物接口返回 201。
- 随后的列表请求返回 500。
- 页面人物区域变成空白。
- 后端堆栈为 `sqlite3.OperationalError: unable to open database file`。

该问题修复前不得标记人物库完成。

### 6.2 SQLite 配置

项目位于映射盘或网络共享路径时，不得使用 WAL：

```sql
PRAGMA journal_mode = DELETE;
PRAGMA synchronous = FULL;
PRAGMA foreign_keys = ON;
PRAGMA busy_timeout = 5000;
```

原因：WAL 依赖共享内存文件，不适合普通网络文件系统。

实现要求：

- 数据库连接创建统一封装在一个方法中。
- 每次连接都设置外键和 busy timeout。
- 写操作统一经过应用级 `RLock`。
- 单次业务操作只使用一个连接和一个事务。
- 不在一个列表请求中为每个人物再次打开统计连接。
- 人物列表所需字段必须通过单条查询或同一连接内的查询获得。
- 所有连接都必须在 `finally` 中关闭。

### 6.3 启动检查

启动时依次检查：

1. 数据库父目录存在。
2. 当前用户可以读写目录。
3. 数据库可以连接。
4. `SELECT 1` 成功。
5. 数据库迁移成功。
6. 再启动 HTTP 服务。

检查失败时停止启动，显示英文错误并返回非零退出码；不得启动一个健康检查正常但业务接口不可用的服务。

BAT 脚本仍不得出现中文。

### 6.4 错误处理

- 数据库不可用时业务接口返回 503，不返回未处理的 500。
- 响应中使用稳定错误码，例如 `database_unavailable`。
- 日志记录异常类型、接口和数据库路径，但不得记录人物提示词等用户内容。
- 前端收到 503 后显示可重试错误状态。

### 6.5 稳定性测试

至少增加：

- 创建人物后立即读取列表 50 次。
- 创建人物后立即读取详情 50 次。
- 连续创建、编辑、列表刷新循环 50 次。
- 并发发起 10 个只读人物列表请求。
- `/api/settings/databases` 若保留，连续请求 50 次均为 200。
- 模拟数据库目录不可用时返回 503。
- 创建后页面不得空白。
- 浏览器控制台无错误或警告。

## 7. 开发顺序

必须按以下顺序开发：

1. 备份并记录生产数据基线。
2. 移除双数据库产品设计。
3. 修复数据库连接和网络共享稳定性。
4. 完成人物基础数据模型和迁移。
5. 完成归档与恢复。
6. 完成完整人物表单和封面。
7. 移除全部演示人物。
8. 补齐自动化测试。
9. 使用临时目录完成浏览器端到端验收。
10. 核对生产业务记录并更新开发日志。

数据库稳定性未通过前，不得先做前端视觉优化。

## 8. 最终验收

必须同时满足：

- 产品中只有一个数据库。
- 只有 `start.bat` 正式启动入口。
- 设置页没有测试数据库和切换功能。
- 人物是项目级资源。
- 人物可完整创建、编辑、归档和恢复。
- 人物封面和数据库标签可用。
- 页面从首次渲染开始就没有演示人物。
- 创建人物后列表立即正常刷新。
- 连续稳定性测试无 500。
- 自动化和浏览器测试只使用临时目录。
- 正式项目、章节和大场景记录与开发前一致。
- `DEVELOPMENT_LOG.md` 记录迁移、测试、浏览器验收和生产数据核对结果。

任意一项不满足，人物库不得标记为完成。
