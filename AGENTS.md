# AGENTS.md

本文件记录 Atelier 项目中与全局规则（user_rules）冲突的项目特有规则。当项目规则与全局规则冲突时，以本文件为准。

## 分支合并策略

### 全局规则
> 开始修改代码的时候需要更新 main 分支，并且从 main 分支新建一个全新的分支进行开发变更...提交推送成功后需要合并到 main 分支

### 项目规则（覆盖全局规则）
- **不允许主动合并到 main 分支**。开发分支推送后即停止，等待用户明确指示是否合并。
- 新建一个全新的分支进行开发变更时，从 main 分支拉取最新代码新建分支，分支命名格式保持全局规则：`[开发类型(dev/fix)-日期-模型-功能简述]`。
- 所有开发分支的改动统一合并到 `GLM-MAIN` 分支供用户测试，不直接合并到 `main`。
- `GLM-MAIN` 分支作为测试分支，用户测试通过后由用户决定是否合并到 `main`。

### 操作流程
1. 从 `main` 拉取最新代码
2. 新建开发分支：`dev-YYMMDD-模型-功能简述` 或 `fix-YYMMDD-模型-功能简述`
3. 开发、测试、提交
4. 推送开发分支到远程
5. 将开发分支合并到 `GLM-MAIN` 分支并推送
6. **不主动合并到 `main`**，等待用户指示
