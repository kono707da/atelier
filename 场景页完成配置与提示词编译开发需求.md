# Atelier 场景页完成配置与提示词编译开发需求

版本：v1.0  
日期：2026-08-02  
状态：待开发  
适用范围：剧本画布的小场景页、人物库、素材映射、项目编译

## 1. 开发目标

在现有“小场景画布 + 素材页映射”的基础上，补齐一张场景页从内容整理到可编译跑图之间缺失的配置能力。

完成后，用户应当能在小场景画布内完成以下闭环：

1. 选择一张场景页。
2. 为该页绑定一个主要人物、人物形象和公共规格。
3. 填写该页专属的正向提示词与负向提示词。
4. 查看并管理场景、构图、表情、光线等素材页映射。
5. 查看页面是否已经具备编译条件，以及尚缺少什么。
6. 将人物、页级提示词和素材提示词正确编译到 ComfyUI 工作流语义插槽。
7. 在未设置项目默认工作流时得到明确提示，并能直接前往工作流配置，而不是在编译后才看到含糊错误。

本需求不是新增一套独立编辑器，而是补全现有小场景画布中的“页面详情”功能。

## 2. 当前问题与验收基线

当前生产数据中的示例页面：

- 项目：`e562a1e2-393f-4cb5-87fd-b56071a9271c`
- 小场景：`522a6766-3ae8-4d26-82a1-1eb72f0270e0`
- 场景页：`d120697d-3ab3-4d58-86ff-7696b503d1b0`
- 页面名称：`跪姿主动控制`

该页已经映射四类素材：场景、构图、表情、光线。现有预检查仍报告：

- `missing_character`：场景页未绑定人物。
- `missing_prompt`：场景页正向提示词为空。

实际编译还会报告：

- `no_workflow`：项目未设置默认工作流。

已经确认的代码问题：

1. 后端已存在 `/api/shot-pages/{id}/character`，但小场景画布没有人物绑定入口。
2. `shot_pages` 已有 `prompt_text` 和 `negative_prompt`，但现有页面编辑表单没有展示这两个字段。
3. 人物绑定只保存人物和形象，没有保存当前页选中的公共规格。
4. 编译器虽然读取 `shot_pages.prompt_text`，但正向提示词语义插槽没有使用它。
5. 编译器先构造 `material_values`，随后才追加人物规格提示词，导致后追加内容不会进入已经构造好的素材提示词。
6. 当前逻辑会读取形象下的全部规格；即使修复追加顺序，也不能把正面全身、侧面全身、背面、半身、面部等互相冲突的规格全部写入同一张图。
7. 素材页的 `negative_prompt` 没有汇总到最终负向提示词语义插槽。
8. 前端预检查使用了 `small-scene`、`large-scene` 等连字符值，并提交 `small_scene_id` 等字段；后端实际要求 `small_scene`、`large_scene` 和统一的 `scope_id`。必须一并修复。

## 3. 核心业务规则

### 3.1 一页绑定一个主要人物

本轮延续现有数据模型：一张场景页只绑定一个“主要人物”。其他次要人物可由页级提示词描述，本轮不扩展为多人角色绑定表。

绑定内容必须包含：

- 人物 `character_id`
- 形象 `variant_id`
- 公共规格 `spec_id`

三者必须形成有效关系：

- 形象必须属于所选人物。
- 规格必须是全局公共规格定义。
- 所选形象下必须存在该规格对应的 `character_spec_values` 记录。

### 3.2 规格是公共概念，规格值属于形象

规格名称是公共的，例如“正面全身”“正面半身”。不同形象分别维护同一规格对应的提示词值。

场景页应保存 `spec_id`，不要保存 `character_spec_value_id`。这样用户更换形象时仍保留“使用正面半身规格”的业务含义，再由后端根据 `variant_id + spec_id` 解析对应规格值。

若新形象没有该规格值：

- 前端立即显示“该形象尚未填写此规格”。
- 不允许把不存在的组合保存为有效绑定。
- 不得静默回退到其他规格。

### 3.3 页级提示词只保存当前画面的专属内容

场景页提供两个字段：

- 页级正向提示词：当前页的角色数量、次要人物、主要动作、人物位置关系及其他只属于这一页的内容。
- 页级负向提示词：当前页特有的错误角色数量、角色位置互换、动作错误、遮挡或裁切等排除内容。

人物固定外貌从所选人物形象和规格解析；场景、构图、表情、光线从素材映射解析。用户不应被迫在页级提示词中重复这些可复用内容。

页级字段必须原样保存用户输入，包括换行和以 `--` 开头的分段行。不得把 `--` 当作注释删除。

### 3.4 素材映射规则保持不变

一张场景页对每种素材类型最多映射一个素材页。现有场景、构图、表情、光线映射数据必须完全兼容，不迁移、不复制、不丢失。

本轮不修改素材库的新建、编辑和素材页管理逻辑。

## 4. 前端开发要求

### 4.1 页面详情布局

在用户进入小场景并选中某一页后，在画布主区域显示该页的详情区。不得恢复已删除的固定右栏。

详情区按以下顺序展示：

1. 页面名称与内容说明。
2. 主要人物配置。
3. 页级提示词。
4. 已关联素材及其页映射。
5. 完成状态与下一步操作。

保持现有明亮苹果风：圆角、简洁、低噪音，不增加大面积说明文字。高级技术信息默认不展开。

### 4.2 主要人物配置

提供三级联动选择：

1. 人物
2. 形象
3. 规格

交互要求：

- 人物默认不替用户猜测；已有绑定时显示保存值。
- 选择人物后，只加载该人物未归档的形象。
- 优先选中该人物的默认形象；如果是编辑已有绑定，不覆盖已有形象。
- 选择形象后，只显示该形象已经填写规格值的公共规格。
- 每一级变化都要清除已经失效的下级选择。
- 点击“保存人物配置”后持久化；失败时保留用户当前选择并显示具体错误。
- 提供“解除绑定”，需要二次确认；解除人物不得删除人物库或规格数据。
- 保存成功后显示紧凑只读摘要，并提供“修改”入口。

### 4.3 页级提示词编辑

提供：

- “页级正向提示词”多行输入框。
- “页级负向提示词”多行输入框。
- 保存按钮。
- 未保存状态提示。

要求：

- 支持长文本和换行，不把英文逗号、下划线或 `--` 分段行改写。
- 保存后默认切换为完整展示内容的只读框，不使用只能看到少量文字的单行截断。
- 只读状态提供“编辑”按钮。
- 离开当前页、切换场景页或刷新前，如存在未保存内容，应提示用户。
- 不接入 AI 自动编写，不自动覆盖用户内容。

### 4.4 页面完成状态

每张场景页卡片显示一个轻量状态：

- 未完成：缺少必要配置。
- 可编译：页面内容完整，但项目可能尚未设置工作流。
- 可跑图：页面内容完整且存在可用的项目默认工作流。

选中页面后展示检查项：

- 页面名称已填写。
- 页级正向提示词已填写。
- 主要人物、形象、规格有效。
- 至少关联一个素材页。
- 项目默认工作流存在且当前版本可用。

每个未完成项必须提供可执行入口：聚焦对应输入区，或导航到工作流页。不要显示没有行为的“下一步：……”提示文案。

### 4.5 默认工作流提示

当页面内容已经完整但项目没有默认工作流时：

- 明确显示“项目尚未设置默认工作流”。
- 提供“前往工作流”按钮，保留当前 `project` 参数。
- 不在本模块内创建虚假工作流或自动选择任意工作流。

## 5. 后端与数据开发要求

### 5.1 数据结构

为 `shot_page_characters` 增加可空字段：

```text
spec_id TEXT NULL
```

并增加外键或等价业务校验，确保引用 `specs.id`。

迁移要求：

- 现有记录原样保留，`spec_id` 默认为空。
- 数据库启动迁移可重复执行。
- 不删除或重建用户现有项目、人物、形象、规格、素材和映射数据。
- 旧数据在未选择规格前仍可读取，但页面完成检查应提示补充规格。

### 5.2 人物绑定 API

扩展现有接口：

```http
GET /api/shot-pages/{shot_page_id}/character
PUT /api/shot-pages/{shot_page_id}/character
DELETE /api/shot-pages/{shot_page_id}/character
```

`PUT` 请求体：

```json
{
  "character_id": "...",
  "variant_id": "...",
  "spec_id": "..."
}
```

`GET` 和 `PUT` 响应至少返回：

```json
{
  "shot_page_id": "...",
  "character_id": "...",
  "character_name": "...",
  "variant_id": "...",
  "variant_name": "...",
  "spec_id": "...",
  "spec_name": "...",
  "spec_value_id": "..."
}
```

业务校验：

- 场景页、人物、形象或规格不存在时返回 404。
- 形象不属于人物时返回 422。
- 形象没有对应规格值时返回 422，并提供可显示给用户的中文错误消息。
- 更新绑定为幂等操作，相同请求重复提交不能创建重复记录。

为兼容旧调用，允许 `spec_id` 暂时缺省，但缺省记录不能被判定为“页面人物配置完整”。

### 5.3 场景页 API

复用现有场景页更新接口保存：

- `prompt_text`
- `negative_prompt`

不新增重复表或重复字段。确认前端使用真实后端返回值，刷新后内容必须一致。

### 5.4 页面完成检查 API

优先扩展现有项目预检查，不重复创建含义相同的检查系统。

修正前端请求协议：

- `scope` 使用后端规定的下划线值：`project`、`chapter`、`large_scene`、`small_scene`、`branch`、`shot_pages`。
- 非项目范围统一发送 `scope_id`。

预检查新增或细化：

- `missing_character`
- `missing_variant`
- `missing_spec`
- `invalid_character_variant`
- `missing_spec_value`
- `missing_prompt`
- `missing_material_mapping`
- `no_workflow`
- `invalid_workflow_version`

返回项必须包含 `type`、`entity_type`、`entity_id`、`entity_name`、`message`，便于前端定位。

## 6. 提示词编译规则

### 6.1 禁止使用全部规格

编译一张场景页时，只能读取该页人物绑定中的一个 `spec_id`，并解析当前 `variant_id + spec_id` 对应的规格值。

严禁遍历并拼接该形象下的全部规格提示词。全身、侧面、背面、半身和特写规格同时进入一张图会产生直接冲突。

### 6.2 人物规格的场景化提取

当前人物规格提示词可能包含训练确认图专用的质量、单人、固定站姿、固定方向、纯色背景、构图和光线段落。这些内容不能原样全部带入剧情场景。

编译器应从所选规格中提取人物身份、触发词、固定外貌、当前形象服装和当前景别可见服装，排除以下内容：

- 训练确认图的质量分段。
- 人物数量和单人限制。
- 固定站姿或手臂位置。
- 正侧背方向和确认图构图。
- 纯色背景、确认图光线和画法要求。

对于现有带 `--` 分段的规格提示词，按分段语义提取；保留下来的分段行仍作为有效提示词输出。不得用简单的整段字符串截断，也不得破坏人物触发词中的下划线。

若规格提示词没有可识别分段，则使用兼容回退：保留原文，但产生 `unstructured_character_spec` 警告，提示用户该规格可能带入构图冲突。不得静默丢弃整条规格。

### 6.3 正向提示词来源

最终正向提示词必须包含且只包含当前页的有效来源：

1. 页级正向提示词。
2. 当前页所选人物形象与规格解析出的场景化人物提示词。
3. 当前页实际映射的素材页正向提示词。

素材按稳定顺序合并：

1. `prompt`
2. `composite_template`
3. `composition`
4. `expression`
5. `scene`
6. `lighting`

同一文本片段只保留第一次出现，保留原始换行和 `--` 分段。不要用集合导致顺序随机。

当前 `shot_pages.prompt_text` 已经读取到 `effective_config`，必须真正进入 `positive_prompt` 语义插槽。不得只保存在编译快照中而不写入工作流。

### 6.4 负向提示词来源

最终负向提示词按以下来源稳定合并：

1. 页级 `negative_prompt`。
2. 当前页全部已映射素材页的 `negative_prompt`。
3. 人物或工作流已有的有效负向默认值（若现有系统支持）。

要求：

- 去除空值。
- 去除完全相同的重复片段。
- 保持首次出现顺序。
- 结果必须进入 `negative_prompt` 语义插槽。
- 未绑定人物时也必须能编译页级和素材负向提示词，不能把负向提示词错误地依附于 `character_values` 是否存在。

### 6.5 LoRA 与模型覆盖

继续使用当前人物形象和所选规格的 LoRA、权重、模型覆盖字段，但只允许当前选中规格参与回退。

优先级：

1. 页级或批量运行显式覆盖。
2. 所选规格值。
3. 人物形象默认值。
4. 项目或工作流默认值。

本轮不开发批量替换 LoRA 的新界面。

### 6.6 编译快照可追溯

编译结果和任务快照应记录：

- `character_id`
- `variant_id`
- `spec_id`
- `spec_value_id`
- 使用的页级正负提示词
- 每个素材映射及其提示词来源
- 最终合并后的正负提示词
- 每个字段的来源说明

这样生成结果进入产出管理后，可以追溯当时实际使用的页面配置。

## 7. 不在本轮范围

- 不开发 AI 自动编写提示词。
- 不开发多人角色库绑定。
- 不重做人物库页面。
- 不重做素材库页面。
- 不开发批量人物替换和批量 LoRA 替换。
- 不自动创建、发布或选择 ComfyUI 工作流。
- 不改变一张场景页可生成多个图片实例及采用排序的既有设计。
- 不进行真实 ComfyUI 提交；本轮必须验证编译结果，真实生图在工作流设置完成后单独验收。

## 8. 测试要求

### 8.1 后端自动化测试

必须覆盖：

1. 旧人物绑定记录迁移后可读取。
2. 新绑定可保存和返回 `spec_id`。
3. 形象不属于人物时拒绝保存。
4. 形象不存在所选规格值时拒绝保存。
5. 相同绑定重复保存保持幂等。
6. 解除绑定后人物库数据不受影响。
7. 页级正负提示词保存、清空、长文本边界和换行保持。
8. 编译只使用一个所选规格，不使用其他规格。
9. 页级正向提示词进入正向语义插槽。
10. 素材正向提示词按规定顺序进入正向语义插槽。
11. 页级和素材负向提示词全部进入负向语义插槽。
12. 未绑定人物时负向提示词仍能正确编译。
13. `--` 分段和触发词下划线在保存、编译后保持不变。
14. 前端使用的 `small_scene + scope_id` 预检查请求可以成功返回。

自动化测试必须使用临时 SQLite 数据库或现有测试夹具，不能污染生产数据库。取消测试库设计不等于允许测试写入用户生产数据。

### 8.2 前端模拟点击测试

必须用浏览器模拟真实用户操作：

1. 打开项目并进入小场景。
2. 选择 P01。
3. 选择人物，确认形象列表联动刷新。
4. 选择形象，确认规格列表只显示已有值的公共规格。
5. 保存绑定并刷新页面，确认选择保持。
6. 编辑正负提示词，保存后确认切换为完整只读展示。
7. 再次编辑，确认原文、换行和 `--` 分段未变化。
8. 查看四类已有素材映射仍然存在。
9. 运行页面完成检查，确认缺项可定位。
10. 未设置工作流时点击“前往工作流”，确认保留项目参数。

不得只调用 API 后宣称前端通过。

### 8.3 编译集成测试

构造一张含以下数据的场景页：

- 一个人物绑定。
- 两个形象。
- 每个形象有多个公共规格值。
- 当前页只选择其中一个规格。
- 页级正负提示词。
- 场景、构图、表情、光线四类素材映射。
- 一个含正向和负向语义插槽的已发布工作流版本。

断言：

- 编译项目数为 1。
- 最终正向提示词包含页级正向、所选人物规格和四类素材正向。
- 最终正向不包含未选规格的方向、背景或构图内容。
- 最终负向包含页级与四类素材负向。
- 语义插槽解析结果与编译快照一致。
- 编译过程不向 ComfyUI 提交任务。

## 9. 当前生产页面的手动验收

代码通过自动化测试后，再使用第 2 节列出的现有生产页面进行手动验收：

1. 现有四类素材映射不得丢失。
2. 为页面绑定现有人物、`带外套`形象和`正面半身`公共规格。
3. 将已经确认的当前页专属提示词保存到页级字段。
4. 刷新页面，人物、形象、规格和提示词保持不变。
5. 页面状态应显示“可编译”。
6. 因项目尚无默认工作流，状态应明确显示“尚未设置项目默认工作流”，而不是误报页面内容未完成。
7. 设置有效默认工作流后，状态变为“可跑图”。
8. 编译预览中检查最终正负提示词，确认没有混入其他人物规格。

生产页面数据只能在人工验收步骤修改，禁止自动化测试脚本直接写入。

## 10. 交付物

编程 AI 必须交付：

1. 数据库兼容迁移。
2. 人物绑定 API 扩展。
3. 场景页详情前端交互。
4. 页面完成状态与预检查协议修复。
5. 正负提示词编译修复。
6. 后端自动化测试。
7. 前端模拟点击测试及结果。
8. 编译集成测试及结果。
9. 简短开发说明，列出修改文件、迁移行为、已知限制。

## 11. 完成定义

只有同时满足以下条件，才能将本需求标记为完成：

- 用户能够完全通过界面绑定人物、形象和一个公共规格。
- 用户能够完全通过界面编辑并查看页级正负提示词。
- 刷新后所有配置正确恢复。
- 编译器只使用被选中的一个人物规格。
- 页级正向、素材正向、页级负向和素材负向都实际进入对应工作流语义插槽。
- 页面完成状态能区分“页面未完成”“缺少工作流”“可以跑图”。
- 现有素材映射和生产数据无回归。
- 后端测试、前端模拟点击测试和编译集成测试全部通过。

仅完成界面、仅完成 API、仅让预检查不报错，或仅能生成编译快照但提示词没有进入工作流，都不算完成。
