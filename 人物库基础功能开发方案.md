# Atelier 人物库基础功能开发方案

状态：待整改（2026-07-28 已取消双数据库设计）
目标：提供一个可以立即独立使用的项目人物库
开发范围：数据库、FastAPI、人物库前端、图片存储、自动化测试

> 数据库决策：产品只保留一个生产数据库。`atelier.test.sqlite3`、数据库切换、测试库锁定和测试库设置页面全部取消。自动化测试仍必须使用 `TemporaryDirectory` 中的一次性数据库，但该数据库不是产品功能，不得在界面或正式启动脚本中出现。

## 1. 开发原则

本轮只做基础人物库，不依赖尚未完成的其他模块。

人物库必须能够单独完成：

- 创建人物。
- 查看人物列表。
- 查看人物资料。
- 编辑人物资料。
- 添加和删除数据库标签。
- 上传、替换和移除人物封面图。
- 按人物名称、说明和标签搜索。
- 按人物类型筛选。
- 归档人物和恢复人物。

本轮不联动：

- 剧本画布。
- 章节、大场景、小场景或分镜页。
- 素材库。
- 人物替换矩阵。
- ComfyUI。
- 工作流语义插槽。
- LoRA。
- 形象变体。
- 全身、半身、特写等规格。
- 批量出图和人物批量替换。
- 全局人物模板。

以上未开发功能不得使用假数据、假引用数量或“演示状态”代替。

## 2. 使用范围

- 人物属于项目。
- 人物库页面继续使用现有路由：

```text
?page=characters&project={project_id}
```

- 当前项目不存在时显示真实空状态，并允许返回项目中心。
- 产品只读取和修改唯一生产数据库 `data/databases/atelier.production.sqlite3`。
- 页面和 API 不再提供数据库环境切换。
- 自动化测试通过显式传入临时 `data_root` 创建一次性数据库，测试结束后自动销毁。

## 3. 人物数据模型

新增 `characters` 表：

```sql
CREATE TABLE IF NOT EXISTS characters (
    id TEXT PRIMARY KEY,
    project_id TEXT NOT NULL,
    name TEXT NOT NULL,
    role_type TEXT NOT NULL DEFAULT 'other',
    description TEXT NOT NULL DEFAULT '',
    base_prompt TEXT NOT NULL DEFAULT '',
    notes TEXT NOT NULL DEFAULT '',
    cover_original_path TEXT,
    cover_thumbnail_path TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    archived_at TEXT,
    FOREIGN KEY (project_id)
        REFERENCES projects(id) ON DELETE CASCADE,
    UNIQUE (project_id, name)
);
```

合法 `role_type`：

- `lead`：主角
- `major`：主要人物
- `supporting`：次要人物
- `cameo`：客串人物
- `other`：其他

新增 `character_tags` 表：

```sql
CREATE TABLE IF NOT EXISTS character_tags (
    character_id TEXT NOT NULL,
    tag_name TEXT NOT NULL COLLATE NOCASE,
    created_at TEXT NOT NULL,
    PRIMARY KEY (character_id, tag_name),
    FOREIGN KEY (character_id)
        REFERENCES characters(id) ON DELETE CASCADE
);
```

索引：

```sql
CREATE INDEX IF NOT EXISTS idx_characters_project_updated
    ON characters(project_id, archived_at, updated_at DESC);

CREATE INDEX IF NOT EXISTS idx_character_tags_name
    ON character_tags(tag_name);
```

## 4. 字段规则

### 4.1 人物名称

- 必填。
- 去除首尾空格。
- 连续空白压缩为一个空格。
- 最长 80 个字符。
- 同一项目内不允许重名。
- 不同项目允许同名。

### 4.2 人物类型

- 必须是合法枚举值。
- 默认 `other`。
- 前端显示中文名称。

### 4.3 人物说明

- 用于人工阅读。
- 最长 2,000 个字符。
- 可以留空。

### 4.4 基础提示词

- 只作为人物资料中的普通文本保存。
- 不编译、不注入工作流、不解析 LoRA。
- 最长 20,000 个字符。
- 可以留空。

### 4.5 备注

- 用于记录创作习惯、来源或其他说明。
- 最长 5,000 个字符。
- 可以留空。

### 4.6 标签

- 以数据库标签为主，不从文件夹名称推导。
- 一个人物可以有多个标签。
- 标签去除首尾空格并压缩连续空白。
- 单个标签最长 40 个字符。
- 同一人物的标签不区分大小写，不能重复。
- 单个人物最多 50 个标签。
- 创建和编辑人物时可以一次提交完整标签列表。

## 5. 人物封面图

### 5.1 范围

基础版本只维护一张人物封面图，不开发人物图片集和形象规格图库。

支持格式：

- JPEG
- PNG
- WebP

限制：

- 单文件最大 25 MB。
- 必须验证实际图片内容，不能只检查扩展名。
- 图片解码失败时拒绝保存。
- 上传后生成最长边 512 像素的 WebP 缩略图。
- 缩略图必须保持原比例。

依赖可以增加：

- `Pillow`
- `python-multipart`

### 5.2 文件存储

正式人物图片只使用一个根目录：

```text
data/character-files/{project_id}/{character_id}/
```

目录内文件：

```text
cover-original.{ext}
cover-thumb.webp
```

要求：

- 禁止使用用户原文件名作为存储路径。
- 所有最终路径必须验证仍在 `data/character-files/` 根目录下。
- 替换封面时先成功写入临时文件，再原子替换旧文件。
- 上传失败不得破坏旧封面。
- 自动化测试必须把图片根目录指向 `TemporaryDirectory`，不得使用正式 `data/character-files/`。

## 6. 后端 API

### 6.1 人物列表

```http
GET /api/projects/{project_id}/characters
```

查询参数：

```text
q=
role_type=
tag=
archived=false
limit=50
offset=0
```

规则：

- `q` 搜索人物名称、说明和标签。
- 默认不返回已归档人物。
- `limit` 范围为 1–100。
- 排序：`updated_at DESC, name ASC`。

响应：

```json
{
  "database_environment": "production",
  "project_id": "",
  "items": [],
  "total": 0,
  "limit": 50,
  "offset": 0
}
```

### 6.2 创建人物

```http
POST /api/projects/{project_id}/characters
```

请求：

```json
{
  "name": "角色名称",
  "role_type": "lead",
  "description": "",
  "base_prompt": "",
  "notes": "",
  "tags": ["女主", "长发"]
}
```

成功返回 201。

### 6.3 查看人物

```http
GET /api/characters/{character_id}
```

返回人物完整资料和标签。

### 6.4 编辑人物

```http
PATCH /api/characters/{character_id}
```

允许修改：

- `name`
- `role_type`
- `description`
- `base_prompt`
- `notes`
- `tags`

只修改请求中出现的字段。

### 6.5 归档人物

```http
DELETE /api/characters/{character_id}
```

- 使用软删除，将 `archived_at` 写为当前时间。
- 前端操作名称使用“归档”，不要显示为永久删除。
- 归档前必须二次确认。

### 6.6 恢复人物

```http
POST /api/characters/{character_id}/restore
```

- 将 `archived_at` 清空。
- 已恢复人物重新出现在默认人物列表。

### 6.7 上传或替换封面

```http
POST /api/characters/{character_id}/cover
Content-Type: multipart/form-data
```

表单字段：

```text
file
```

### 6.8 读取封面

```http
GET /api/characters/{character_id}/cover/original
GET /api/characters/{character_id}/cover/thumbnail
```

- 返回正确的图片媒体类型。
- 没有封面时返回 404。
- 缩略图接口设置合理的本地缓存响应头。

### 6.9 移除封面

```http
DELETE /api/characters/{character_id}/cover
```

- 清空数据库路径并移除对应文件。
- 没有封面时保持幂等，仍返回成功。

## 7. 前端页面

### 7.1 替换演示页面

现有 `charactersPage()` 中的角色 A、角色 B等全部为演示数据。

要求：

- 唯一生产数据库中不得展示任何演示人物。
- 人物页面由 `runtime-api.js` 读取真实 API 后重绘。
- 不改动其他未开发页面。

### 7.2 人物列表

页面顶部：

- 标题：人物库。
- 当前项目名称。
- 搜索框。
- 人物类型筛选。
- 标签筛选或标签文本筛选。
- “新建人物”按钮。
- “查看已归档”开关。

人物卡片显示：

- 封面缩略图；没有封面时显示人物名称首字。
- 人物名称。
- 人物类型。
- 最多显示 4 个标签，多余标签显示数量。
- 人物说明的两行摘要。
- 更新时间。
- “编辑”按钮。
- “归档”按钮。

不得显示：

- 虚假的分镜引用数量。
- 虚假的规格完整度。
- 虚假的 LoRA 状态。
- 虚假的形象变体数量。
- “打开替换矩阵”按钮。
- “从项目复制”按钮。
- “全局人物模板”标签页。

### 7.3 空状态

没有人物时显示：

- “还没有人物”
- 简短说明。
- “新建人物”按钮。

不要显示“下一步：……”等写死的教学文案。

### 7.4 新建与编辑

使用同一个人物资料弹窗或右侧面板。

字段：

- 人物名称。
- 人物类型。
- 人物说明。
- 基础提示词。
- 标签输入。
- 备注。
- 封面图。

交互要求：

- 创建人物时先保存人物资料，再允许上传封面。
- 编辑时可上传、替换或移除封面。
- 保存期间禁用提交按钮。
- 后端错误显示在表单内。
- 保存成功后重新读取真实 API。

### 7.5 搜索与筛选

- 用户停止输入 250–400 毫秒后发起搜索。
- 切换人物类型或归档状态后立即刷新。
- 搜索条件写入当前页面状态即可，本轮不要求写入 URL。
- 新请求开始时取消或忽略旧请求，避免结果倒序覆盖。

### 7.6 图片加载

- 列表缩略图必须使用 `loading="lazy"`。
- 不在列表加载原图。
- 图片加载失败时回退为人物名称首字，不得出现破图图标。

## 8. 数据库和测试安全

1. `create_app` 工厂保持无真实数据库副作用。
2. 自动化测试只能使用 `TemporaryDirectory`。
3. 测试代码不得导入会创建默认全局应用的 ASGI 入口。
4. 产品中删除 `production/test` 环境选择、`locked_environment` 和数据库切换接口。
5. 正式启动只保留 `start.bat` 和端口 8110。
6. 浏览器端到端测试必须由测试脚本创建临时数据目录和临时端口，退出时自动删除，不得连接正式服务。
7. 禁止向正式项目创建验收人物。
8. 禁止清空或重建现有生产数据库。
9. 数据库迁移只能在事务内执行，并必须先生成可恢复备份。
10. 开发前后必须记录生产数据库：
   - SHA256
   - 文件大小
   - 修改时间
   - 项目、章节和大场景业务记录
11. 单元测试、接口测试和浏览器验收产生的数据只能存在于系统临时目录。

## 9. 自动化测试

至少覆盖：

### 数据库与接口

- 新项目人物列表为空。
- 创建人物成功。
- 名称空白和超长被拒绝。
- 同项目重名被拒绝。
- 不同项目允许同名。
- 人物只出现在所属项目。
- 编辑每个基础字段。
- 非法人物类型被拒绝。
- 标签去重、清理和数量限制。
- 名称、说明和标签搜索。
- 人物类型筛选。
- 默认不返回已归档人物。
- 归档和恢复。
- 项目删除时人物级联删除。
- 上传 JPEG、PNG 和 WebP。
- 非图片文件被拒绝。
- 超大文件被拒绝。
- 生成 WebP 缩略图。
- 替换封面。
- 移除封面。
- 图片路径不能逃逸存储根目录。
- 临时测试人物和图片不会进入正式数据目录。
- 原有全部测试继续通过。

### 前端端到端

- 正式页面在空库时不显示演示人物。
- 临时端到端环境创建人物后立即显示人物卡片。
- 编辑人物后列表即时更新。
- 上传封面后显示缩略图。
- 搜索名称和标签。
- 人物类型筛选。
- 归档后默认列表消失。
- 查看已归档后可以恢复。
- 浏览器控制台无错误或警告。

## 10. 非功能要求

- 保持 FastAPI 和当前 SQLite 架构。
- 前后端仍由同一个 FastAPI 端口交付。
- 不新增独立前端端口。
- 正式启动只保留 `start.bat`。
- 删除 `start-test.bat` 以及产品界面中的数据库切换功能。
- BAT 文件不得加入中文。
- UI 保持明亮、圆润、简洁的苹果风格。
- 不修改与人物库无关的页面和接口。

## 11. 验收命令

```bat
.venv\Scripts\python.exe -m unittest discover -s backend\tests -v
node --check design\ui-preview\app.js
node --check design\ui-preview\runtime-api.js
```

完成后使用：

```bat
start.bat
```

最终必须确认：

- 8110 健康检查正常。
- 设置页不再展示测试数据库或数据库切换。
- 全部自动化测试通过。
- 临时端到端环境的人物操作通过。
- 自动化测试和端到端测试没有修改正式数据库及正式人物图片目录。

## 12. 交付记录

完成开发后，在 `DEVELOPMENT_LOG.md` 追加：

- 修改文件。
- 新增数据表和索引。
- 图片存储目录。
- 新增接口。
- 测试数量和结果。
- 浏览器验收结果。
- 生产数据对比结果。
- 明确列出仍未开发的联动功能。

没有通过全部自动化测试、临时端到端验收和生产数据核对时，不得标记为完成。
