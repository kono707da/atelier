# Atelier 小场景画布前后端联调验收整改需求

版本：1.0  
日期：2026-07-28  
状态：待开发  
验收基线：`GLM-MAIN`，提交 `9474d64`  
项目路径：`C:\Users\kono707da\Documents\AICode\atelier`

---

## 1. 任务目标

完成现有“小场景画布与素材页映射”前端和后端的真实联调，使下面这条使用路径形成完整闭环：

```text
项目
→ 章节
→ 大场景
→ 小场景
→ 双击进入小场景画布
→ 管理场景页
→ 关联素材
→ 查看素材的只读页面
→ 将素材页映射到场景页
→ 刷新后数据仍然存在
```

本任务不是重新设计页面，也不是另写一套与前端无关的后端模型。完成标准以现有前端能够真实使用为准。

---

## 2. 当前验收结论

当前版本不通过小场景模块验收。

已确认：

- `GLM-MAIN` 已合并 `main`。
- JavaScript 语法检查通过。
- 后端 295 项自动化测试通过。
- 开发者管理的全系统进度和页面滚动正常。
- 后端已经实现 `small_scenes`、`shot_pages`、`branches`、`small_scene_materials` 和 `shot_page_materials`。

阻断问题：

1. 前端请求 `GET /api/projects/{project_id}/story-tree`，后端没有该接口。
2. 后端已保存的小场景无法显示在剧本目录和画布中。
3. 前端请求 `GET /api/small-scenes/{small_scene_id}/workspace`，后端没有该接口。
4. 前端使用 `pages`、`small-scene-pages`、`resources` 和 `mappings`。
5. 后端使用 `shot-pages` 和 `materials`，接口名称、字段和业务粒度均不一致。
6. 后端当前只关联整个素材，不能表达“素材中的某一页映射到某个场景页”。
7. 现有测试只验证后端自身，没有验证前端所需契约。

实测现象：

- 临时数据库中后端返回 1 个小场景。
- 剧本画布显示 0 个小场景，并显示“小场景待接入”。
- 直接打开该小场景时，页面提示缺少 `GET /api/small-scenes/{id}/workspace`。

---

## 3. 唯一业务规则

以下规则为本次开发和验收的唯一依据。

### 3.1 层级

```text
项目
└─ 章节
   └─ 大场景
      └─ 小场景
         ├─ 场景页
         └─ 关联素材
            └─ 素材页
```

### 3.2 场景页

- 一个小场景允许任意数量的场景页。
- 场景页可以创建、改名、修改说明、删除和排序。
- 删除后剩余页面的 `sort_order` 必须从 1 开始连续编号。
- 同一小场景内场景页名称不允许重复，比较时忽略大小写和首尾空白。

### 3.3 素材页

- 一个素材允许包含任意数量的素材页。
- 素材页的内容由素材库维护。
- 在小场景画布中，素材页只读，不允许修改。
- 现有素材如果还没有素材页，迁移时必须自动生成一个默认素材页。
- 新建素材时必须自动生成一个默认素材页，保证素材关联后立即可用。

### 3.4 素材关联

- 小场景先关联素材，关联后才能使用该素材的页面。
- 同一个素材在同一个小场景中只能关联一次。
- 移除小场景中的素材关联，不得删除素材本身。
- 移除关联时，该素材在此小场景中的全部页面映射一并删除。

### 3.5 页面映射

- 一个素材页可以同时映射到多个场景页。
- 一个场景页可以绑定不同类型的素材。
- 同一个场景页、同一种素材类型最多只能绑定一个素材页。
- 给同一场景页重新绑定同类型素材页时，直接替换旧映射。
- 取消映射时只删除映射，不删除场景页、素材或素材页。
- 被映射的素材页必须属于当前小场景已经关联的素材。

示例：

```text
小场景共有 10 个场景页
表情素材共有 4 个素材页

场景页 1 → 表情素材页 1
场景页 2 → 表情素材页 1
场景页 3 → 表情素材页 2
场景页 4 → 表情素材页 2
```

---

## 4. 开发原则

### 4.1 前端界面作为固定验收界面

现有文件：

- `design/ui-preview/app.js`
- `design/ui-preview/runtime-api.js`
- `design/ui-preview/styles.css`

页面结构、交互和视觉样式本次不重写。

允许修改前端的范围仅限：

- 修复接口字段适配。
- 修复真实联调发现的错误状态。
- 增加必要的加载、失败和空状态。
- 删除已经失效的“后端待开发”判断。

### 4.2 现有后端数据不得丢失

保留：

- `small_scenes`
- `shot_pages`
- `branches`
- `small_scene_materials`
- `shot_page_materials`

`shot_pages` 继续作为场景页的内部规范表，不再另建一张内容重复的 `small_scene_pages` 表。

前端契约中的：

```text
small_scene_page.id
small_scene_page.name
```

映射到后端内部：

```text
shot_pages.id
shot_pages.title
```

API 层负责 `name` 与 `title` 的转换。

### 4.3 不允许只改提示文字伪装完成

不能通过以下方式通过验收：

- 隐藏“小场景待接入”提示但仍不读取数据。
- 在前端构造演示小场景。
- 把后端返回的素材整体伪装为素材页。
- 只新增路由但不持久化数据。
- 修改测试去迁就错误实现。

---

## 5. 数据库整改

## 5.1 `material_pages`

新增素材页表：

```sql
CREATE TABLE material_pages (
    id TEXT PRIMARY KEY,
    material_id TEXT NOT NULL,
    name TEXT NOT NULL,
    description TEXT NOT NULL DEFAULT '',
    content TEXT NOT NULL DEFAULT '',
    prompt_text TEXT NOT NULL DEFAULT '',
    negative_prompt TEXT NOT NULL DEFAULT '',
    sort_order INTEGER NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    FOREIGN KEY (material_id)
        REFERENCES materials(id) ON DELETE CASCADE,
    UNIQUE (material_id, name)
);

CREATE INDEX idx_material_pages_material_sort
    ON material_pages(material_id, sort_order);
```

规则：

- `sort_order` 从 1 开始连续。
- 删除素材时级联删除素材页。
- 删除素材页时级联删除引用它的页面映射。

## 5.2 `small_scene_materials`

复用现有表作为“小场景资源关联表”。

需要新增稳定的关联 ID：

```text
id TEXT
```

迁移要求：

- 旧行生成 UUID 并回填。
- 创建唯一索引保证 `id` 唯一。
- 新关联必须写入 `id`。
- 保留原有复合主键或唯一约束：

```text
(small_scene_id, material_id)
```

该 `id` 作为前端的 `link_id`。

## 5.3 `small_scene_page_mappings`

新增素材页映射表：

```sql
CREATE TABLE small_scene_page_mappings (
    id TEXT PRIMARY KEY,
    scene_page_id TEXT NOT NULL,
    material_page_id TEXT NOT NULL,
    material_type TEXT NOT NULL,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    FOREIGN KEY (scene_page_id)
        REFERENCES shot_pages(id) ON DELETE CASCADE,
    FOREIGN KEY (material_page_id)
        REFERENCES material_pages(id) ON DELETE CASCADE,
    UNIQUE (scene_page_id, material_type)
);

CREATE INDEX idx_small_scene_page_mappings_material_page
    ON small_scene_page_mappings(material_page_id, scene_page_id);
```

`material_type` 允许值必须与素材库一致：

```text
composition
expression
scene
lighting
prompt
composite_template
```

写入时必须从 `material_pages → materials` 查询真实类型，不信任前端传值。

## 5.4 默认素材页迁移

数据库初始化或迁移时：

1. 查询没有素材页的现有素材。
2. 为每个素材创建一个默认素材页。
3. 默认页名称使用素材名称。
4. `description`、`content`、`prompt_text`、`negative_prompt` 从素材主记录复制。
5. `sort_order = 1`。
6. 迁移必须幂等，多次启动不能重复创建。

---

## 6. 后端 API 唯一契约

以下接口和字段必须完整实现。现有其他接口可以保留，但不能代替这些接口。

## 6.1 项目剧本树

```http
GET /api/projects/{project_id}/story-tree
```

成功：

```json
{
  "database_environment": "production",
  "project_id": "project-id",
  "chapters": [
    {
      "id": "chapter-id",
      "project_id": "project-id",
      "name": "章节名称",
      "sort_order": 1,
      "large_scenes": [
        {
          "id": "large-scene-id",
          "chapter_id": "chapter-id",
          "name": "大场景名称",
          "scene_type": "content",
          "sort_order": 1,
          "small_scenes": [
            {
              "id": "small-scene-id",
              "large_scene_id": "large-scene-id",
              "name": "小场景名称",
              "scene_type": "content",
              "description": "",
              "sort_order": 1,
              "page_count": 10,
              "resource_count": 2,
              "pages": [
                {
                  "id": "scene-page-id",
                  "name": "第 1 页",
                  "description": "",
                  "sort_order": 1
                }
              ],
              "resources": [
                {
                  "link_id": "resource-link-id",
                  "material_id": "material-id",
                  "name": "表情素材",
                  "material_type": "expression",
                  "page_count": 4,
                  "pages": [
                    {
                      "id": "material-page-id",
                      "name": "表情 1",
                      "description": "",
                      "sort_order": 1
                    }
                  ]
                }
              ]
            }
          ]
        }
      ]
    }
  ]
}
```

要求：

- 项目不存在返回 404。
- 排序全部按 `sort_order ASC`。
- 不允许每层循环单独创建大量数据库连接。
- 使用少量批量查询组装树，避免明显的 N+1 查询。

## 6.2 小场景工作区

```http
GET /api/small-scenes/{small_scene_id}/workspace
```

成功：

```json
{
  "database_environment": "production",
  "project": {
    "id": "project-id",
    "name": "项目名称"
  },
  "chapter": {
    "id": "chapter-id",
    "name": "章节名称"
  },
  "large_scene": {
    "id": "large-scene-id",
    "name": "大场景名称"
  },
  "small_scene": {
    "id": "small-scene-id",
    "name": "小场景名称",
    "description": "",
    "sort_order": 1
  },
  "pages": [
    {
      "id": "scene-page-id",
      "small_scene_id": "small-scene-id",
      "name": "第 1 页",
      "description": "",
      "sort_order": 1
    }
  ],
  "resources": [
    {
      "link_id": "resource-link-id",
      "material_id": "material-id",
      "name": "表情素材",
      "material_type": "expression",
      "pages": [
        {
          "id": "material-page-id",
          "material_id": "material-id",
          "name": "表情 1",
          "description": "",
          "sort_order": 1
        }
      ]
    }
  ],
  "mappings": [
    {
      "id": "mapping-id",
      "scene_page_id": "scene-page-id",
      "material_page_id": "material-page-id",
      "material_type": "expression"
    }
  ]
}
```

小场景不存在返回 404。

## 6.3 场景页接口

### 创建

```http
POST /api/small-scenes/{small_scene_id}/pages
```

请求：

```json
{
  "name": "第 1 页",
  "description": "画面说明"
}
```

成功返回 201：

```json
{
  "database_environment": "production",
  "page": {
    "id": "scene-page-id",
    "small_scene_id": "small-scene-id",
    "name": "第 1 页",
    "description": "画面说明",
    "sort_order": 1
  }
}
```

内部调用现有 `create_shot_page`，将 `name` 转为 `title`。

### 修改

```http
PATCH /api/small-scene-pages/{page_id}
```

请求允许：

```json
{
  "name": "修改后的名称",
  "description": "修改后的说明"
}
```

内部调用现有 `update_shot_page`。

### 删除

```http
DELETE /api/small-scene-pages/{page_id}
```

删除后：

- 相关映射级联删除。
- 同小场景剩余页面连续编号。

### 排序

```http
PUT /api/small-scenes/{small_scene_id}/pages/order
```

请求：

```json
{
  "page_ids": [
    "page-id-2",
    "page-id-1",
    "page-id-3"
  ]
}
```

校验：

- `page_ids` 必须与该小场景直属页面集合完全一致。
- 不允许缺失、重复或混入其他小场景页面。
- 使用一个事务完成全部排序。

## 6.4 素材页接口

```http
GET    /api/materials/{material_id}/pages
POST   /api/materials/{material_id}/pages
PATCH  /api/material-pages/{page_id}
DELETE /api/material-pages/{page_id}
PUT    /api/materials/{material_id}/pages/order
```

本轮前端不需要在小场景画布内编辑素材页，但后端必须提供完整 CRUD，供素材库后续接入。

返回字段统一：

```json
{
  "id": "material-page-id",
  "material_id": "material-id",
  "name": "素材页名称",
  "description": "",
  "content": "",
  "prompt_text": "",
  "negative_prompt": "",
  "sort_order": 1
}
```

## 6.5 关联素材

### 关联

```http
POST /api/small-scenes/{small_scene_id}/resources
```

请求：

```json
{
  "material_id": "material-id"
}
```

成功返回 201：

```json
{
  "database_environment": "production",
  "resource": {
    "link_id": "resource-link-id",
    "material_id": "material-id",
    "name": "素材名称",
    "material_type": "expression",
    "pages": []
  }
}
```

重复关联返回 409。

### 移除

```http
DELETE /api/small-scene-resource-links/{link_id}
```

成功后：

- 删除关联行。
- 删除该素材在此小场景内的全部映射。
- 不删除素材和素材页。

## 6.6 设置或取消素材页映射

```http
PUT /api/small-scene-pages/{scene_page_id}/mappings/{material_type}
```

设置：

```json
{
  "material_page_id": "material-page-id"
}
```

取消：

```json
{
  "material_page_id": null
}
```

设置时必须校验：

1. 场景页存在。
2. 素材页存在。
3. 素材页的真实类型等于 URL 中的 `material_type`。
4. 素材所属素材已经关联到场景页所属小场景。
5. 同场景页、同素材类型的旧映射被原子替换。

成功返回：

```json
{
  "database_environment": "production",
  "mapping": {
    "id": "mapping-id",
    "scene_page_id": "scene-page-id",
    "material_page_id": "material-page-id",
    "material_type": "expression"
  }
}
```

取消成功时 `mapping` 为 `null`。

---

## 7. 现有后端接口的处理

以下现有接口可以保留：

```text
GET/POST /api/small-scenes/{id}/shot-pages
GET/PATCH/DELETE /api/shot-pages/{id}
POST /api/shot-pages/{id}/move
GET/PUT /api/small-scenes/{id}/materials
GET/PUT /api/shot-pages/{id}/materials
```

处理原则：

- 不删除，避免破坏已有测试和未来兼容性。
- 新旧接口必须调用同一组 `DatabaseManager` 方法。
- 不能维护两份场景页数据。
- 新接口是现有前端的正式契约。
- `shot_page_materials` 不等价于素材页映射，不得用它冒充 `small_scene_page_mappings`。

---

## 8. 前端联调要求

开发完成后必须确认：

1. `loadStoryHierarchy()` 能得到真实 `story-tree`。
2. 不再进入 `backendAvailable: false` 降级状态。
3. 后端已有小场景会显示在左侧目录和中间画布。
4. “添加小场景”按钮真实创建并刷新。
5. 双击小场景进入工作区。
6. 工作区不再显示“小场景后端待开发”。
7. 场景页新增、编辑、删除、前移和后移均持久化。
8. 关联素材后立即显示该素材的全部素材页。
9. 素材页在小场景画布内没有编辑入口。
10. 点击页面编号可设置、替换或取消映射。
11. 请求失败时保留当前页面并显示明确错误。
12. 刷新浏览器后所有数据保持一致。
13. 浏览器控制台没有错误或警告。

---

## 9. 错误状态

| 场景 | 状态码 |
|---|---:|
| 父级或目标不存在 | 404 |
| 同级名称重复、重复关联 | 409 |
| 请求字段或映射规则无效 | 422 |
| 排序列表缺失、重复或跨父级 | 422 |
| 数据库异常 | 500 |

所有错误返回：

```json
{
  "detail": "可直接显示给用户的中文说明"
}
```

---

## 10. 数据安全

- 自动化测试必须使用临时 `data_root`。
- 测试应用必须使用：

```python
environment="test"
locked_environment="test"
```

- 禁止测试读取或写入实际生产数据库。
- 禁止清空现有生产项目、章节、大场景、小场景、素材和人物。
- 数据库迁移必须幂等。
- 启动失败或迁移失败时不得留下半迁移状态。
- 所有批量排序和映射替换使用事务。

---

## 11. 必须新增的自动化测试

建议新增：

```text
backend/tests/test_story_tree_contract.py
backend/tests/test_small_scene_workspace_contract.py
backend/tests/test_material_pages.py
backend/tests/test_small_scene_page_mappings.py
backend/tests/test_frontend_api_contract.py
```

## 11.1 剧本树

- 空项目返回空章节数组。
- 项目不存在返回 404。
- 正确返回章节、大场景、小场景、场景页和素材页。
- 所有层级按 `sort_order` 排序。
- `page_count` 和 `resource_count` 正确。

## 11.2 小场景工作区

- 返回完整祖先路径。
- 返回场景页、关联素材、素材页和映射。
- 小场景不存在返回 404。
- 不属于该小场景的数据不会混入。

## 11.3 场景页

- 创建、修改、删除、排序完整覆盖。
- 同名返回 409。
- 空名称返回 422。
- 跨小场景排序返回 422。
- 删除后重新连续编号。
- 删除页面级联删除映射。

## 11.4 素材页

- 现有素材迁移后拥有默认页。
- 新建素材自动拥有默认页。
- 素材页 CRUD 和排序。
- 删除素材页级联删除映射。
- 删除素材级联删除所有素材页、关联和映射。

## 11.5 素材关联

- 关联和移除。
- 重复关联返回 409。
- 移除关联不删除素材。
- 移除关联删除该素材在该小场景内的映射。

## 11.6 映射规则

- 一个素材页可映射到多个场景页。
- 同一场景页可绑定不同素材类型。
- 同一场景页、同一类型只能有一个映射。
- 新映射原子替换旧映射。
- 未关联素材的素材页不能映射。
- URL 类型与素材真实类型不一致返回 422。
- 取消映射后返回 `mapping: null`。

## 11.7 前后端契约

必须增加一项测试，检查当前 `runtime-api.js` 使用的以下路由和方法全部存在于 FastAPI OpenAPI：

```text
GET    /api/projects/{project_id}/story-tree
GET    /api/small-scenes/{small_scene_id}/workspace
POST   /api/small-scenes/{small_scene_id}/pages
PATCH  /api/small-scene-pages/{page_id}
DELETE /api/small-scene-pages/{page_id}
PUT    /api/small-scenes/{small_scene_id}/pages/order
POST   /api/small-scenes/{small_scene_id}/resources
DELETE /api/small-scene-resource-links/{link_id}
PUT    /api/small-scene-pages/{scene_page_id}/mappings/{material_type}
```

该测试用于防止后端测试全绿、真实前端仍无法使用的情况再次发生。

---

## 12. 浏览器端到端验收场景

使用临时测试数据库执行以下完整流程：

1. 创建 1 个项目。
2. 创建 1 个章节。
3. 创建 1 个大场景。
4. 创建 2 个小场景。
5. 刷新剧本画布。
6. 左侧目录和中间画布都必须显示 2 个小场景。
7. 双击第一个小场景进入小场景画布。
8. 创建 10 个场景页。
9. 编辑第 1 页名称和说明。
10. 删除其中 1 页，确认剩余页连续编号。
11. 调整场景页顺序并刷新确认。
12. 创建 1 个表情素材，包含 4 个素材页。
13. 创建 1 个场景素材，包含 2 个素材页。
14. 将两个素材关联到小场景。
15. 小场景画布只读显示 6 个素材页。
16. 将表情素材页 1 同时映射到场景页 1 和场景页 2。
17. 将场景素材页 1 映射到场景页 1。
18. 场景页 1 同时显示表情与场景两种映射。
19. 把场景页 1 的表情映射改为表情素材页 2，旧映射必须被替换。
20. 取消场景页 2 的表情映射。
21. 刷新浏览器，所有结果保持一致。
22. 移除表情素材关联，相关映射消失，素材本身仍存在。
23. 浏览器控制台没有错误或警告。

---

## 13. 回归要求

必须全部通过：

```bat
node --check design\ui-preview\app.js
node --check design\ui-preview\runtime-api.js
.\.venv\Scripts\python.exe -m unittest discover -s backend\tests -p "test_*.py" -q
```

还必须确认：

- 项目、章节和大场景现有功能没有回归。
- 人物库没有回归。
- 素材库 CRUD、筛选、标签和预览图没有回归。
- 角色查询没有回归。
- 开发者管理仍显示全系统进度，页面可滚动。
- `start.bat` 仍可在 8110 启动生产环境。

---

## 14. 开发进度清单更新

只有浏览器端到端验收全部通过后，才允许更新 `系统功能清单.md`。

可以改为完成的项目仅限实际闭环项，例如：

```text
[x] 小场景目录与画布前后端闭环
[x] 小场景页管理前后端闭环
[x] 素材页映射前后端闭环
[x] 小场景绑定分类素材
[x] 素材页到多个场景页的映射与单类型唯一约束
```

未完成的功能不得因为存在数据库表或演示界面而勾选。

---

## 15. 交付要求

编程 AI 完成后必须提供：

1. 修改文件列表。
2. 数据库迁移说明。
3. 新增和保留的 API 列表。
4. 新增测试文件与测试数量。
5. 完整测试结果。
6. 浏览器端到端验收记录。
7. 生产数据库未被测试写入的证明。
8. 已知未完成项。
9. 开发分支和提交号。

分支与合并遵守项目 `AGENTS.md`。本功能尚未通过验收前，不允许主动合并到 `main`。

---

## 16. 完成定义

同时满足以下条件才算完成：

- 后端保存的小场景能在剧本目录和画布中显示。
- 可以从页面创建小场景并刷新读回。
- 可以双击小场景进入真实工作区。
- 场景页增删改和排序真实持久化。
- 素材关联真实持久化。
- 素材页真实存在并只读显示。
- 素材页映射符合单类型唯一规则。
- 刷新和重启后数据一致。
- 前端不再显示“小场景后端待开发”。
- OpenAPI 与前端调用契约一致。
- 新旧自动化测试全部通过。
- 浏览器端到端流程全部通过。
- 浏览器控制台无错误或警告。
- 生产数据库未被测试写入。

任意一项不满足，均不得标记为完成，也不得提交验收通过。
