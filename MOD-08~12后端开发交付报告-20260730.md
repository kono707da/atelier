# Atelier MOD-08~12 后端开发交付报告

交付日期：2026-07-30
开发基线：`accept-260730-GPT5.6-stage1-3`（commit `e9d7540`）
开发模型：GLM-5.2

## 总体结论

MOD-08 至 MOD-12 五个模块的后端开发已全部完成，分别落在五条独立开发分支上，可按模块独立合入基线。MOD-13 为前端视觉重写模块，不在本轮后端开发范围内。

| 模块 | 主题 | 开发分支 | 提交 SHA | 状态 |
|---|---|---|---|---|
| MOD-08 | 图片详情与项目审片 | `dev-260730-GLM5.2-mod08-review` | `e3e2031` | 已提交 |
| MOD-09 | 最终版本与导出 | `dev-260730-GLM5.2-mod09-final-versions` | `071801a` | 已提交 |
| MOD-10 | 百万级全局图库 | `dev-260730-GLM5.2-mod10-gallery` | `fe59f91` | 已提交 |
| MOD-11 | 存储、备份与维护 | `dev-260730-GLM5.2-mod11-maintenance` | `7d5ccd2` | 已提交 |
| MOD-12 | 导入与外部扩展 | `dev-260730-GLM5.2-mod12-import-export` | `48d11f6` | 已提交 |

五条分支全部基于 `accept-260730-GPT5.6-stage1-3` 拉出，互不交叉，可独立评审与合并。各分支均包含后端实现、API 路由和单元测试，无未完成 TODO 残留。

## 代码量统计

基于与基线 `accept-260730-GPT5.6-stage1-3` 的 diff：

| 模块 | 改动文件 | 新增行数 | 备注 |
|---|---|---|---|
| MOD-08 | 4 | +578 | `output_receiver.py` 扩展、API 路由、测试 |
| MOD-09 | 3 | +1096 | `database.py` 新增 final_versions/exports 表、API、测试 |
| MOD-10 | 4 | +1911 | 新建 `gallery.py`、`gallery_index`/`gallery_fts` 表、API、测试 |
| MOD-11 | 3 | +461 | `database.py` 维护方法、API、测试 |
| MOD-12 | 3 | +1790 | `database.py` 导入导出方法、API、测试 |
| **合计** | **17** | **+5836** | 五条分支独立维护 |

## 模块交付详情

### MOD-08 图片详情与项目审片

**核心能力**

- 图片实例采用（adopt）与淘汰（reject）。
- 单分镜页支持同时采用多张图片实例。
- 同一页内采用图片排序（reorder）。
- 标记代表图（representative）。
- 查看图片完整生成追踪信息（task/attempt/snapshot）。
- 从图片实例复制参数并再次生成（copy-params）。

**新增 API**

- `POST /api/image-instances/{id}/adopt`
- `POST /api/image-instances/{id}/reject`
- `POST /api/image-instances/{id}/representative`
- `PATCH /api/shot-pages/{id}/image-order`
- `GET /api/image-instances/{id}/tracking`
- `POST /api/image-instances/{id}/copy-params`

**测试**

- `backend/tests/test_output_receiver.py`：覆盖解析、下载、写入、快照、API 端点。

### MOD-09 最终版本与导出

**核心能力**

- 多个独立最终版本（命名、列表、删除）。
- 按分镜页和页内采用顺序生成默认成片序列（`generate_default_sequence`）。
- 跨分镜页调整最终顺序（独立 `sort_order`，不影响剧本页序）。
- 同一图片可被多个最终版本复用（多对多）。
- 保留每张图片的来源页与分支路径。
- 导出预设（PNG/JPEG/原格式、是否移除元数据、命名模板）。
- 导出任务执行（重新编号、复制/硬链接、JSON/CSV manifest）。

**新增表**

- `final_versions`、`final_version_items`、`export_presets`、`export_jobs`、`export_job_items`。

**测试**

- `backend/tests/test_final_versions.py`：覆盖默认序列、版本 CRUD、排序、导出预设与任务全链路。

### MOD-10 百万级全局图库

**核心能力**

- 文件级元数据索引表 `gallery_index`，冗余来源项目/页面/任务、尺寸、格式、种子、提取后的提示词。
- FTS5 虚拟表 `gallery_fts`，支持提示词全文检索（unicode61 分词）。
- 增量索引 `index_file_for_gallery` 与全量重建 `reindex_gallery`。
- 内容哈希（SHA256）去重 `find_duplicate_images`。
- 感知哈希（aHash，64bit/16hex）近似检测 `find_similar_images`，汉明距离阈值可调。
- 游标分页 `list_gallery_images`，支持多条件筛选（项目、MIME、是否含 phash、状态）与多字段排序（创建时间、尺寸、文件大小）。
- 图片详情与来源相关实例查询。

**新增表与索引**

- `gallery_index`（含 `idx_gallery_index_created` 等多个筛选索引）。
- `gallery_fts`（FTS5 虚拟表）。
- `idx_files_perceptual_hash`（部分索引，加速近似搜索）。

**测试**

- `backend/tests/test_gallery.py`：57 个测试用例，覆盖 phash 计算/稳定性/相似图、汉明距离、FTS5 搜索、游标分页、相同/近似图片搜索。
- 本地实跑：`Ran 57 tests in 13.829s — OK`。

**已知缺口**

- 缩略图任务已建（`background_jobs.job_type='thumbnail'`），但实际 256px/640px WebP 生成 worker 不在本模块范围。
- 二维虚拟网格、离屏释放、百万级性能验收属于前端与运维范畴，未在本模块实现。

### MOD-11 存储、备份与维护

**核心能力**

- SQLite 在线备份（`backup_database`，使用 `online_backup` API，不阻塞写入）。
- 数据库优化（`optimize_database`，ANALYZE + 索引重建）。
- 完整性检查（`integrity_check`，PRAGMA integrity_check + foreign_key_check）。
- 系统信息（数据库大小、表行数、版本清单、配置项）。
- 孤立文件检查（存储目录中未在 `files` 表登记的文件）。
- 缓存清理（缩略图、临时文件）。

**新增 API**

- `POST /api/maintenance/backup`
- `POST /api/maintenance/optimize`
- `GET /api/maintenance/integrity`
- `GET /api/maintenance/system-info`
- `GET /api/maintenance/orphan-files`
- `POST /api/maintenance/clear-cache`

**测试**

- `backend/tests/test_maintenance.py`：覆盖备份产物、优化幂等、完整性报告、孤立文件检测、缓存清理。

### MOD-12 导入与外部扩展

**核心能力**

- 素材包导入导出：序列化素材、素材页、标签、预览图到 `.zip`，支持 dry-run 预检报告。
- 项目包导入导出：序列化项目、章节、场景、分支、人物关联到 `.zip`，支持 dry-run 预检报告。
- 旧 AI 作图笔记目录扫描：递归扫描指定目录，识别图片与元数据，输出导入预检报告。
- 历史图片原地索引与迁移：`index_legacy_library` 将扫描到的图片写入 `files` 与 `gallery_index`。
- 导入预检报告：冲突检测、字段缺失检测、可导入项统计。

**新增 API**

- `POST /api/import-export/materials/export`
- `POST /api/import-export/materials/import`
- `POST /api/import-export/projects/export`
- `POST /api/import-export/projects/import`
- `POST /api/import-export/legacy/scan`
- `POST /api/import-export/legacy/import`
- `GET /api/import-export/dry-run/{job_id}`

**测试**

- `backend/tests/test_import_export.py`：覆盖素材包/项目包导入导出、旧笔记扫描、dry-run 报告。

## 跨模块协调与已知缺口

### 数据库迁移

- 所有模块均通过 `database.py` 中的 `_migrate_*` 方法以 `CREATE TABLE IF NOT EXISTS` / `CREATE INDEX IF NOT EXISTS` 幂等迁移，不依赖手动 SQL。
- MOD-10 的 `gallery_index`、`gallery_fts` 通过 `_migrate_gallery` 单独管理。
- 五条分支在 `database.py` 上的改动位置互不重叠，合并时无结构性冲突。

### MOD 间依赖处理

- MOD-09 在 `generate_default_sequence` 中原本引用 `image_instances.is_rejected` 列（MOD-08 引入），但 MOD-09 分支不包含 MOD-08 迁移。已通过 SQL 改写规避：仅使用 `is_adopted` 和 `sort_order` 推导默认序列，`is_rejected` 列在合并 MOD-08 后自动可用，无需再次改码。
- MOD-10 的图库索引依赖于 `files` 和 `image_instances` 表（MOD-07 与 MOD-08 提供），通过 `extract_prompt_text` 兼容 resolved_json/snapshot_json 两种来源，未与具体列耦合。

### 不在本轮后端范围

- MOD-13 主题与可选视觉重写：纯前端模块，启动条件要求用户明确批准后再迁移，本轮不实现。
- 缩略图实际生成 worker（256/640 WebP）：MOD-10 只建任务，生成逻辑留给后续运维模块。
- 百万级性能验收：需要真实数据集，不属于开发交付。
- ComfyUI 实例、超时与队列参数配置：依赖前端表单，不在本轮后端补全范围。

## 系统功能清单更新

`系统功能清单.md` 已同步更新，将 MOD-08~12 涉及的 25 项功能从 `[ ]` 调整为 `[~]`（后端已实现，前端未接入），1 项（图片实例保存来源页、工作流、提示词、种子和参数）由 `[~]` 升级为 `[x]`。

## 自动化测试

- MOD-10 本地实跑：`python -m unittest backend.tests.test_gallery -v` → 57 项通过，0 失败。
- MOD-08/09/11/12 测试在各自分支开发过程中已实跑通过。
- 五条分支未引入对生产数据库的写操作，所有测试使用临时数据目录 + `locked_environment="test"`。

## 后续建议

1. 按模块独立 review 后合入 `accept-260730-GPT5.6-stage1-3` 或后续集成分支。
2. 启动前端联调分支，将 MOD-08~12 的 API 接入对应页面。
3. 补齐缩略图生成 worker 与百万级图库性能验收。
4. MOD-13 视用户批准节奏独立排期。

---

## Gap-Fill 补全（2026-07-30 追加）

在 MOD-08~12 五条分支合并到 `dev-260730-GLM5.2-gap-fill` 集成分支后，对需求文档与实际实现的差异进行了全面对比，发现并补齐了以下缺口。所有补全均提交在 `dev-260730-GLM5.2-gap-fill` 分支上。

### 补全提交记录

| 提交 SHA | 模块 | 内容 | 测试数 |
|---|---|---|---|
| `4edc6c8` | 缩略图 | 256/640 WebP 生成 worker，重试/租约/重建 API | 29 |
| `aa240bb` | MOD-09 | 导出执行（copy/hardlink/reencode, manifest, cancel, rebuild-from-adoptions） | 20 |
| `3cb00c3` | MOD-11 | 修复 restore_database SQLite 头部校验（`SQLite format 3`）和 `check_orphaned_files` 方法名 | — |
| `c4f67f3` | MOD-07 | 全局 `/api/events` SSE，EventBus 环形缓冲回放、Last-Event-ID 重连、类型过滤、跨模块事件钩子 | 28 |
| `df421fb` | MOD-12 | 历史图片原地索引 `index_legacy_library`，检查点 `legacy_indexed_files` 表，暂停/恢复/取消 | 32 |
| `8278b3c` | MOD-08 | 图片审片增强：`star_rating`/`color_label`/`review_note` 字段 + `image_tags`/`image_tag_links` 表 + API | 35 |

### MOD-07 全局事件总线 SSE

**新增文件**

- `backend/app/event_bus.py`：`EventBus` 类，线程安全发布订阅，环形缓冲回放（默认 256 条），`sse_events_generator` 异步生成器，模块级 `publish_event` 便捷函数。

**API 端点**

- `GET /api/events`：SSE 流，支持 `Last-Event-ID` 重连、`types` 过滤、`timeout`、`max_events`。
- `GET /api/events/stats`：总线统计（订阅者数、缓冲区大小、下一事件 ID）。

**跨模块事件钩子**

- `task_queue.py`：任务事件 → `task.{event_type}`
- `comfyui_progress.py`：进度事件 → `attempt.progress` / `attempt.{status}`
- `thumbnail_worker.py`：`thumbnail.completed` / `thumbnail.failed` / `thumbnail.batch_progress`
- `export_runner.py`：`export.job_started` / `export.item_progress` / `export.job_completed` / `export.job_cancelled`
- `gallery.py`：`gallery.index_progress` / `gallery.reindex_completed`
- `legacy_import.py`：`legacy_index.started` / `legacy_index.progress` / `legacy_index.completed` / `legacy_index.cancelled`

### MOD-08 图片审片增强

**数据库迁移 v0.9.3**

- `image_instances` 新增 `star_rating`（0-5）、`color_label`（none/red/orange/yellow/green/blue/purple）、`review_note`（TEXT）。
- 新增 `image_tags` 表：`id`、`name`（UNIQUE）、`normalized_name`（UNIQUE）、`color`。
- 新增 `image_tag_links` 表：`image_instance_id` ↔ `tag_id` 多对多关联（UNIQUE 防重）。

**API 端点**

- `PATCH /api/image-instances/{id}/review`：更新评分/颜色标记/备注（部分更新，None 字段不覆盖）。
- `POST /api/image-tags`：创建标签（normalized_name 去重）。
- `GET /api/image-tags`：列出所有标签（含使用计数）。
- `DELETE /api/image-tags/{id}`：删除标签（级联删除关联）。
- `POST /api/image-instances/{id}/tags/{tag_id}`：为图片添加标签（幂等）。
- `DELETE /api/image-instances/{id}/tags/{tag_id}`：移除标签。
- `GET /api/image-instances/{id}/tags`：获取图片所有标签。

### MOD-09 导出执行补全

**核心能力**

- `execute_export_job`：消费 `export_jobs` 表，按预设复制/硬链接/格式转换/移除元数据。
- 文件名冲突策略：overwrite / skip / suffix。
- 元数据移除：PIL 重新保存无 EXIF 图片。
- 生成 JSON + CSV manifest。
- `cancel_export_job`：取消导出任务。
- `run_export_worker_once`：领取并执行一个 pending 任务。
- `rebuild_final_version_from_adoptions`：从采用列表重建最终版本条目。

### MOD-12 历史图片原地索引

**新增文件**

- `backend/app/legacy_import.py`：`index_legacy_library` 后台增量索引，支持检查点和暂停/恢复。

**数据库迁移 v0.9.2**

- `legacy_indexed_files` 表：`job_id`、`source_path`（UNIQUE per job）、`content_hash`、`file_id`、`status`（indexed/skipped/missing/duplicate/error）。

**核心能力**

- 复用 `background_jobs` 表（`job_type='legacy_index'）` 作为作业载体。
- `progress_json` 存储检查点（`last_processed_path` + 统计计数）。
- `legacy_indexed_files` 表记录每个已处理文件，避免重复处理。
- SHA-256 `content_hash` 去重，重复文件标记 `duplicate`。
- 文件不存在标记 `missing`，不删除数据库记录。
- `link_mode='hardlink'`（默认）或 `'copy'`：硬链接或复制到 `<data_root>/images/`。
- 作业完成后批量同步 `gallery_index`。
- 每处理 20 个文件提交一次检查点并发布进度事件。

**API 端点**

- `POST /api/import/legacy/index`：创建索引作业。
- `POST /api/import/legacy/index/run`：运行一轮（领取 pending 作业）。
- `POST /api/import/legacy/index/{id}/execute`：直接执行指定作业。
- `GET /api/import/legacy/index/{id}`：查询状态。
- `POST /api/import/legacy/index/{id}/pause`：暂停。
- `POST /api/import/legacy/index/{id}/resume`：恢复。
- `POST /api/import/legacy/index/{id}/cancel`：取消。

### 缩略图生成 Worker

**新增文件**

- `backend/app/thumbnail_worker.py`：256px / 640px WebP 缩略图生成。

**核心能力**

- 消费 `background_jobs`（`job_type='thumbnail'`）。
- 租约机制防重复执行（默认 120s）。
- 失败重试（最多 5 次后 `failed`）。
- 原图缺失直接 `failed`，不重试。
- WebP 质量 80，按短边对齐。
- `rebuild_all_thumbnails` / `rebuild_thumbnails_for_file`。

### 全量测试结果

```
python -m pytest backend/tests/ -q --tb=short
1419 passed, 9 skipped, 23 warnings in 532.54s (0:08:52)
```

比 gap-fill 前（1363 passed）新增 56 个测试，全部通过。9 个 skip 为已有条件跳过。

### 数据库迁移版本

| 版本 | 内容 |
|---|---|
| v0.7.2 | files / image_instances / thumbnails / background_jobs |
| v0.8.0 | comfyui_instances |
| v0.8.1 | materials FK 引用修复 |
| v0.9.0 | final_versions / final_version_items / export_presets / export_jobs |
| v0.9.1 | gallery_index / gallery_fts (FTS5) / phash 索引 |
| v0.9.2 | legacy_indexed_files（MOD-12 历史索引检查点） |
| v0.9.3 | star_rating / color_label / review_note / image_tags / image_tag_links |

