# Atelier 剩余功能模块化并行开发需求

版本：1.0  
日期：2026-07-30  
适用仓库：`C:\Users\kono707da\Documents\AICode\atelier`  
上位产品需求：`Atelier全功能产品与技术开发需求.md`  
功能状态来源：`系统功能清单.md`

---

## 1. 文档定位

本文档重新规定 Atelier 剩余功能的开发组织方式。

`Atelier全功能产品与技术开发需求.md` 中原有的阶段 1～7，继续用于：

- 描述产品目标；
- 查找原始功能要求；
- 组织最终系统验收；
- 说明业务背景和数据关系。

阶段编号不再作为剩余功能的强制开发顺序。后续开发以本文档定义的独立模块为单位，允许多个编程 AI 同时开发、测试和提交自己的模块。

本规则覆盖：

1. 原阶段 1～3 中尚未完成、尚未接入或尚未验收的功能；
2. 原阶段 4～6 中尚未开发的功能；
3. 用户以后新增但能够形成独立边界的功能；
4. 原阶段 7 的可选视觉工作，但阶段 7 仍须用户明确批准后才能开始。

已经完成并通过验收的功能不要求重写。现有代码是开发基线，模块开发必须兼容已有生产数据。

---

## 2. 核心目标

新的开发方式必须满足：

- 一个模块未完成，不阻塞其他模块开始开发；
- 一个模块测试失败，不阻塞无依赖模块继续测试；
- 前端和后端可以按模块形成纵向闭环；
- 尚未完成的依赖通过冻结契约、模拟数据和适配器解决；
- 每个编程 AI 只负责自己的模块，不擅自修改其他模块的业务规则；
- 每个模块可以独立提交、独立验收、独立回退；
- 最终仍保留跨模块集成验收和全系统验收；
- 模块独立通过不等于整个系统已经完成。

---

## 3. 非目标

本文档不做以下事情：

- 不改变现有产品概念；
- 不删除原需求文档中的功能；
- 不把未开发功能降级为可选功能；
- 不因为并行开发而放弃数据库迁移、事务、错误处理和测试；
- 不允许各模块自行发明不兼容的数据格式；
- 不允许使用生产数据库作为开发测试数据库；
- 不要求为了并行开发立即重写整个后端或前端框架；
- 不允许模块 AI 自行合并到 `main`；
- 不自动开始前端视觉重写。

---

## 4. 开发与验收状态必须分开

每个模块分别记录三种状态，禁止只使用一个百分比：

### 4.1 开发状态

- `未开始`
- `开发中`
- `代码完成`

### 4.2 模块验收状态

- `未验收`
- `验收中`
- `模块通过`
- `模块失败`

### 4.3 集成状态

- `独立运行`：只使用模块自己的数据；
- `契约联调`：依赖模块由 mock、fixture 或适配器提供；
- `真实联调`：使用真实依赖模块完成；
- `系统通过`：已经纳入全系统端到端验收。

只有前端、后端、持久化和真实浏览器操作全部闭环，且真实依赖已经联调，才能建议把 `系统功能清单.md` 中对应功能更新为 `[x]`。

模块在契约模拟环境通过时，可以标记“模块通过、契约联调”，但不能伪装成“系统通过”。

---

## 5. 并行开发总规则

### 5.1 模块之间不设置线性门槛

- `MOD-08 项目审片` 不需要等待整个任务中心完成后才开发；
- `MOD-09 最终装配与导出` 不需要等待审片模块真实完成后才开发；
- `MOD-10 百万级图库` 不需要等待最终装配完成后才开发；
- `MOD-11 运维与恢复` 可以和业务模块同时开发；
- 原阶段 1～3 的缺口可以与阶段 4～6 的新模块同时开发。

缺少上游模块时，使用本文档规定的接口契约和固定 fixture 继续开发。

### 5.2 只有硬冲突需要协调

出现以下情况时才需要模块之间协调：

- 同时修改同一数据库表的同一字段含义；
- 同时占用同一 API 路径；
- 同时修改同一前端路由的主要渲染函数；
- 同时修改同一状态机；
- 同时修改同一迁移版本；
- 一个模块准备删除另一个模块正在使用的字段或接口。

普通依赖、页面跳转、数据暂时为空、上游尚未完成，都不是停止开发的理由。

### 5.3 未完成依赖的处理方式

依赖功能尚未完成时，模块 AI 必须：

1. 定义所需的最小请求和响应契约；
2. 在测试中提供固定 fixture；
3. 前端通过可替换的数据适配器读取；
4. 后端通过接口或 repository 边界访问；
5. 建立契约测试；
6. 在交付报告中标记“等待真实联调”；
7. 不进入其他模块内部代写其完整业务。

### 5.4 模块内部仍需纵向闭环

允许多个模块并行，不代表一个模块可以只写后端后长期搁置前端。

单个模块内部默认仍按以下顺序完成：

1. 范围与非目标；
2. 数据结构和迁移；
3. 后端领域逻辑；
4. API；
5. 后端测试；
6. 前端 API 接入；
7. 前端交互；
8. 契约测试；
9. 模拟依赖验收；
10. 真实浏览器验收；
11. 真实依赖联调；
12. 模块交付报告。

不同模块可以同时处于上述不同步骤。

---

## 6. 分支、工作区与所有权

### 6.1 基线

并行开发开始前，由协调者记录：

- 基线分支；
- 基线提交 SHA；
- 数据库 schema 版本；
- API OpenAPI 快照；
- `系统功能清单.md` 状态；
- 当前生产数据库备份状态；
- 当前可用的测试命令。

所有模块分支从同一个确认基线创建。基线变化后，已经开始开发的模块不强制立即重建分支。

### 6.2 分支命名

```text
dev-mod-<模块编号>-<简短名称>-<AI标识>
```

示例：

```text
dev-mod-08-review-glm
dev-mod-09-assembly-gpt
dev-mod-10-gallery-codex
```

建议每个模块使用独立 Git worktree，避免多个 AI 在同一工作目录覆盖文件。

### 6.3 模块所有权

每个模块开始前登记：

- 模块编号；
- 负责人；
- 分支；
- 预计修改的数据表；
- 预计新增的 API；
- 预计修改的前端路由；
- 预计修改的共享文件；
- 对其他模块提供的契约。

同一模块同一时间只有一个主负责人。其他 AI 可以做只读验收或独立测试，但不能同时写同一实现。

### 6.4 共享文件

以下文件属于高冲突区：

- `backend/app/database.py`
- `backend/app/app_factory.py`
- `design/ui-preview/runtime-api.js`
- `design/ui-preview/app.js`
- `系统功能清单.md`
- 全局 CSS 和启动脚本

模块应优先新增独立的 router、service、repository、schema、前端 controller 和样式文件。

必须修改高冲突文件时：

1. 只做最小接线；
2. 将接线改动与模块主体分成不同提交；
3. 在交付报告中列出准确位置；
4. 不顺手重构无关代码；
5. 集成者负责解决不同模块的接线冲突。

---

## 7. API 与数据契约

### 7.1 API 规则

- 新接口统一放在 `/api/...`；
- 请求和响应必须有明确模型；
- 列表接口统一返回稳定分页结构；
- 时间统一使用带时区 ISO 8601；
- 写操作支持 revision 或等价并发保护；
- 删除操作明确软删除、硬删除和级联影响；
- 错误使用统一错误结构和 request ID；
- 模块 API 不返回前端专用的静态演示数据。

### 7.2 跨模块引用

跨模块只保存稳定 ID 和必要快照，不直接复制对方整行数据。

最低稳定标识：

- `project_id`
- `chapter_id`
- `large_scene_id`
- `small_scene_id`
- `scene_page_id`
- `material_id`
- `material_page_id`
- `character_id`
- `character_variant_id`
- `project_spec_id`
- `workflow_id`
- `workflow_version_id`
- `batch_id`
- `task_id`
- `attempt_id`
- `file_id`
- `image_instance_id`
- `review_decision_id`
- `final_version_id`
- `export_job_id`

### 7.3 契约 fixture

每个提供跨模块数据的模块必须附带：

- 最小正常数据；
- 空数据；
- 大列表数据；
- 已删除引用；
- 缺失文件；
- revision 冲突；
- 未知枚举值；
- 旧版本数据。

fixture 放在测试目录，不写入生产数据库。

### 7.4 数据库迁移

并行模块不得重复使用迁移版本。

每个模块交付：

- 模块迁移标识；
- 向前迁移函数；
- 幂等检查；
- 旧库升级测试；
- 空库初始化测试；
- 生产数据保持测试；
- 回滚策略或不可回滚说明。

如果现有迁移仍集中在 `database.py`，模块主体可以先在独立文件中实现迁移函数，由集成者统一登记执行顺序。登记冲突不阻塞模块服务和界面开发。

---

## 8. 模块划分

## MOD-01 项目生命周期

### 范围

- 项目改名和基础信息编辑；
- 项目安全删除；
- 删除影响预览；
- 项目搜索、筛选和排序；
- 项目复制；
- 项目归档和恢复；
- 项目默认工作流和默认生成策略。

### 独立契约

只依赖现有 `project_id`。工作流未完成时，默认工作流字段允许引用 fixture `workflow_version_id`。

### 验收

- 创建、编辑、复制、归档、恢复和删除均可刷新保持；
- 删除不会误删其他项目资源；
- 引用影响可见；
- 搜索和筛选不使用展示数据；
- revision 冲突可见。

---

## MOD-02 素材页、版本与复用

### 范围

- 一个素材包含任意数量素材页；
- 素材页创建、编辑、删除和排序；
- 素材页预览图；
- 素材版本历史；
- 项目、场景和页面引用反查；
- 删除影响与回收站；
- 链接引用；
- 复制后独立；
- 镜头模板、场景包和转场包。

### 独立契约

向剧本模块提供稳定的 `material_id`、`material_page_id`、类型、排序和版本快照。

### 验收

- 1000 页素材仍使用分页或虚拟列表；
- 排序刷新后保持；
- 被引用素材删除时给出影响；
- 链接引用更新可追踪；
- 独立副本修改不影响来源。

---

## MOD-03 人物高级管理与替换

### 范围

- 人物与规格预览图；
- 标签、搜索、筛选；
- 归档和恢复；
- 人物替换矩阵；
- 批量粘贴；
- 默认 LoRA；
- 规格完整性检查；
- 分镜页引用展示；
- 人物值解析和语义插槽输出。

### 独立契约

向工作流和编译模块提供只读人物值解析接口。工作流模块未完成时，用语义插槽 fixture 验证输出。

### 验收

- 替换人物后不会覆盖原人物数据；
- 变体 × 规格值可稳定解析；
- 缺失规格明确阻塞；
- LoRA 和提示词替换结果可预览；
- 大批量粘贴可撤销或有确认。

---

## MOD-04 剧本分支、继承与版本

### 范围

- 小场景完整 CRUD 和排序；
- 分镜页完整 CRUD 和排序；
- 分类素材绑定；
- 素材页映射和单类型唯一约束；
- 转场结构块；
- 大场景和小场景条件分支；
- 分支人物、素材和参数覆盖；
- 自动保存、撤销和重做；
- 快照和版本；
- 页面级继承来源；
- 编译预检查。

### 独立契约

向编译模块提供稳定的只读“项目结构快照”。编译模块未完成时，只验证快照结构和确定性。

### 验收

- 章节、大场景、小场景和页面可完整刷新恢复；
- 同一分类只能绑定一个素材，素材页可映射多个场景页；
- 分支互不覆盖；
- 撤销、重做和快照不破坏引用；
- 预检查能定位到具体页面和字段。

---

## MOD-05 工作流库与规整节点画布

### 范围

- 工作流 CRUD、复制、归档和恢复；
- UI JSON、API JSON 和图片元数据导入；
- 草稿、版本、发布和项目副本；
- 标准、自定义和未知节点；
- 所有节点组件参数；
- 端口连线和类型校验；
- 禁止自由拖动的拓扑布局；
- 泳道、前后移动、换列和自动避让；
- 节点折叠、上下游聚焦和错误定位；
- 模型、LoRA 和枚举搜索；
- 语义插槽；
- UI 与 API 工作流转换；
- 导入—编辑—导出往返。

### 独立契约

人物、素材和项目值未完成时，使用固定语义插槽 fixture。ComfyUI 不可达时允许使用已缓存节点定义开发，但真实验收必须选择可达实例。

### 验收

- 任意已安装节点不会导致画布崩溃；
- 编辑后导出使用最新值；
- 未知字段可保留；
- 500 节点性能测试通过；
- 真实工作流在 ComfyUI 校验通过。

---

## MOD-06 跑图列表与批量配置前端

### 范围

- 从结构快照编译页级跑图列表；
- 按项目、章节、场景、分支和页面选择范围；
- 页面实例数；
- 工作流、尺寸和种子策略；
- 配置继承来源；
- 批量覆盖和预设；
- 阻塞项、警告和任务量预估；
- 跳过已达到采用数量的页面；
- 只选失败项重跑。

### 独立契约

使用固定结构快照、工作流版本和人物值 fixture。任务中心未完成时，提交结果可以停在不可变批次快照。

### 验收

- 相同输入产生相同编译结果；
- 提交前能看到全部最终参数；
- 刷新不会改变已提交批次；
- 阻塞项不会进入任务队列；
- 警告允许用户确认继续。

---

## MOD-07 ComfyUI 执行与任务中心

### 范围

- 持久化任务队列；
- 提交、幂等和 attempt；
- SSE 实时进度；
- WebSocket 和 history 轮询；
- 暂停、继续、取消、重试和优先级；
- 断网恢复；
- 应用重启恢复；
- 错误详情和工作流快照；
- 输出收集和图片实例；
- 任务中心列表、筛选和批量操作。

### 独立契约

批量配置未完成时，使用固定 batch fixture；审片未完成时，只输出标准 `image_instance`。

### 验收

- 使用用户选择的真实 ComfyUI 实例；
- 重复请求不重复提交；
- 断网后保留 `prompt_id`；
- 重启后可恢复或标记明确未知状态；
- 只重跑失败项；
- 成功图片不会因失败重试重复；
- SSE 测试必须设置终止条件，不允许无限等待。

---

## MOD-08 图片详情与项目审片

### 范围

- 图片详情和完整生成追踪；
- 按项目结构和场景页聚合候选实例；
- 未审、已采用、无采用和失败页筛选；
- 放大和并排比较；
- 评分、颜色、备注、淘汰和代表图；
- 单页采用多张图片；
- 页内采用排序；
- 复制参数再次生成。

### 独立契约

任务中心未完成时，使用标准 `image_instance` fixture。每个 fixture 必须含来源页面、工作流版本、参数快照、文件状态和缩略图。

### 验收

- 单页可以采用多张图并排序；
- 采用、淘汰和代表图是独立状态；
- 刷新和重启后保持；
- 文件缺失不会导致页面崩溃；
- 十万候选实例时只加载当前项目和可见区域。

---

## MOD-09 最终版本与导出

### 范围

- 用户命名的多个最终版本；
- 按场景页和页内采用顺序生成默认序列；
- 跨页拖动排序；
- 同一图片复用于多个最终版本；
- 保存来源页和分支路径；
- PNG、JPEG 和原格式；
- 复制、硬链接及降级；
- 元数据保留策略；
- JSON、CSV 来源清单；
- 导出预设、历史、取消和失败项；
- 临时目录完成后原子发布。

### 独立契约

审片模块未完成时，使用固定 adoption fixture。图库未完成时，直接通过 `file_id` 读取文件服务。

### 验收

- 最终排序不修改剧本页顺序和采用顺序；
- 两个最终版本互不影响；
- 重名处理确定；
- 导出失败不留下伪完成目录；
- 来源清单可以反查图片实例、页面、任务和工作流。

---

## MOD-10 百万级全局图库

### 范围

- 文件与图片实例索引；
- 增量索引；
- 内容哈希和感知哈希；
- 256px、640px WebP 缩略图；
- 游标分页；
- 多条件筛选和排序；
- FTS5 提示词搜索；
- 相同和近似图片搜索；
- 二维虚拟网格；
- 可视区域懒加载；
- 离屏请求取消和资源释放；
- 图片详情和关联实例；
- 百万条数据性能测试。

### 独立契约

生成、审片和导出模块未完成时，通过百万条模拟元数据和少量真实缩略图开发。不得要求先生成一百万张真实图片。

### 验收

- 百万条模拟元数据下首屏和查询满足性能指标；
- DOM 数量有上限；
- 连续滚动内存不持续增长；
- 列表不读取原图；
- 列表不扫描文件目录；
- 失败缩略图可以重试。

---

## MOD-11 存储、备份与维护

### 范围

- 可配置数据、图片和缓存目录；
- SQLite 在线备份；
- 配置和版本清单；
- 恢复前自动备份；
- 恢复后迁移与完整性检查；
- 重建缩略图；
- 重建 FTS；
- 重算感知哈希；
- 缺失和孤立文件检查；
- 临时文件、缓存和回收站清理；
- 数据库优化和 checkpoint；
- 持久化、可取消、可恢复的维护任务。

### 独立契约

业务模块未完成时，使用通用文件记录和数据库副本测试。维护任务不得理解具体业务页面。

### 验收

- 全部测试在临时目录执行；
- 生产数据库不可作为恢复演练目标；
- 恢复失败可以回到恢复前状态；
- 路径切换有权限、空间和可写检查；
- 维护任务重启后可继续或安全终止。

---

## MOD-12 导入与外部扩展

### 范围

- 旧 AI 作图笔记扫描方案；
- 历史图片原地索引；
- 外部工作流导入导出；
- 素材包导入导出；
- 项目包导入导出；
- 冲突、重复和版本处理；
- 导入预检、预览、执行和报告。

### 独立契约

导入目标模块未完成时，导入器只生成中间 manifest 和 dry-run 报告，不直接写入未知表结构。

### 验收

- 默认只读扫描；
- 未经确认不移动原文件；
- dry-run 与实际执行使用同一解析结果；
- 中途失败有清晰报告；
- 重复导入幂等；
- 导入来源可追踪。

---

## MOD-13 主题与可选视觉重写

### 范围

- 多彩主题；
- 主题切换；
- 设计 token；
- 用户明确批准后的全页面视觉迁移。

### 启动条件

该模块可以提前设计主题 token 和独立组件实验，但在用户明确批准前：

- 不整体替换现有页面；
- 不删除现有前端；
- 不修改业务 API；
- 不改变已经通过的交互语义。

---

## 9. 推荐并行分组

以下只是降低冲突的建议，不是线性顺序：

### 组 A：生产资料

- `MOD-01 项目生命周期`
- `MOD-02 素材页、版本与复用`
- `MOD-03 人物高级管理与替换`
- `MOD-04 剧本分支、继承与版本`

### 组 B：工作流与生成

- `MOD-05 工作流库与规整节点画布`
- `MOD-06 跑图列表与批量配置前端`
- `MOD-07 ComfyUI 执行与任务中心`

### 组 C：成片流程

- `MOD-08 图片详情与项目审片`
- `MOD-09 最终版本与导出`

### 组 D：规模和运维

- `MOD-10 百万级全局图库`
- `MOD-11 存储、备份与维护`
- `MOD-12 导入与外部扩展`

### 组 E：可选视觉

- `MOD-13 主题与可选视觉重写`

同一组内可以并行，不同组之间也可以并行。

---

## 10. 并行依赖矩阵

| 消费模块 | 可能缺少的提供模块 | 未完成时的开发方式 |
|---|---|---|
| MOD-03 人物替换 | MOD-05 语义插槽 | 使用插槽 schema fixture |
| MOD-04 剧本 | MOD-06 编译器 | 输出固定结构快照并做确定性测试 |
| MOD-05 工作流 | MOD-03 人物值 | 使用人物值 fixture |
| MOD-06 批量配置 | MOD-04、MOD-05 | 使用结构快照和工作流版本 fixture |
| MOD-07 任务中心 | MOD-06 批次 | 使用不可变 batch fixture |
| MOD-08 审片 | MOD-07 图片实例 | 使用 image_instance fixture |
| MOD-09 装配导出 | MOD-08 采用结果 | 使用 adoption fixture |
| MOD-10 图库 | MOD-07、MOD-08 | 使用模拟文件索引和图片实例 |
| MOD-12 导入 | 任意目标模块 | 先输出 manifest 和 dry-run |

表中的依赖不构成开发阻塞。只有真实集成状态会等待提供模块。

---

## 11. 测试要求

### 11.1 模块测试

每个模块至少包含：

- 正常路径；
- 空状态；
- 错误路径；
- 权限和路径安全；
- revision 冲突；
- 重复请求和幂等；
- 删除影响；
- 旧数据库迁移；
- 刷新恢复；
- 大数据分页；
- 前端 API 契约；
- 浏览器核心操作。

### 11.2 隔离

- 使用临时数据库和临时目录；
- 不修改生产数据库；
- 不依赖另一个 AI 正在运行的服务；
- 固定随机种子；
- 固定 fixture 版本；
- 测试结束清理自己创建的数据；
- SSE、WebSocket 和后台任务必须有超时与终止条件。

### 11.3 回归范围

模块开发期间只要求：

1. 模块自己的完整测试；
2. 直接契约测试；
3. 受修改共享文件影响的测试；
4. 前端语法或构建检查。

模块未完成不会阻塞其他模块运行自己的测试。

模块合并到集成分支后，由集成者运行全量回归和跨模块测试。

---

## 12. 合并和集成

### 12.1 模块 AI 的职责

模块 AI 必须：

- 在自己的分支开发；
- 提交模块代码和测试；
- 提供迁移说明；
- 提供接口契约；
- 提供模拟 fixture；
- 提供浏览器验收步骤；
- 记录未完成的真实依赖；
- 报告共享文件改动；
- 不自行合并到 `main`。

### 12.2 集成者的职责

集成者负责：

- 确认基线；
- 检查模块边界；
- 解决迁移登记顺序；
- 解决 app router 和前端路由接线冲突；
- 运行契约测试；
- 运行全量回归；
- 进行真实依赖联调；
- 更新 `系统功能清单.md`；
- 决定是否合并到 `main`。

### 12.3 合并失败不阻塞其他模块

某模块因为冲突或测试失败未能合并时：

- 该模块保持在自己的分支整改；
- 已通过的其他模块继续集成；
- 无依赖模块继续开发；
- 不回滚其他模块的独立成果；
- 只有共享基线存在数据破坏风险时才暂停相关合并。

---

## 13. 模块交付物

每个模块必须提交：

1. 模块编号和名称；
2. 基线提交和模块分支；
3. 完成范围；
4. 明确未完成范围；
5. 数据库迁移；
6. API 清单；
7. 前端路由和操作路径；
8. 跨模块契约；
9. fixture；
10. 自动化测试命令和结果；
11. 浏览器验收结果；
12. 性能结果；
13. 已知风险；
14. 共享文件改动；
15. Git 提交；
16. 开发状态、模块验收状态和集成状态。

交付报告不需要等待其他模块完成。

---

## 14. 最终集成验收

模块化并行开发不取消最终系统验收。

当多个模块完成后，按用户实际使用流程组合验收：

1. 项目、素材、人物和剧本；
2. 工作流；
3. 批量生成和任务恢复；
4. 审片；
5. 最终装配和导出；
6. 图库；
7. 备份和恢复；
8. 导入和迁移。

集成验收发现问题时，只阻塞受影响的真实流程，不把无关模块退回“未开发”。

最终状态必须同时展示：

- 模块独立通过情况；
- 模块真实联调情况；
- 全系统流程通过情况。

---

## 15. 编程 AI 开始开发时的固定回复

编程 AI 接到开发任务后，先回复：

```text
仓库：<绝对路径>
基线提交：<SHA>
模块：<MOD 编号和名称>
分支：<分支>

本模块负责：
- ...

本模块不负责：
- ...

依赖契约：
- ...

缺失依赖的模拟方式：
- ...

预计修改的共享文件：
- ...

准备先完成的可验收闭环：
- ...
```

用户已经指定模块时，不再要求确认“从第几阶段开始”。

用户同时指定多个模块时，可以为每个模块分配独立编程 AI 并行开发。

---

## 16. 优先级原则

本文档不规定强制线性顺序，但可以按以下因素选择模块：

1. 用户当前马上要使用；
2. 模块边界清晰；
3. 与其他正在开发模块文件冲突少；
4. 能形成独立前后端闭环；
5. 能提供其他模块复用的稳定契约；
6. 风险高、需要尽早验证；
7. 可用模拟数据独立验收。

优先级只影响资源安排，不构成开发门槛。

---

## 17. 最终结论

Atelier 后续开发从“按阶段依次推进”改为“按模块并行推进”。

任何单一模块的未完成、失败或延期，只影响：

- 该模块自身状态；
- 直接依赖该模块的真实集成状态；
- 包含该模块的端到端流程。

它不得阻止其他模块：

- 开始开发；
- 使用契约 fixture 测试；
- 完成模块验收；
- 提交独立分支；
- 进入无冲突的集成工作。

最终系统是否完成，仍以全部必要模块的真实联调和全系统验收为准。
