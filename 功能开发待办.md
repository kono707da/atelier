# 功能开发待办

本文件只记录近期开发事项，不用于计算整个 Atelier 的完成率。全系统进度以 `系统功能清单.md` 为准。

- [ ] 项目删除功能
- [x] 章节删除功能
- [x] 章节改名功能
- [x] 合并重复的“添加大场景”和“添加第一个大场景”按钮
- [x] 大场景删除功能
- [x] 大场景改名功能
- [x] 人物库基础功能：人物/形象变体/项目规格的 CRUD，卡片展开管理
- [x] 人物规格值编辑：变体 × 规格的交叉表，编辑提示词/LoRA/权重/模型覆盖/备注
- [ ] 人物替换矩阵页接入真实 API
- [x] 开发者管理：在侧边栏“全局资源”下新增“开发者管理”入口，样式与现有全局资源入口一致；入口内提供“开发进度汇总”按钮，点击后展示全部功能的完成状态与总体进度
- [ ] 调整过渡段层级：过渡段应作为小场景类型，不再作为大场景类型；移除大场景的“内容段/过渡段”选择与徽标，并在开发前制定现有 `large_scenes.scene_type=transition` 数据的安全迁移方案。
- [ ] 修复大场景拖拽到自身的无效排序：拖动目标和插入位置必须排除被拖动项；释放在自身区域或原排序位置时应直接视为无操作，不调用移动接口、不刷新页面，也不显示“已移动”。
- [ ] 简化素材库新建与编辑表单：删除重复的“简介”输入，只保留“素材正文”并改名为“素材内容”；素材卡片和搜索摘要由素材内容自动截取生成，后端暂时兼容已有 `description` 数据，不要求用户重复填写。
